# Rebuild Plan

Last updated: 2026-06-07

## Execution Rules

- Use the mirrored original bundle as the implementation spec:
  - `legacy-mirror/public/assets/bundle.250f01b7.js`
  - `legacy-mirror/public/assets/bundle.87ba3613.css`
- Keep the stack: Astro, TypeScript, Three.js, GSAP, Lenis, Howler.
- Audio must use Howler.
- Do not delete `public/` or `legacy-mirror/`.
- Work in batches of related source-alignment steps, up to ten differences per batch. Keep each batch on one coherent chain such as `GA/VA` or `OA/CA`; shader/render-target batches may stop below ten when the risk is high or an early probe shows regression. Do not test/commit after every isolated one-line change unless it is the only safe change in that chain.
- Implementation decisions must be source-backed. Visual QA is used to catch regressions and attribute remaining differences, not to justify visually beneficial changes without source evidence.
- Prioritize source-structure, source-value, and source-behavior parity over any visual-payoff scoring. Visual improvement is not an implementation goal by itself; production changes need mirrored-bundle evidence even when they look better. Low perceived visual payoff is not a reason to stop while a source mismatch remains open.
- Next-work ordering is based on three accepted criteria: clear mirrored-source mismatch, 1:1 blocker severity, and controllable implementation risk. Expected visual payoff is explicitly not an ordering or rejection criterion.
- After each completed batch:
  - update this document,
  - run build and diff checks,
  - run a browser smoke/QA check when relevant,
  - commit the code and this document together.
- Commits use `Boyang Hu <i@boyang.hu>` and no AI co-author.

## Phase Status

1. Home WebGL source parity: in progress, current priority.
2. Home DOM and interaction parity: paused until Phase 1 is complete.
3. Project detail media system: stable, monitor for regressions.
4. About and auxiliary pages: pending.
5. Global transitions, state, audio, Lenis lifecycle: pending.
6. Final source/visual QA and cleanup: pending.

Cadence: batch related source-backed work into coherent commits, with an upper bound of ten small differences per batch. Use smaller batches for shader/render-target/material changes when risk is higher; run QA and commit once per batch.

## Completed Steps

| Commit | Area | Progress |
| --- | --- | --- |
| `current batch` | Phase 1 / `I1` Optional Lensflare Material Ownership | Matched source `I1` default lensflare ownership without visual tuning. Source `I1.initRenderer()` always clones `renderTargetLensflare`, but `this.lensflareMaterial=new L1` is inside `this.settings.lensflare.enabled&&(...)`, and default `I1.initSettings()` sets `lensflare.enabled:false`. The rebuild now keeps `mainLensflareTarget` full-size but makes `mainLensflareMaterial` optional and only creates it behind `SOURCE_MAIN_LENSFLARE_SETTINGS.enabled`; dispose, resize, render, and probes are guarded. Runtime probes now assert default `enabled:false`, `materialCreated:false`, `materialMode:null`, and `ownership=source-I1-lensflareMaterial-created-only-when-enabled`, while retaining full-size lensflare targets. Renderer audit checks the source optional creation anchor and rejects the old unconditional constructor call. `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, renderer audit, desktop/mobile output probes, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop/mobile output probes confirmed target sizes `1440x900` / `390x844`; project media stayed `5/5` visible for both `gc-2026` and `hashgraph-vc`; full capture had no failures/exceptions. Final band deltas were desktop center `+0.0044` and mobile center `+0.0313`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, remaining `A1/OA/kA/Lu/I1` transfer/composite evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `19beedb` | Phase 1 / `Lu/I1` Pass Material Ownership | Restored source pass-material object ownership for the work and main render managers without visual tuning. Source `Lu.initRenderer()` creates its own `Na` horizontal/vertical blur materials, `ig` FXAA material, `sg` luminosity material, five `rg` bloom blur materials, and `cg` bloom composite material before `kA` replaces only the composite material with `OA`; source `I1.initRenderer()` separately creates its own `Na`, `ig`, `sg`, `rg`, and `cg` materials, plus optional `L1` behind `settings.lensflare.enabled`. The rebuild now splits the old shared/generic `sg/Na/ig` fields into work-owned `workLuminosityMaterial`, `workBlurHorizontalMaterial`, `workBlurVerticalMaterial`, `workFxaaMaterial` and main-owned `mainLuminosityMaterial`, `mainBlurHorizontalMaterial`, `mainBlurVerticalMaterial`, `mainFxaaMaterial`; existing work/main bloom blur and composite materials remain separate and are now covered by ownership probes. Runtime probes expose/assert `source-Lu-pass-materials-owned-by-work-render-manager`, `source-I1-pass-materials-owned-by-main-render-manager`, and all work/main material sharing flags as false. Renderer audit verifies source/rebuild owned material construction anchors and absence of old shared pass-material fields. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Full capture reported no failures/exceptions. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0051` and mobile center `+0.0316`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, remaining `A1/OA/kA/Lu/I1` transfer/composite evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `bd239ac` | Phase 1 / `Lo/H1/O1/x1` Derived Target Clone Ownership | Restored source `Lo` render-target ownership for the sky, wavves/displacement, and thumb render managers without visual tuning. Source `Lo.initRenderer()` creates `renderTargetA` with `{ depthBuffer:false, stencilBuffer:false }` and derives `renderTargetComposite=this.renderTargetA.clone()`; source `H1`, `O1`, and `x1` reuse that target graph while only replacing the composite material. The rebuild now derives `skyCompositeTarget` from `skyRawTarget.clone()`, `displacementTarget` from `displacementRawTarget.clone()`, and `thumbCompositeTarget` from `thumbTarget.clone()`, and removes the rebuild-only independent `1024x1024` thumb targets plus manual thumb-target texture default overrides. Runtime probes expose/assert raw/composite target construction modes for sky, displacement, and thumb; thumb spotlight probe now guards `source-Lo-renderTargetComposite-renderTargetA-clone`. Renderer audit verifies the source `Lo` clone anchors, rebuild clone ownership for all three derived managers, absence of independent thumb 1024 targets, and absence of manual thumb target texture defaults. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0048` and mobile center `+0.0295`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `2c57223` | Phase 1 / `a1/i1` Floor Reflector Target Clone Ownership | Restored three source-owned floor-reflector construction details without visual tuning. Source `i1` is a `Group` subclass, creates `renderTarget` with only `{ depthBuffer:false }`, clones `renderTargetRead` and `renderTargetWrite` from that raw target, and only then toggles `renderTarget.depthBuffer=true`; source does not manually override reflection target texture sampling state after construction. The rebuild now constructs `floorReflector` as `Group`, builds the raw floor reflection target with `{ depthBuffer:false }`, derives read/write targets via `floorReflectionTarget.clone()`, removes the manual `generateMipmaps/minFilter/magFilter` overrides, and keeps the source post-construction raw depth toggle. Runtime output probes expose/assert reflector type, read/write clone construction modes, read/write depth staying false, raw construction/runtime depth ownership, and the existing source blur-swap/render-target-uniform path. Renderer audit now checks the source `i1` clone anchors plus rebuild clone ownership and absence of manual target texture defaults. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0046` and mobile center `+0.0291`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `7f997d4` | Phase 1 / `Qe/Xt/p1` WebP-Selected Asset Extension Ownership | Restored source asset-extension ownership without visual tuning. Source `Qe.init()` detects lossy WebP before `Xt.init()` / WebGL scene setup, source `Xt.preloadTextures()` uses one `Le.WEBP ? "webp" : "jpg"` extension for `floor-normal`, `perlin-1`, and `perlin-2`, and source `p1.addEnvironment()` uses the same WebP-selected extension for `/images/cubemaps/01/[px,nx,ny,py,pz,nz]` before assigning `scene.environment`. The rebuild now uses a cached lossy WebP image probe, stores `sourceWebpSupport` / `sourceAssetExt`, loads those three textures and all six cubemap faces through that selected extension, and removes the hardcoded cubemap webp plus webp-first/jpg-fallback bridge. Runtime probes expose/assert WebP detection mode, support/ext consistency, cubemap load mode, cubemap URL extension consistency, and loaded/failed state. Renderer audit extracts source `Qe`, `Xt.preloadTextures()`, and `p1.addEnvironment()` anchors and verifies the rebuild source-ext ownership. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0045` and mobile center `+0.0299`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `30b0a0d` | Phase 1 / `U1/yg/I1/C1` Main Raw Camera and C1 Update Order | Restored two source-owned main render-manager details without visual tuning. Source `yg` owns a perspective main camera at distance `1000`, far plane `2000`, source `Ef(...)` fov calculation, z position `1000`, and no camera controller; source `U1.update()` runs `super.update()` first, then updates `C1/A1` through `this.renderManager.compositeMaterial.update(...)`. The rebuild now renders `mainRawTarget` with a dedicated source-shaped `mainCamera` instead of the home work camera, recomputes its fov/aspect/far/position on resize, and writes `C1/A1.uTime` after `renderHomeCompositePass()` to match source update ownership. Runtime probes expose/assert `mainRawCameraMode=source-yg-perspective-distance-1000-no-camera-controller`, `mainRawRenderCamera=source-U1-I1-renderTargetA-uses-yg-camera`, `mainCompositeUpdateOrder=source-U1-super-update-renders-I1-before-C1-update`, and `preComposite.uTimeUpdateOrder=source-U1-C1-update-after-I1-render`. Renderer audit extracts source `yg`/`U1` anchors and checks the rebuild camera/render/update-order ownership. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0060` and mobile center `+0.0300`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `3205bcd` | Phase 1 / `Lu/I1` Remaining Screen-Pass Ownership Cleanup | Removed the remaining rebuild-only dedicated pass-scene bridge around source `Lu/I1` screen ownership without visual tuning. Source `Lu.update()` and `I1.update()` use one fullscreen `screen` mesh and repeatedly swap `this.screen.material` for optional blur, lensflare, luminosity, bloom blur/composite, composite, and FXAA passes. The rebuild now removes old dedicated pass scenes for work/main composite, pre-composite, bloom/luminosity/fxaa helpers, and main optional blur/lensflare; `renderHomeBlurPass()` now renders `Na` horizontal/vertical blur through `mainPostScreen`, and `renderMainLensflarePass()` renders `L1` through `mainPostScreen` after the source explicit lensflare clear. Runtime probes expose/assert `optionalBlurScreenMode=source-I1-mainPostScreen-material-swap`, `lensflareScreenMode=source-I1-mainPostScreen-material-swap`, and per-material screen modes for `standardBlur` and `lensflare`. Renderer audit checks source `I1` material-swap anchors, rebuild `mainPostScreen` blur/lensflare assignments, and `rebuildNoDedicatedPassScenes=true`. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0065` and mobile center `+0.0301`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `71998a4` | Phase 1 / `Lo/H1/O1/x1` Screen-Pass Ownership | Restored source `Lo` single-screen material-swap ownership across the sky, wavves/displacement, and thumb render managers without visual tuning. Source `Lo.update()` renders the raw scene to `renderTargetA`, binds `compositeMaterial.uniforms.tScene` to that raw texture, assigns `this.screen.material=this.compositeMaterial`, and renders the same screen mesh through `screenCamera`; source `H1`, `O1`, and `x1` only replace `compositeMaterial` with `z1`, `N1`, and `_1`. The rebuild now uses `skyPostScreen`, `displacementPostScreen`, and `thumbPostScreen` via `makeSourcePassScreen()`, removes the independent composite scenes, and splits displacement into a source raw red scene/target plus composite target so `N1/F1.tScene` reads the raw wavves target instead of self-binding to the composite output. Runtime probes expose/assert the three `source-*-Lo-single-screen-material-swap` ownership markers, `screenMode=source-Lo-screen-material-composite`, raw target bindings, and displacement raw/composite target sizing. Renderer audit now extracts source `O1` and checks `Lo.update()` plus `H1/O1/x1` subclass ownership. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`; a prior transient project-media `Uncaught (in promise)` was not reproduced in the final rerun, which had empty exceptions and console messages. Final band deltas were desktop center `+0.0041` and mobile center `+0.0295`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `f2d7396` | Phase 1 / `Ka/rA/oA` Mouse Simulation Shader Surface | Restored source `Ka` mouse simulation shader-surface parity without visual tuning: source `Ka` binds fragment `rA` and vertex `oA`; `oA` uses the modelView/projection vertex path, while `rA` keeps the noise direction path, commented `lineSegment(...)` and `blur(...)` helpers, inert diffusion placeholders, commented line-segment brush assignment, and active `circle(newUv, posOld, th)` brush. The rebuild now uses a dedicated `mouseSimulationVertex`, dumps `Ka-mouse-simulation`, and restores the source `rA` comments/placeholders around the existing active formulas. Runtime probes expose/assert `source-Ka-rA-oA-shader-surface`, source `oA` vertex mode, source noise path, diffusion placeholders, commented helpers, and circle brush path. Renderer audit extracts source `rA/oA`; shader dump maps `Ka-mouse-simulation` to those source shaders and records `mouseSimulationCoreChecks`; output probe hard-fails on drift. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0048` and mobile center `+0.0304`, recorded only as regression evidence. Phase 1 remains open for spotlight/thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `7ff6db1` | Phase 1 / `E1/M1/w1` Thumb Plane Creation and Visibility Ownership | Restored source thumb-plane creation/visibility ownership without visual tuning: source `E1` creates the plane, assigns `new M1`, scales it to `2,2,2`, and does not set an initial hidden state; source `w1.updateGalleryProgress()` owns visibility through the centered x-wrap rule. The rebuild now removes the rebuild-only `mesh.visible = false` from thumb creation and lets the existing source-shaped gallery update path control visibility. Thumb probe records/asserts `sourceInitialVisibleMode=source-E1-no-initial-hidden-state-w1-updateGalleryProgress-owns-visible` per thumb and asserts source `M1` default `NormalBlending=1`. Renderer audit extracts source `E1`, verifies no source initial hidden state, and verifies the rebuild no longer contains that bridge. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Thumb probe reports active thumb target/composite luma `0.4226` with source x-wrap visibility ownership. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0053` and mobile center `+0.0295`, recorded only as regression evidence. Phase 1 remains open for actual thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `1961746` | Phase 1 / `C1/OA/lA/W1` Source Matrix Fullscreen Vertex Surface | Restored source fullscreen vertex ownership for the central composite materials without visual tuning: source `C1/A1` binds `vertexShader:D1`, while source `OA/CA`, `lA/Lu`, and `W1/j1` bind `vertexShader:el`. The mirrored `tl`, `D1`, and `el` templates are byte-equivalent matrix fullscreen vertices using `modelMatrix`, `viewMatrix`, and `projectionMatrix`; helper passes that source maps to direct clip-space vertices were left unchanged. The rebuild now routes `A1-pre-composite`, `OA-work-composite`, `Lu-main-composite`, and `j1-media-composite` through `sourceMatrixFullscreenVertex`. Runtime probes assert `source-D1-matrix-fullscreen` / `source-el-matrix-fullscreen` vertex modes; renderer audit records source `vertexShader:D1/el` anchors, proves `tl==D1==el`, and verifies the rebuild material bindings; shader dump compares these four vertices against source `D1/el` and reports newline-only vertex deltas with source anchors matched. Renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Full capture reported no failures/exceptions. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0052` and mobile center `+0.0297`, recorded only as regression evidence. Phase 1 remains open for actual thumb projection content/transfer parity, unresolved `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `9fc4e86` | Phase 1 / `Xt/TextureLoader` Loaded Texture Sampling Defaults | Restored source loaded-texture sampling defaults without visual tuning: source `Xt.preloadTextures()` only applies wrapping overrides after `TextureLoader` returns textures (`blueNoise`, `floorNormal`, and `perlin2` use `RepeatWrapping`; `perlin1` uses `MirroredRepeatWrapping`), and source `Xt.loadImage/loadTexture` does not set `minFilter`, `magFilter`, `generateMipmaps`, or `anisotropy`. The rebuild now replaces the loaded-texture `setTextureQuality(...)` override with `applySourceLoadedTextureState(...)`, which only sets `colorSpace`; image-loaded textures keep Three defaults `minFilter=LinearMipmapLinearFilter`, `magFilter=LinearFilter`, `generateMipmaps=true`, `anisotropy=1`, while `VideoTexture` keeps its own Three/source-compatible defaults. Runtime probes expose/assert `sourceLoadedTextureMode=source-Xt-TextureLoader-default-sampling-wrap-only-overrides` plus `sourceTextureProbe(...)` state for noise/perlin/floor-normal/thumb map textures. Renderer audit records source `Xt` anchors, local Three r164 `Texture`/`VideoTexture` defaults, and verifies the helper no longer overrides loaded texture filters/anisotropy. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0049` and mobile center `+0.0294`, recorded only as regression evidence. Phase 1 remains open for actual thumb projection content/transfer parity, `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `390d71b` | Phase 1 / `T1/w1/SD` SpotLight Map Projection Guardrails | Aligned source spotlight-map projection guardrails without visual tuning: source `w1` sets `frustumCulled=false`, source `SD.init()` assigns the thumb composite texture to `J.workScene.spotLight.map`, and source spotlight setup does not explicitly enable `.castShadow`. Renderer audit now checks source no-explicit-`castShadow`, the local Three r164 `WebGLLights.js` path where `light.map` feeds `state.spotLightMap`, `shadow.updateMatrices(light)` updates the matrix, and `light.castShadow` only gates the shadow-count branch. Runtime probes assert `thumbWrapFrustumCulled=false`, `projectionPath=source-SpotLight.map-without-castShadow`, `shadowPathMode=source-map-projection-not-shadow-cast`, and `castShadow=false`; shader dump checks `shadowmap_pars_vertex`, `shadowmap_vertex`, and `lights_fragment_begin` anchors in ordinary `VA-work`. `git diff --check`, `npm run build`, renderer audit, desktop output probe with `PROBE_WAIT=9000` after one async preload-window retry, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0042` and mobile center `+0.0313`, recorded only as regression evidence. Phase 1 remains open for actual thumb projection content/transfer parity, `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `a87ee7d` | Phase 1 / `I1/C1` Default Screen Output Target | Aligned the source `I1/C1` default `renderToScreen` path without visual tuning: source `I1.initSettings()` defaults `renderToScreen:true`, and the default `I1.update()` branch presents `C1/A1` directly to the canvas with `setRenderTarget(null)`, without writing the default output into `renderTargetComposite`. The rebuild now removes the extra default-path `mainPostScreen -> compositeTarget` write before `renderHomeCompositePass()`. Runtime metadata and probes now assert `preCompositeTargetRole=source-I1-renderTargetComposite-unused-in-default-renderToScreen`, `defaultRenderToScreenWritesCompositeTarget=false`, and `productionOutputChanged=false`; renderer audit statically checks that the old target-write sequence is absent. `git diff --check`, `npm run build`, renderer audit, desktop/mobile output probes, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0058` and mobile center `+0.0311`, recorded only as regression evidence. Phase 1 remains open for source-backed spotlight/thumb projection, `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `1104e15` | Phase 1 / `zA` Source `SPECULAR` Macro Guardrail | Closed the ordinary-work `SPECULAR/USE_SPECULAR` attribution gap without visual tuning: the direct `VA-work` `zA` template now keeps source `#define SPECULAR` / `#ifdef SPECULAR`, and runtime probes hard-check that the active work material is `MeshStandardMaterial`, `isMeshPhysicalMaterial=false`, and has no `PHYSICAL` define. This proves the source `PHYSICAL` branch is inactive for ordinary work, so the temporary Three r164 `USE_SPECULAR` bridge is not needed there. Shader dump now classifies `VA-work` as `source-old-specular-macro-standard-material-physical-branch-inactive`, and the residual summarizer reports that restored source macro classification instead of stale bridge wording. `git diff --check`, `npm run build`, renderer audit, desktop/mobile output probes, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0041` and mobile center `+0.0288`, recorded only as regression evidence. Phase 1 remains open for source-backed spotlight/thumb projection, `A1/OA/kA/Lu` target/transfer graph evidence, floor/environment residuals, and interactive mouse/fluid verification. |
| `2980ace` | Phase 1 / `zA` Direct Work Fragment Shader Template | Replaced the ordinary work `VA` fragment generation path with a direct source-shaped `zA` fragment shader template instead of patching and stripping the local Three r164 `meshphysical` fragment template. This keeps the source custom declarations, material surface order, include order, random grid alpha tail, mouse-simulation sampling, reveal/vignette alpha logic, and final source tail. Auxiliary block fragments remain on the safer r164 bridge because they are not ordinary source `VA-work`. Shader dump asserts `noR164StripCommentsInWorkFragment`. This batch originally kept a temporary `SPECULAR -> USE_SPECULAR` bridge; the later S1-201/current source macro guardrail supersedes that attribution and restores source `SPECULAR` for ordinary work by proving the `PHYSICAL` branch inactive. Phase 1 remains open for source-backed spotlight/thumb projection, `OA/CA/kA/Lu` transfer evidence, mobile/fog-bed residuals, and mouse/fluid verification. |
| `current batch` | Phase 1 / `HA` Direct Work Vertex Shader Template | Replaced the ordinary work `VA` vertex generation path with a direct source-shaped `HA` vertex shader template instead of patching and stripping the local Three r164 `meshphysical` vertex template. This keeps the source formulas, constants, include order, world-position path, main varying assignment order, and `uUvOffset` declaration placement, while preserving the documented `uUvOffset vec2` runtime bridge proven by source `VA.customUniforms.uUvOffset` / `GA` `.x/.y` writes. Auxiliary block shaders remain on the safer r164 bridge because they are not the ordinary source `VA-work` shader. Shader dump now asserts `noR164StripCommentsInWorkVertex`; `VA-work` vertex text delta narrowed from `+275` to `-28`, with the final vertex diff limited to `uUvOffset vec2` plus whitespace normalization. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0036` and mobile center `+0.0289`, recorded only as regression evidence. `zA` direct fragment template parity is tracked separately in the latest batch; Phase 1 remains open for `SPECULAR`/`USE_SPECULAR` compile bridge review, `OA/CA` transfer evidence, and mobile/fog-bed residuals. |
| `current batch` | Phase 1 / `HA` Main Varying Assignment Order | Aligned the source `HA` main-function assignment surface without visual tuning: `vUv = uv;` now sits immediately after `void main() {` and before `#include <uv_vertex>`, instance varying assignments now sit after `#include <shadowmap_vertex>` / `#include <fog_vertex>`, and `vWorldPosition` now appears between `vOffset` and `vPosition` exactly once. The patch strips the local Three r164 `USE_TRANSMISSION` tail that would otherwise duplicate the source-positioned assignment. Shader dump now records `sourceUvAssignedBeforeUvChunk`, `sourceInstanceVaryingsAfterFog`, and `singleTransmissionWorldPositionAssignment`, all source/rebuild true; final `VA-work` vertex text delta narrowed from `339` to `275`. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed across the batch; final post-cleanup desktop output and project-media smoke also passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Final band deltas were desktop center `+0.0044` and mobile center `+0.0308`, recorded only as regression evidence. Phase 1 remains open for source-backed render-target/transfer evidence, mobile/fog-bed residuals, and safe deeper generated-material bridge work. |
| `current batch` | Phase 1 / `VA/HA/zA` Declaration and Helper Surface Order | Aligned a source `VA/HA/zA` shader-surface batch without visual tuning: work-block custom vertex declarations now sit before the Three material header like source `HA`; source-proven `uUvOffset` remains a `vec2` bridge but is ordered before reveal uniforms; `vNoise` now sits with `vViewPosition`; fragment custom declarations now precede `#define STANDARD` like source `zA`; and `random(...)` / `vignette(...)` now sit after the fragment pars surface and before `main()`. Shader dump now records `vaVertexCoreChecks` and `vaFragmentCoreChecks`, with `sourceDeclarationBeforeStandard`, `sourceUvOffsetBeforeRevealUniforms`, `sourceVNoiseAfterViewPosition`, `sourceDeclarationsBeforeStandard`, `sourceHelpersAfterPars`, and `sourceHelpersBeforeMain` all source/rebuild true. `VA-work` still has no live include or uniform residuals, and the remaining `SPECULAR`/`USE_SPECULAR` difference remains classified as the r164 compile bridge. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Desktop center-band delta is `+0.0047`; mobile center-band delta is `+0.0314`, recorded only as regression evidence. Phase 1 remains open for source-backed render-target/transfer evidence, mobile/fog-bed residuals, and safe deeper generated-material bridge work. |
| `current batch` | Phase 1 / `A1/C1` Pre-Composite Shader Declaration Surface | Aligned the source `A1` shader surface without visual tuning: `homePreCompositeFragment` now places shared color helpers/full blend dispatcher before `luminance(...)` and `coverTexture(...)`, then declares uniforms in source order, then declares `random(vec2 st)` after `in/out`, matching the mirrored bundle's declaration order. `coverTexture(...)` now keeps the source `vec4 color = texture(tex, uv); return color;` temporary. Runtime probes assert `helperOrderMode=source-A1-helpers-coverTexture-before-uniforms-random-after-uniforms` and `hasSourceCoverTextureTemporary=true`; shader dump now checks `a1SourceDeclarationOrder` and the cover-texture temporary. `A1-pre-composite` fragment text delta narrowed from `-174` to `-168`, with source/rebuild core anchors matched. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Desktop center-band delta is `+0.0033`; mobile center-band delta is `+0.0299`, recorded only as regression evidence. Phase 1 remains open for source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `a1/o1/s1` Floor Material Surface and Guardrails | Aligned a source floor-chain batch without visual tuning: `floorFragment` now matches source `s1` normal-map uniform order (`tNormalMap` before `uNormalScale`) and source literal surfaces for normal distortion, zero theta, and `mix(vec4(0), ...)`. Runtime probes now expose/assert source `o1` material defaults (`transparent=false`, `depthWrite=true`, `depthTest=true`, `blending=NoBlending`, inherited `toneMapped=true`), `a1` `CircleGeometry(60,32)`, `floorGroup -> floorPlane -> reflector` hierarchy, group y `-1.65`, plane rotation `-PI/2`, and matching reflection-state fields on desktop/mobile. Shader residual summaries now include `o1-floor-material` and `t1-floor-reflection-blur`; both report source/rebuild core anchors matched. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Desktop center-band delta is `+0.0051`; mobile center-band delta is `+0.0291`, recorded only as regression evidence. Phase 1 remains open for source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `x1/M1/N1/ag` GLSL Source Surface | Aligned a source GLSL-surface batch without visual tuning: thumb composite now places `blendMultiply`/`saturation` helpers before tonemapping pars like source `_1/x1`; thumb plane now keeps the source `coverTexture(...)` before uniforms and `transition (...)` surface; displacement composite now follows source `F1/N1` helper, uniform, vignette-global, comment, and literal surfaces; main-fluid shaders now carry source `#define GLSLIFY 1`, source `spot_new` / `vel_old` / `vel_2` naming, and source `GT` advection `px` uniform ownership. Shader dump now records `mainFluidCoreChecks` and `displacementCoreChecks`; the residual summary reports `N1`, `x1`, `M1`, and all `ag-*` helpers with source/rebuild anchors matched. Output probe asserts advection material `px` and `bounds` uniform keys. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Desktop center-band delta is `+0.0042`; mobile center-band delta is `+0.0302`, recorded only as regression evidence. Phase 1 remains open for source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `tA/lg/cg` Shared Dither and Bloom Define Surface | Aligned a source shader-surface batch without visual tuning: the shared dither helper now matches source `tA/lg`, including `random(vec2 co)` with local `a/b/c/dt/sn` variables and `sn = mod(dt, 3.14)`, and both work/main bloom composite materials now match source `cg` by keeping `defines:{ NUM_MIPS:5, DITHERING: undefined }`. Output probes assert the source `DITHERING` key-present/undefined semantics through `ditheringDefinePresent=true`, JSON value `null`, string value `"undefined"`, and `ditheringDefineMode=source-cg-defines-DITHERING-undefined`; shader dump and renderer audit now record the source `tA`, `lg`, `s1`, `nA`, and `cg` anchors. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Desktop center-band delta is `+0.0049`; mobile center-band delta is `+0.0305`, recorded only as regression evidence. Phase 1 remains open for source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `I1/C1` Main Optional Pass Input and Order | Aligned the source main render-manager chain without visual tuning: main blur now reads source `I1.renderTargetA` (`mainRawTarget`), lensflare/luminosity/bloom run before `C1/A1`, bloom uses the source raw/bright target ownership instead of `preCompositeWorkTarget` or the post-`C1` composite target, `C1/A1.tScene` reads blur/raw main source, and the final screen path keeps `C1/A1` as the production material with only the source FXAA tail behind the source flag. Removed the rebuild-only pre-`C1` main bloom from the work target and post-`C1` bloom/lensflare scheduling from `compositeTarget`. Output probes now assert `renderManagerPassInputs`, `noPostC1Bloom=true`, `defaultScreenMaterialMode=source-I1-default-direct-C1-screen-render-fxaa-tail-only`, and expanded main update order `raw -> optional blur -> optional lensflare -> optional luminosity -> optional bloom -> fluid -> C1`; renderer audit records the source/rebuild pass-input anchors. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project media retained five visible tracks on both `gc-2026` and `hashgraph-vc`. Desktop center-band delta is `+0.0034`; mobile center-band delta is `+0.0298`, recorded only as regression evidence. Phase 1 remains open for source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved transfer/target interpretation. |
| `current batch` | Phase 1 / `GA.update` Per-Item Local Simulation Writeback | Aligned source `GA.update()` sequencing without visual tuning: each visible work item now performs the source per-item chain in one update path, writing `VA` time/coords, running local `Ka`, rebinding local `tMouseSim`, damping/writing `uMouseSpeed`, updating mouse-plane `uTime`, rebinding displacement, and only then applying source `p1.update` side reveal plus screen `tMouseSim2`. Removed the unused split local mouse update/sync helpers so the rebuild no longer has a separate global local-mouse writeback pass. Output probes now assert `sourceGAUpdateMode=source-GA-update-material-then-local-Ka-then-bindings-before-p1-side-reveal`, `uCoordsMatchesWorkTarget`, `mousePlaneTimeMatchesMaterialTime`, `tDisplacementIsWavves`, and `uMouseSpeedMatchesLocal`; renderer audit records source `GA.update` anchors, rebuild checks, and `rebuildNoSplitLocalMouseUpdate=true`. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop/mobile probes report no failures/exceptions and the source `GA.update` mode. Desktop center-band delta is `+0.0030`; mobile center-band delta is `+0.0296`, recorded only as regression evidence. Phase 1 remains open for remaining source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `Pe/Ka/GA` Mousemove Event Timing and Normalization | Aligned the source mouse-input chain without visual tuning: rebuild now listens to `window` `mousemove` instead of pointer down/move/up, derives pointer/raycast/screen mouse coordinates from the `.gl` root size like source `Pe.w/Pe.h`, calls per-item raycast during the mouse event like source `Ka.onMouseMove()`, and removes the rebuild-only RAF-tail pointer projection. Output probes now assert `mouseEventMode=source-Pe-window-mousemove-only`, `mouseNormalizationMode=source-Pe-gl-root-width-height`, `raycastMode=source-Ka-onMouseMove-per-item-raycast-immediate-pointer`, `raycastEventMode=source-Ka-raycast-during-mousemove-not-raf-tail`, and `raycastNormalizationMode=source-Pe-width-height`; renderer audit records source `Pe`/`Ka` anchors and `rebuildNoRafRaycast=true`. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop/mobile probes report no failures/exceptions and the source mouse/raycast modes. Project pages retained visible media. Desktop center-band delta is `+0.0036`; mobile center-band delta is `+0.0299`, recorded only as regression evidence. Phase 1 remains open for remaining source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `GA/Ka` Per-Item Raycast Ownership | Aligned source `GA/Ka` pointer ownership without visual tuning: the rebuild now keeps immediate pointer NDC state separately from the smoothed camera pointer, runs `raycaster.setFromCamera(pointerRay, homeCamera)`, and raycasts every visible work item's own `rayPlane` independently instead of selecting one global hit. This matches source `GA` constructing one `Ka` per block and source `Ka.raycast()` intersecting only that instance's `rayCastMesh` before writing that instance's UV target. Output probes now assert `raycastMode=source-Ka-per-item-raycast-immediate-pointer`, pointer-vs-smoothed state, visible work item count, independent visible targets, and per-item ray-plane state; renderer audit records source `Ka` anchors and rebuild raycast checks. `git diff --check`, `npm run build`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed before this documentation update. Desktop center-band delta is `+0.0044`; mobile center-band delta is `+0.0299`, recorded only as regression evidence. Phase 1 remains open for remaining source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `nD/Iu/Lu/I1/Lo` Update and Render-Manager Sequencing | Aligned the source frame/update order without visual tuning: rebuild now follows source `nD` scene order `sky -> media -> work -> main -> workthumb -> wavves -> character`, moves screen `Ka` mouse simulation into the source `Lu` slot after work raw/bloom and before `OA` composite, runs `p1` camera/components after work render and before main, renders media before work/main, updates main fluid after optional main bloom and before `C1`, and moves sky/wavves `uTime` writes after their `Lo` render-manager pass. Output probes now strictly assert `rebuildSceneOrder`, `rebuildFrameOrder`, `workUpdateOrder`, `mainUpdateOrder`, and `mouseSimulationOrder`; renderer audit records source and rebuild order anchors. `npm run build`, `git diff --check`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop/mobile output probes report the source-shaped order boards, `targetXY=[1,.5]`, and no failures/exceptions. Thumb spotlight retained map ownership; project media retained five visible tracks. Desktop center-band delta is `+0.0046`; mobile center-band delta is `+0.0302`, so Phase 1 remains open for source-backed `VA/GA` projection/material evidence, floor/environment distribution, and unresolved `OA/CA/I1` transfer or target interpretation. |
| `current batch` | Phase 1 / `nD/Xt` Texture Preload and Canvas Animate-In Lifecycle | Aligned the source `nD.animateIn()` resource gate without using visual tuning: `WebGLBackdrop` now tracks the four source-preloaded textures (`blueNoise`, `floorNormal`, `perlin1`, `perlin2`) through promises and exposes `animateIn()` that waits for them before scheduling the 0.5s canvas fade. Home page entry calls that canvas animate-in separately from the gallery entry so source `yD/SD` camera settings and `rd:home-gallery-in` timing are not delayed. Output probes now assert `animateInMode=source-nD-animateIn-awaits-init-and-four-preloaded-textures`, `animateInResolvedMode=source-nD-animateIn-resolves-after-fade-scheduled`, all four texture preload states, and resource-gate completion. Renderer audit records both source and rebuild anchors. `npm run build`, `git diff --check`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop/mobile probes report all four preload states true, `targetXY=[1,.5]`, `rotateAngle=20`, and no failures/exceptions. Project media retained visible tracks. Desktop center-band delta is `+0.0045`; mobile center-band delta is `+0.0285`, so Phase 1 remains open for render-manager graph parity, floor/environment distribution, strict `VA/GA` projection/material feel, and `OA/CA/Lu` transfer evidence. |
| `current batch` | Phase 1 / `IT/p1` Camera Controller Guardrails | Added source-backed camera-chain guardrails without changing production rendering: output probes now expose/assert source `IT` ownership for centered initial mouse state, `group -> rotateGroup -> innerGroup` manual matrices, entry `targetXY=[1,.5]`, `rotateAngle=20`, source `Math.min(Fn(2/(fps/60),0),2)*.01` lerp mode, y-driven z coupling, pointer deltas, and controller matrix decomposition. Renderer audit now records the source `IT` anchors next to existing `p1.resize`, `p1.setCameraControllerSettings`, `Iu.update`, and `yD.animateIn` evidence. `npm run build`, `git diff --check`, renderer audit, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop/mobile probes report `targetXY=[1,.5]`, `rotateAngle=20`, `lastLerp=.02`, and no failures/exceptions. Project media retained visible tracks. Desktop center-band delta is `+0.0041`; mobile center-band delta is `+0.0286`, so Phase 1 remains open for reflected scene content/camera visibility, mobile fog-bed distribution, strict `VA/GA` projection/material feel, and `OA/CA/Lu` transfer evidence. |
| `current batch` | Phase 1 / `sA/Ka` Mouse Simulation Target State and Update Semantics | Aligned a focused `sA/Ka` source batch without tuning visual constants: mouse simulation ping-pong targets now use source clamp wrapping, linear filters, `RGBAFormat`, `FloatType`, no stencil/depth, and no mipmaps; `mouseSimulationFragment` now preserves source `gl_FragColor = vec4(col)` instead of forcing alpha to `1.0`; and the brush update path now matches source `newPos.lerp(targetPos, delta * 7.5)` without a rebuild-side clamp. Runtime probes expose/assert target state for screen and active local simulations and record `updateLerpMode=source-Ka-newPos-lerp-targetPos-delta-times-7_5-no-clamp`. `npm run build`, `git diff --check`, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, renderer audit, full capture, and band analysis passed. Desktop/mobile probes report active local target type `FloatType(1015)`, format `RGBAFormat(1023)`, target/`uCoords` `45.5x29.9`, and no failures/exceptions. Desktop center-band delta is `+0.0048`; mobile center-band delta is `+0.0288`, so Phase 1 remains open for remaining mouse coordinate/timing evidence, strict `VA/GA` projection/material feel, reflected scene content/camera visibility, and mobile fog-bed distribution. |
| `current batch` | Phase 1 / `GA/Ka` Local Mouse Simulation Target Sizing | Aligned a source-backed local mouse simulation sizing mismatch: source `GA.resize()` passes the work plane scale directly into `Ka.onResize()`, so the local `GA` mouse simulation target and `uCoords` now use raw `45.5x29.9` values instead of the rebuild-only rounded `46x30`. Output probes now expose/assert `targetSizingMode=source-GA-resize-plane-scale-no-pre-rounding`. `npm run build`, `git diff --check`, desktop output probe, mobile output probe, shader dump, thumb spotlight probe, project-media probe, renderer audit, full capture, and band analysis passed. Desktop and mobile probes report active local mouse simulation target/`uCoords` as `45.5x29.900000000000002`; project pages retained visible media targets. Desktop center-band delta is `+0.0052`; mobile center-band delta is `+0.0290`, so Phase 1 remains open for local mouse coordinate/update semantics, strict `VA/GA` projection/material feel, reflected scene content/camera visibility, and mobile fog-bed distribution. |
| `current batch` | Phase 1 / `Ka/sA/GA` Mouse Simulation Uniform Surface Guardrails | Added source-backed runtime guardrails for the mouse simulation uniform surface without changing production rendering: output probes now expose/assert source `Ka.simulationMaterial` uniform ownership for both screen and active local `GA` paths, including blue-noise `uNoiseTexture` binding, `diffusion=0`, `diffusionSize=0`, and white `uColor`. This keeps future interaction/fluid work aligned with source `Ka/sA` instead of tuning mouse trails blindly. `npm run build`, `git diff --check`, shader dump, desktop output probe, mobile output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0046`; mobile center-band delta is `+0.0291`, so Phase 1 remains open for source-backed parity work in `VA/GA` projection/material feel, thumb-map transfer/content, floor/environment distribution, or `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `I1` Default Screen-Output Guardrail | Added a no-visual-change source guardrail for the main render-manager output path: runtime output now records/asserts that the default main path renders source `C1/A1` directly to screen (`source-I1-default-direct-C1-screen-render`) while the rebuild's populated `compositeTarget` is a QA/probe mirror of source `renderTargetComposite`, not the default screen output. This prevents future work from mistaking the probe target for a required extra production pass. `npm run build`, `git diff --check`, renderer audit, shader dump, desktop output probe, mobile output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0055`; mobile center-band delta is `+0.0289`, so Phase 1 remains open for source-backed `I1/OA/kA` transfer evidence, mobile background/floor distribution, and strict `VA/GA` projection/material feel. |
| `current batch` | Phase 1 / `U1/I1` Main Raw Scene Background Ownership | Completed the next same-chain source render-manager alignment after S1-179: `mainRawTarget` now renders a dedicated source `U1` main scene with `#D9D9D9.convertLinearToSRGB()` background instead of the rebuild-only procedural background shader. This keeps `C1/A1.tScene` on source `I1.renderTargetA` content rather than a local bridge scene. Output probe now exposes/asserts `mainRawSceneMode=source-U1-empty-main-scene-background-D9D9D9-linear-to-srgb` and the converted background RGB values. `npm run build`, `git diff --check`, shader dump, desktop output probe, mobile output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0045`; mobile center-band delta is `+0.0293`, so Phase 1 remains open for remaining `I1/OA/kA` render-order and transfer evidence, mobile background/floor distribution, and strict `VA/GA` projection/material feel. |
| `current batch` | Phase 1 / `I1/C1` Main Raw/Composite Target Split | Aligned a high-risk source render-manager graph detail without tuning visual constants: main rendering now uses a separate `mainRawTarget` for source `I1.renderTargetA`, and `C1/A1.tScene` reads from that raw target while `compositeTarget` remains the distinct pre-composite output target. This removes the previous source-incorrect self-binding shape where `compositeTarget` was both `tScene` input and pre-composite output. Output probes now expose/assert `tSceneSourceMode=source-I1-renderTargetA-raw-main-scene`, `tSceneIsMainRawTarget=true`, `tSceneIsCompositeTarget=false`, and both `mainRaw` / `preComposite` target probes. `npm run build`, `git diff --check`, shader dump, desktop output probe, mobile output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0044`; mobile center-band delta is `+0.0282`, so Phase 1 remains open for remaining `I1/OA/kA` render-order and transfer evidence, real mobile background/floor distribution, and strict `VA/GA` projection/material feel. |
| `current batch` | Phase 1 / `a1/o1` Floor Dither Helper Surface | Restored an inert source floor shader surface without changing active formulas or constants: source `s1` injects `${lg}` before the floor body, so `o1-floor-material` now includes the same `random(vec2 co)` and `dither(vec3 color)` helper surface even though the source `DITHERING` branch is inactive for `a1`. The helper is shared with the bloom composite helper string, and shader dump now hard-checks `sourceDitherRandomHelper` and `sourceDitherHelper` with source/rebuild true. `npm run build`, `git diff --check`, renderer audit, desktop output probe with isolated CDP port/wait, mobile output probe with isolated CDP port/wait, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0044`; mobile center-band delta is `+0.0281`, so Phase 1 remains open for real mobile background/floor distribution, remaining environment/sky target attribution, `VA/GA` bridge depth, and source-backed `OA/CA/Lu` graph evidence. |
| `current batch` | Phase 1 / `T1/w1/E1` Thumb Hierarchy and ScrollWrap Ownership | Restored the source thumb scene hierarchy without changing shader formulas, target sizes, or visual constants: `T1` now owns a thumb group with a child `scrollWrap`, and every `E1` thumb plane is added to that scroll wrapper instead of directly under the scene group. Thumb probe now exposes/asserts `thumbHierarchyMode=source-T1-w1-scrollWrap-E1-mesh`, `thumbWrapParentIsScene=true`, and `thumbScrollWrapParentIsThumbWrap=true`. `npm run build`, `git diff --check`, renderer audit, desktop output probe, mobile output probe after one transient retry, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. `x1-thumb-composite` remains source anchors matched with fragment delta `-5`. Desktop center-band delta is `+0.0051`; mobile center-band delta is `+0.0275`, so Phase 1 remains open for actual thumb-map transfer/content feel, mobile background/floor distribution, remaining `VA/GA` bridge depth, and source-backed `OA/CA/Lu` graph evidence. |
| `current batch` | Phase 1 / `T1/x1` Thumb Composite Helper Surface | Aligned source `_1/x1/v1` helper ownership without changing formulas or visual constants: `thumbCompositeFragment` now injects the source-shaped `${hg}` multiply helper surface and shared `${Ur}` saturation helper instead of local simplified helper bodies, and `A1/CA/x1` now share the same source saturation helper string. Shader dump now hard-checks the source thumb multiply/saturation helpers and absence of the rebuild-only local gray/inline multiply bridges. `x1-thumb-composite` fragment delta is now `-5`. `npm run build`, `git diff --check`, renderer audit, desktop output probe, mobile output probe after one transient retry, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0028`; mobile center-band delta is `+0.0293`, so Phase 1 remains open for actual thumb-map transfer/content feel, mobile background/floor distribution, remaining `VA/GA` bridge depth, and source-backed `OA/CA/Lu` graph evidence. |
| `current batch` | Phase 1 / `VA/HA` Ordinary Work Vertex Begin Surface | Aligned a source `HA` vertex-surface residual without changing production formulas: ordinary `VA-work` now preserves source `#include <begin_vertex>` before the custom work-cube transform instead of expanding it into a rebuild-local `vec3 transformed = vec3(position);`, and the work vertex declaration order is closer to source while keeping the existing `uUvOffset` vec2 bridge. Shader dump now checks `sourceBeginVertexInclude`; generated `VA-work` vertex include residuals are empty, with remaining differences classified as ordering/comments and the r164 bridge. `npm run build`, `git diff --check`, renderer audit, desktop/mobile output probes, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0056`; mobile center-band delta is `+0.0283`, so Phase 1 remains open for remaining `VA/zA` bridge depth, thumb-map transfer/content feel, and mobile background/floor distribution. |
| `current batch` | Phase 1 / `p1/h1/u1` Environment Constructor and Light Ownership Guardrails | Added source-backed negative-evidence guardrails without changing production rendering: output probe now exposes/asserts that source `h1` passes only `side`, `envMapIntensity`, and `fog:false` into `u1`, so the nearby `Qn` roughness/metalness/emissive constants remain unused by the environment material; runtime defaults are `roughness=1`, `metalness=0`, `emissiveIntensity=1`. It also exposes/asserts source `p1.setLights()` ownership: ambient, spotlight, spotlight target, and first directional light are in the scene, while `directionalLight2` is created/positioned but not added. Renderer audit records the same source anchors. `npm run build`, `git diff --check`, renderer audit, desktop/mobile output probes, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0042`; mobile center-band delta is `+0.0295`, so Phase 1 remains open for mobile background/floor distribution, thumb-map transfer/content feel, and remaining `VA/GA` bridge depth. |
| `current batch` | Phase 1 / `p1` Mobile Spotlight Guardrail and Mobile Attribution Probes | Corrected the mobile output-probe guardrail to match source `p1.update()`: below `MD=800`, spotlight y is `.3 + camera.position.y * .175`, while desktop remains `camera.position.y * .175`. Production rendering is unchanged. `compare-home-brightness-attribution.mjs` and `compare-composite-stages.mjs` now accept `VIEWPORT=mobile`, allowing direct mobile residual attribution. `VIEWPORT=mobile` output probe passed; mobile brightness attribution shows default `workRaw~0.346`, `workComposite~0.254`, `preComposite~0.296`, with environment/floor-reflection debug switches high-impact but diagnostic only. Mobile composite stage debug did not produce a new source-backed A1/OA production fix, so Phase 1 remains open for source-backed mobile floor/environment distribution, mobile scene/camera geometry, thumb-map transfer/content feel, and remaining `VA/GA` bridge depth. |
| `current batch` | Phase 1 / `A1/CA` Shared Color Helper Surface and A1 Vignette | Added the source shared color helper surface used by source `A1`/`CA` (`saturation`, `vignette`, `circle`, `contrast`, `hue`, `rgbshift`) alongside the existing full blend dispatcher, removed local duplicate helper definitions, and corrected A1's vignette helper back to the source non-ratio `distance(coords, .5)` formula instead of the rebuild `p.x *= uRatio` bridge. Shader dump now hard-checks all six helper surfaces and `noA1RatioVignetteBridge`; output probe exposes the same runtime guardrails. `OA-work-composite` fragment text delta narrowed from roughly `-489` to `-1`, and `A1-pre-composite` from roughly `-563` to `-175`. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta improved to `+0.0034`; mobile center-band delta remains `+0.0284`, so Phase 1 remains open for mobile/background distribution, floor/environment residuals, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `A1/CA/OA` Composite Blend Helper Surface | Replaced the reduced local blend helpers in `homePreCompositeFragment` and `homeCompositeFragment` with the shared source-shaped full `sourceBlendHelper`, matching source `A1` and `CA` ownership through `${Po}` while preserving formulas and visual constants. Shader dump now hard-checks full dispatcher mode `25` plus `blendAdd`, `blendMultiply`, and `blendLighten` surfaces for both `A1-pre-composite` and `OA-work-composite`; the latest dump reports those checks true for source and rebuild. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0205`; mobile center-band delta is `+0.0243`, so Phase 1 remains open for floor/environment distribution, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `V1/z1/h1/u1/i1` Roughness Helper and Reflector Target State | Added source-surface and target-state guardrails without changing production formulas: sky/environment shared shader helpers now include source `randomF(...)` and inert `customRoughness(...)`, and shader dump now asserts those anchors for both `z1-sky-composite` and `u1-environment`. Runtime output now exposes/asserts source floor-reflector raw target construction depth `false` and runtime depth `true`, matching source `i1` construction/cloning followed by `renderTarget.depthBuffer=!0`; renderer audit now records those source anchors. `npm run build`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, band analysis, and `git diff --check` passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0217`; mobile center-band delta is `+0.0239`, so Phase 1 remains open for actual environment/floor distribution, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `nD/a1/Xt` Scene Order and Floor Normal Guardrails | Added source-backed runtime/audit guardrails without changing production rendering: output probe now exposes/asserts source `nD` scene order (`sky`, `media`, `work`, `main`, `workthumb`, `wavves`, `character`), current rebuild frame order, source texture preload gate (`blueNoise`, `floorNormal`, `perlin1`, `perlin2`), and source floor normal binding state (`Xt.floorNormal`, repeat `[45,45]`, transform matrix, empty color space). Renderer audit now checks all source `nD.addScene(...)` anchors, the four-texture preload gate, and `a1` floor normal repeat setup. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0216`; mobile center-band delta is `+0.0239`, so Phase 1 remains open for actual environment/floor distribution, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `nD/V1/h1/u1` Sky Environment Binding Guardrails | Added same-chain source guardrails without changing production rendering: runtime output now exposes/asserts source `V1` sky background `#666666` linear-to-sRGB mode, sky raw/composite `height*.75` sizing, source `nD` post-resize-delay composite binding mode, environment `tSky` identity against the sky composite target, and repeat wrapping on that bound texture. Renderer audit now checks the source `V1` background anchors and the `nD` `pe.emit(RESIZE), await fn(100)` before sky composite repeat/bind chain. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0216`; mobile center-band delta is `+0.0241`, so Phase 1 remains open for actual environment/floor distribution, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / Batch Cadence and Floor Reflection Probe Hygiene | Updated the working cadence to use coherent source-backed batches with an upper bound of ten small differences instead of one-line commit cycles. Production rendering is unchanged. The floor-reflection output marker now names the source exception explicitly as `source-i1-conditional-clear-when-autoClear-false`, matching source `i1.update()` where the raw reflection target conditionally calls `renderer.clear()` only when `autoClear === false`; `probe-output-color.mjs` asserts the new marker to avoid future no-clear misattribution. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0222`; mobile center-band delta is `+0.0250`, so Phase 1 remains open for floor/environment distribution, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / Shader Residual Source Template Expansion | Improved the Phase 1 shader-dump evidence path without changing production rendering: `dump-va-shader.mjs` now recursively expands mirrored source shader template placeholders such as `${Ur}`, `${Po}`, `${xg}`, `${vg}`, `${Fr}`, `${dg}`, `${ug}`, `${pg}`, and `${_g}` before writing/comparing source shader files. Composite-core checks are now limited to actual composite shaders (`A1`, `OA`, `Lu-main`, `x1`) so `j1-media-composite` is not misreported by a generic `saturationTail` check. Expanded shader dump now confirms the S1-165 `u1-environment` full blend/oil/simplex helper surfaces are source and rebuild true, and removes the `j1-media-composite` false open residual. `npm run build`, `git diff --check`, expanded shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0211`; mobile center-band delta is `+0.0235`, so Phase 1 remains open for actual floor/environment distribution, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `V1/z1/h1/u1` Sky/Environment Helper Surfaces | Batched six same-chain source helper-surface alignments without tuning visual constants: added shared source-shaped contrast, simplex noise, procedural noise shader, full environment blend dispatcher, limited sky blend helpers, and oil helper surfaces, then injected them into the `u1-environment` and `z1-sky-composite` fragments according to source `l1` and `B1` ownership. Shader dump now hard-checks environment full blend/simplex/oil surfaces and sky blend/simplex/oil surfaces. `npm run build`, `git diff --check`, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0219`; mobile center-band delta is `+0.0249`, so Phase 1 remains open for actual floor/environment distribution residuals, thumb-map transfer/content feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `8446735` | Phase 1 / `SD/p1/SpotLight` Spotlight Projection Guardrails | Added a same-chain source-backed guardrail batch without tuning visual constants: output probe now exposes/asserts the source `SpotLight.map` projection path through Three r164 `vSpotLightCoord`, `spotLightMatrix`, and `lights_fragment_begin`, plus source spotlight intensity `220`, angle `PI/4`, penumbra `.95`, position `(0,0,3.7)`, target `(0,0,-8)`, a 16-value shadow matrix, and the existing 3x3 in-map coverage/luma. Renderer audit now records `SD.init()` map binding, `p1.setLights()` light constants, and the local Three r164 fragment/vertex chunk anchors. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0207`; mobile center-band delta is `+0.0230`, so Phase 1 remains open for actual thumb-map transfer/content feel, remaining `VA/GA` bridge depth, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `cd59c21` | Phase 1 / `p1/h1/u1/a1` Environment/Floor Production Guardrails | Added a low-risk source-guardrail batch without tuning visual constants: output probe now exposes/asserts production debug cleanliness for floor/environment branches, source `h1` material-update ownership, `h1.speed=5e-5`, and `p1` demorgen-only initial environment rotation ownership. Renderer audit now checks the source `h1.update(e,t,n){this.material.update(e,t,n)}` anchor. `npm run build`, `git diff --check`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, home capture, and band analysis passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0207`; mobile center-band delta is `+0.0228`, so Phase 1 remains open for actual floor/environment distribution, spotlight/thumb projection feel, remaining `VA/GA` bridge depth, and source-backed `OA/CA` transfer evidence. |
| `24b7f90` | Phase 1 / `VA/HA` Work Vertex Variable-Step Surface | Aligned another same-chain `HA` shader-surface batch without tuning visual constants: ordinary `VA-work` vertex code now mirrors source local step names and ordering for `newOffset`, two-step `mouseUv`, `perlinUv/perlin`, `revealCombined`, `perlinDisplacementHeight`, `perlinScaleDisplacement`, source `fadeDiplacement`, `mouseTransform`, `displacementF`, `waveDisplacement`, and `spread / 2.0`. The source `screenUv` replacement was updated for the expanded `newOffset` path, and shader dump now hard-checks these anchors while still requiring the S1-161 manual projection/r164-chunk guardrails. `npm run build`, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, home capture, band analysis, and `git diff --check` passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0198`; mobile center-band delta is `+0.0249`, so Phase 1 remains open for remaining `VA/zA` bridge depth, strict spotlight/thumb projection feel, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `56e7a41` | Phase 1 / `VA/HA` Work Vertex Shader Surface | Aligned the ordinary work-cube vertex bridge with source `HA` without tuning visual constants: `VA-work` now strips source-absent Three r164 vertex pars/runtime chunks for batching, morph targets, skinning, displacement maps, logdepthbuf, clipping planes, and replaces `project_vertex` with the source manual `mvPosition -> modelViewMatrix -> gl_Position` projection block. Auxiliary blocks are intentionally unchanged because this evidence is for ordinary source `VA`. Shader dump now hard-checks absence of those r164 vertex includes and presence of the manual projection anchors. `npm run build`, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, home capture, band analysis, and `git diff --check` passed. Project pages retained visible media tracks. `VA-work` vertex `includesOnlyRebuild` is empty; desktop center-band delta is `+0.0212`, mobile center-band delta is `+0.0255`, so Phase 1 remains open for remaining `VA/zA` bridge depth, strict spotlight/thumb projection feel, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `cf05a45` | Phase 1 / `Iu/p1/GA/k1` Frame-Tail Update Order | Aligned the source frame-tail order without tuning visual constants: output probes now run after the source-shaped `p1` post-render update, and the displacement/wavves target renders after that update, matching the source scene order where `work` updates before `wavves`. Visible work blocks now report source screen/local mouse bindings at the probe point (`tMouseSim2IsScreen=true`, `tMouseSimIsLocal=true`), and the output probe hard-fails if those visible bindings drift. `npm run build`, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, home capture, band analysis, and `git diff --check` passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0213`; mobile center-band delta is `+0.0244`, so Phase 1 remains open for floor/environment distribution, strict `VA/GA` projection/material feel, thumb-map transfer/content, and source-backed `OA/CA` transfer evidence. |
| `c164e02` | Phase 1 / `nD/V1/p1` Sky Composite Wrapping | Aligned source sky/environment texture ownership without tuning visual constants: the sky composite render target texture now uses source `RepeatWrapping` before it is bound to the work environment material, matching `nD.init()` (`e.wrapS=e.wrapT=ci`; `tSky.value=e`). Output probe now asserts `wrapMode=source-nD-sky-composite-repeat-for-work-env` and `wrapS/wrapT=RepeatWrapping`; renderer audit records the source `nD` binding anchors. `npm run build`, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, home capture, band analysis, and `git diff --check` passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0220`; mobile center-band delta is `+0.0248`, so Phase 1 remains open for environment/floor distribution, strict `VA/GA` projection/material feel, thumb-map transfer/content, and source-backed `OA/CA` transfer evidence. |
| `44bf2e5` | Phase 1 / `a1/i1/t1` Floor Reflector Blur Loop | Aligned source floor-reflector pass ownership without tuning visual constants: reflection projection now carries source `clipBias=0` in the projection-row formula, the blur/no-blur paths write through the source write target before swapping read/write targets, and the floor reflection texture is updated after each source-style blur swap. Output probe now asserts `clipBias`, `source-i1-write-target-loop-swap`, and `source-i1-update-after-each-blur-swap`; renderer audit records source `i1.update()` blur-loop anchors. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, home capture, and band analysis passed. Project pages retained visible media tracks. Desktop center-band delta is `+0.0147`; mobile center-band delta is `+0.0207`, so Phase 1 remains open for environment/floor distribution, strict `VA/GA` projection/material feel, thumb-map transfer/content, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `p1/OA` Update Culling Guardrails | Added source-backed runtime guardrails without changing production rendering: output probe now exposes/asserts `p1.update()` world-position cull bounds (`x > 5.5 || x < -5.5 || z > 5`), home work item count, visibility agreement, and reveal range shape. It also asserts the default work composite remains `source-OA-raw-glsl3` with `debugShaderActive=false` and no debug uniforms in the production shader surface. Renderer audit now records the source `p1.update()` cull/update anchors. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, and project-media probe passed. Project pages retained visible media tracks. Phase 1 remains open for strict `VA/GA` projection/material feel, thumb-map transfer/content, floor/environment distribution, and deeper source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `GA/Ka` Interaction Plane Materials | Aligned `GA.createPlane()` interaction-plane ownership with the source: the work mouse plane now has its own source-shaped transparent/depthless `ShaderMaterial` with `uRatio=35/23`, remains `visible=true` while detached from the scene, and the ray plane now uses a transparent zero-opacity/depthless material instead of object hiding while staying inside `rotationWrap`. Runtime output probe now exposes/asserts mouse-plane and ray-plane material state, visibility, parentage, `uRatio`, and existing source scale/position checks. Renderer audit records the source `planeMaterial` and `rayPlaneMaterial` anchors. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, and project-media probe passed. Phase 1 remains open for strict `VA/GA` projection/material feel, thumb-map transfer/content, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `Ka/sA/GA` Mouse Simulation Clear Ownership | Aligned the source mouse-simulation ping-pong pass: local `updateMouseBrush()` no longer clears the output render target before rendering, matching source `sA.render()` which only sets the output target, renders the ortho simulation scene, and restores `null`. Runtime output probe now exposes/asserts `renderClearMode=source-sA-no-explicit-clear`; renderer audit records source `sA` anchors and `noExplicitClear=true`. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, and project-media probe passed. Project pages retained five visible media tracks. Phase 1 remains open for strict `VA/GA` projection/material feel, thumb-map transfer/content, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `GA/VA` Work UV Offset Type | Aligned ordinary work-block mouse UV offset ownership with source `VA/GA`: `uUvOffset` now uses a `Vector2`/`uniform vec2` path for the main work material, matching source `uUvOffset:new I(new Q)` and `GA.createPlane()` `.x/.y` writes plus `uUvOffsetScale=t`. The audit avoids confusing this with auxiliary shader surfaces that contain `uniform vec3 uUvOffset`. Mouse-simulation probe now exposes/asserts `uvOffsetType=Vector2`; shader dump records bundle-source evidence for the `VA/GA` Vector2 path and confirms the runtime work shader uses vec2 and avoids vec3. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, and project-media probe passed. Project pages retained five visible media tracks. Phase 1 remains open for strict `VA/GA` projection/material feel, thumb-map transfer/content, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `T1/x1/_1/Lo` Thumb Composite Guardrails | Added source-backed guardrails for the thumb composite chain without changing production rendering: thumb probe now exposes/asserts `_1/x1` Raw GLSL3 material state, `tScene` binding to the thumb target, source `Lo` depthless clone-target mode, target texture defaults, and existing `T1` square height sizing. Renderer audit now records source `x1/_1` material anchors and `T1` background/camera/render-manager/resize anchors. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0110`; mobile center-band delta is `+0.0182`, so Phase 1 remains open for actual thumb-map transfer/content interpretation, strict `VA/GA` projection/material feel, floor/environment distribution, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `Se/yD` Home Fallback State | Corrected source route-state ownership for active home projects: overview darken and saturation now use source home fallbacks `.1` and `1`, while home thumbnail darkness now only reads `thumbnail.darkness` and falls back to source `0` instead of leaking `darkenOverview` through `payload.darkness`. Project/detail fallback behavior stays separate. Output and thumb probes now expose/assert these source defaults; `hashgraph-vc` home thumb composite darkness now reports `0` instead of `0.2`. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `+0.0143`; mobile center-band delta is `+0.0182`, so Phase 1 remains open for floor/environment distribution, strict `VA/GA` projection/material feel, thumb-map transfer/content, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `V1/H1/z1` + `a1/i1/o1` Source Surface | Tightened the sky/floor source shader surface without tuning visual constants: `z1-sky-composite` now carries source `uv`, `noise`, `contrast(...)`, and `diffuseColor.rgb = diffuseColor.rgb * 2.0` anchors, while `o1-floor-material` now uses source `uv` naming for the normal-distorted reflection sample instead of rebuild-only `reflectUv`. Shader dump now reports `z1` core checks and stronger `o1` core checks for these anchors. `git diff --check`, build, renderer audit, shader dump, output probe, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0095`; mobile center-band delta is `-0.0212`, so Phase 1 remains open for mobile fog distribution, strict `VA/GA` projection/material feel, thumb-map transfer/content, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `T1/w1/E1/M1` Thumb Guardrails | Tightened the source thumb-chain without changing visual constants: `M1-thumb-plane` now preserves the source `S1/y1` helper surface with the `vec4 color = texture(tex, uv); return color;` temporary, shader dump has core checks for the `b1/S1` vertex/fragment anchors, and the thumb probe now asserts source `M1` material defaults, transition uniforms, `E1` plane scale `(2,2,2)`, map binding, map/resolution `(1,1)`, `w1` centered wrap/visibility, `T1` square height target sizing, and spotlight map ownership. `git diff --check`, build, thumb probe, shader dump, output probe, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0096`; mobile center-band delta is `-0.0204`, so Phase 1 remains open for thumb-map transfer/content, strict `VA/GA` projection/material feel, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `GA/VA` Fragment Tail Step Surface | Restored the source `zA` ordinary work-block fragment-tail surface while preserving output formulas: `newUv`/`newOffset` are now expanded through source x/y grid division and offset adds, mouse input uses source `vec4 mouseSim`, and the reveal/alpha tail carries the source vignette temporaries plus `v`/`v2` variables. Shader dump now hard-checks the step surface, mouse-lightness, mouse-alpha, and vignette anchors; `VA-work` reports source anchors match with no include/uniform residuals, apart from the tracked Three r164 `SPECULAR`/`USE_SPECULAR` compile bridge. `git diff --check`, build, renderer audit, output probe, shader dump, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0095`; mobile center-band delta is `-0.0201`, so Phase 1 remains open for strict `VA/GA` projection/material feel, thumb-map transfer/content, and source-backed `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `GA/VA` Inert Varying Surface | Restored another source `HA/zA` ordinary work-block shader surface without changing formulas: vertex/fragment declarations now include source `vInstanceIndex`, `vInstanceColor`, `vPosition`, and vertex `vNoise`, and the vertex tail assigns `vInstanceIndex = instanceIndex`, `vInstanceColor = instanceColor`, and `vPosition = position`. Shader dump now hard-checks those source anchors. `git diff --check`, build, renderer audit, output probe, shader dump, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0092`; mobile center-band delta is `-0.0175`, so this is source shader-surface parity rather than Phase 1 visual closeout. |
| `current batch` | Phase 1 / `Lu/I1` Render-Target State Attribution | Added source-backed runtime target-state guardrails without changing production rendering: output probe now records work `Lu` and main `I1` render-target state boards for raw/composite/bloom/blur/fxaa/lensflare roles, including depth/stencil, colorSpace, type, format, filters, mipmaps, and wrapping. `probe-output-color.mjs` now asserts source defaults: work `renderTargetA` has depth enabled after clone setup, work clones stay depthless, and main `I1` targets/clones stay depthless. `git diff --check`, build, renderer audit, output probe, shader dump, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0093`, mobile center-band delta is `-0.0188`, so this closes a target-state false target but Phase 1 remains open for `OA/CA` transfer interpretation, strict `VA/GA` projection/material feel, and floor/environment residuals. |
| `current batch` | Phase 1 / `GA/VA` Varying and Alpha Tail Surface | Restored source `HA/zA` shader-surface naming for ordinary work blocks without changing formulas: `vUv`, `vInstanceAlpha`, and `vec3 vOffset` now replace rebuild-only `vLocalUv`/`vAlpha` shapes, the vertex path assigns `vUv = uv`, fragment helpers are named `random(...)` and `vignette(...)`, and the ordinary alpha tail uses source `mixedAlpha = alpha * alpha2 * vInstanceAlpha` structure. Shader dump now hard-checks these source-surface anchors and the absence of rebuild-only names. `git diff --check`, build, output probe, shader dump, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0089`, mobile center-band delta is `-0.0192`, so Phase 1 remains open for remaining `VA/GA` bridge residuals, thumb/material interpretation, and `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `GA/VA` World Position Path | Restored the source `HA` work-block world-position path: the vertex shader now applies the mouseSim divide, creates `worldPosition`, multiplies by `instanceMatrix`, then `modelMatrix` unconditionally, without the rebuild-only Three r164 conditional wrapper or batching branch. Output probe now records/asserts `vertexWorldPositionMode=source-HA-unconditional-instance-world`, and shader dump checks the source `instanceMatrix` anchor plus absence of `batchingMatrix` in this path. `git diff --check`, build, output probe, shader dump, brightness attribution, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta is `-0.0089`, mobile center-band delta is `-0.0182`, so Phase 1 remains open for remaining `VA/GA` bridge residuals, thumb/material interpretation, and `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / `GA/VA` Material State Parity | Removed the rebuild-only `toneMapped:false` override from ordinary work-block and auxiliary block `MeshStandardMaterial` instances, matching source `VA` construction where the material keeps default tone-mapped state while the shader text comments out the tonemapping include. Output probe now records and asserts `source-VA-meshstandard-default-toneMapped` plus active/auxiliary material flags (`transparent`, depth, dithering, env map, roughness, metalness). `git diff --check`, build, output probe, shader dump, project-media probe, brightness attribution, full capture, and band analysis passed. Project pages retained five visible media tracks. `VA-work` shader residual narrowed with no live include/uniform deltas; desktop center-band delta is `-0.0086`, mobile center-band delta is `-0.0197`, so Phase 1 remains open for the remaining `VA/GA`, spotlight/material interpretation, and `OA/CA` transfer evidence. |
| `current batch` | Phase 1 / Spotlight Projection Coverage Probe | Hardened source `T1/w1/E1` spotlight/thumb projection attribution without changing production rendering: the output probe now samples a 3x3 grid across the active work-block bounds, records `source-thumb-composite-target` map ownership, UV bounds, sample count, in-map coverage, and thumb-map luma. `git diff --check`, build, output probe, project-media probe, and brightness-attribution matrix passed. Project pages retained five visible media tracks. The active block's nine projection samples all land inside the thumb composite map with non-empty luma, so the remaining spotlight/thumb residual should continue from thumb-map transfer/content or `VA` material interpretation rather than coverage guesswork. |
| `current batch` | Phase 1 / `OA/CA` Transfer Re-Audit | Re-ran the repaired composite-stage and brightness-attribution matrices without changing production rendering. Results confirm the open transfer blocker remains real: default `workComposite/preComposite` stayed around `0.234/0.306`, while query-only `debug-composite-transfer=1` raised them to roughly `0.407/0.690`; however `debug-renderer-output=linear` stayed near default (`0.235/0.308`), and source `qw` still sets `outputColorSpace=srgb` with `OA/CA` `toneMapped:false`. This batch records the evidence as attribution only. Do not promote gamma-like transfer, renderer-output-linear, darken-off, or spotlight-map-off as production fixes. Phase 1 remains open for source-backed target encoding evidence or narrower upstream `VA/GA` / spotlight-map material interpretation. |
| `current batch` | Phase 1 / `p1.resize` Mobile Probe Attribution | Expanded the output probe and renderer audit around source `p1.resize` without changing production rendering: probes now run desktop or mobile via `VIEWPORT`, expose/assert `MD=800`, `mobileResizeBranch`, desktop `cameraController.origin.z=5.5` / `sceneWrap.position.y=0`, and mobile `origin.z=5` / `sceneWrap.position.y=.3`. `git diff --check`, build, renderer audit, desktop output probe, mobile output probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0073`; mobile center-band delta is `-0.0194`, confirming the remaining mobile fog-bed/horizon residual is not from a missed `p1.resize` mobile branch. Phase 1 remains open for source-backed environment/floor residuals, `OA/CA` final transfer interpretation, and strict `VA/GA` projection/material feel. |
| `current batch` | Phase 1 / `Lu/I1` Source Screen-Pass Swap | Replaced the work/main post-pass bridge scenes with source-style fullscreen screen meshes and material swaps for work luminosity/bloom/composite, main luminosity/bloom/pre-composite/final composite, and FXAA routing. This preserves existing shader formulas, uniforms, targets, and pass order while matching the source `Lu/I1` ownership model. `git diff --check`, build, renderer audit, output probe, composite-stage comparison, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0075`; mobile center-band delta is `-0.0197`, confirming this is structural parity rather than the remaining brightness/fog-bed fix. Phase 1 remains open for source-backed final transfer interpretation, strict `VA/GA` projection/material feel, and mobile/fog-bed residuals. |
| `current batch` | Phase 1 / `Lu/I1` Screen-Pass Ownership Attribution | Added source-backed render-manager ownership attribution without changing production rendering: renderer audit now verifies source `Lu` and main `I1` use one fullscreen `screen` mesh with `this.screen.material` pass swaps, and output probe exposes the current rebuild bridge as dedicated pass scenes for work composite and main composite. Probe assertions now guard this as an explicit structural residual around the `OA/CA` / `A1/C1` transfer chain. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0070`; mobile center-band delta is `-0.0198`. Phase 1 remains open for source-backed final transfer interpretation, possible safe screen-pass consolidation, strict `VA/GA` projection/material feel, and mobile/fog-bed residuals. |
| `current batch` | Phase 1 / `OA/CA` Debug Attribution Surface | Repaired the query-only debug composite path after the production `OA/CA` Raw GLSL3 migration: debug composite now uses `RawShaderMaterial`, `GLSL3`, source fullscreen vertex, and `FragColor` early returns; the output probe still requires `source-OA-raw-glsl3` for production but accepts `debug-OA-raw-glsl3` only when a debug composite query is present. This is QA infrastructure only and does not change default production rendering or promote any transfer/debug branch. `git diff --check`, build, shader dump, output probe, composite-stage comparison, brightness-attribution matrix, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0076`; mobile center-band delta is `-0.0195`. Phase 1 remains open for source-backed `OA/CA` transfer interpretation, strict `VA/GA` projection/material feel, and mobile/fog-bed residuals. |
| `current batch` | Phase 1 / `VA/zA` Commented Include Surface | Restored source-style commented include surface in ordinary `VA-work` without changing formulas or constants: source-authored omitted chunks now appear as `// #include <...>` for the MeshStandard fragment path, including `tonemapping_fragment`, while r164-only bridge comments remain explicit. Shader residual reporting now includes commented include source/rebuild columns and shows the `VA-work` commented lists match with core anchors still matched. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0090`; mobile center-band delta is `-0.0186`. Phase 1 remains open for strict `GA/VA` bridge review, `OA/CA` transfer/source-output interpretation, and mobile/fog-bed residuals. |
| `current batch` | Phase 1 / `A1/C1` Pre-Composite Helper Surface | Restored source `A1` pre-composite helper surface without changing output formulas: added the inert `luminance`, `coverTexture`, and `random` helpers and renamed the local dispatcher/calls from rebuild-only `sourceBlend(...)` to source `blend(...)`. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0074`; mobile center-band delta is `-0.0195`. Phase 1 remains open for mobile/fog-bed residual, strict projection/material feel, and transfer/source-output interpretation. |
| `current batch` | Phase 1 / `VA/zA` Fragment Tail Surface | Restored two source-surface details in the ordinary work-block shader tail without tuning constants: the fragment tail now uses source `newUv` naming for the work-block coordinate path and includes the source inert `vec4 displacement = texture2D(tDisplacement, newUv)` sample before reveal/alpha math. Shader dump now asserts this `VA-work` displacement-sample anchor and reports source/rebuild core anchors matched with no shader/WebGL console errors. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0075`; mobile center-band delta is `-0.0189`. Phase 1 remains open for mobile/fog-bed residual, strict projection/material feel, and transfer/source-output interpretation. |
| `current batch` | Phase 1 / False-Target Audit After Sky Clamp | Added source audit anchors and documentation for five recently checked Phase 1 candidates without changing production rendering: source `p1.setCameraControllerSettings()` defaults remain `targetXY=(.25,.25), rotateAngle=10`; source home gallery `yD.animateIn()` explicitly overrides to `targetXY=(1,.5), rotateAngle=20`; source `Se.setAmbientColor()` uses `formatColor()/sr()` raw RGB and writes ambient plus environment darken color; source `C1.uBgColor` is `#1F1F1F.convertLinearToSRGB()`; and the local `backgroundMaterial/backgroundScene` has no current default production render call, so it is not a safe visual tuning target. Renderer audit now hard-records the source anchors. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0065`; mobile center-band delta is `-0.0186`. Phase 1 remains open for mobile/fog-bed residual, strict projection/material feel, and transfer/source-output interpretation. |
| `9579b4c` | Phase 1 / `Lo/H1/V1` Sky Composite Target Wrapping | Removed the rebuild-only `RepeatWrapping` override from `skyCompositeTarget` so the source `Lo` sky composite target uses the default cloned `WebGLRenderTarget` clamp wrapping. This matters because source `h1/u1` samples `tSky` with shifted/scaled UVs. Output probe now hard-checks `ClampToEdgeWrapping` for the sky composite target, and renderer audit records source `Lo` clone target defaults. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0073`; mobile center-band delta is `-0.0199`. Phase 1 remains open for mobile/fog-bed residual, strict projection/material feel, and transfer interpretation. |
| `ab61d39` | Phase 1 / `w1/T1` Thumb Strip QA Attribution | Hardened source `w1.updateGalleryProgress()` attribution without changing visual constants: thumb probe now labels and asserts the source centered x-wrap path (`x=(xHook+progress+totalWidth*67890)%totalWidth`, then `x>totalWidth/2 && x-=totalWidth`) and source visibility bounds `[-1.5,1.5]`; renderer audit now extracts source `w1` and checks those anchors. Static light-chunk comparison confirms the source bundle and local Three r164 `lights_fragment_begin` spotlight-map sampling path are identical, so this batch closes a false projection target rather than changing lighting. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0008`; mobile center-band delta is `-0.0163`. Phase 1 remains open for mobile/fog-bed distribution, strict projection/material feel, and transfer interpretation. |
| `current batch` | Phase 1 / `GA.createPlane()` Local Mouse Mesh Ownership | Aligned source `GA.createPlane()` / local `Ka` ownership without changing visual constants: each work item now keeps an explicit invisible `mousePlane` at source scale `35*1.3` by `23*1.3`, derives local mouse-simulation target sizing and `uCoords` from that plane scale like source `mouseSim.onResize(this.plane.scale.x,this.plane.scale.y)`, and keeps `rayPlane` parented under `rotationWrap` with source scale/z. Output probe now hard-checks mouse-plane/ray-plane scale, z, parentage, target sizing, UV offset/scale, persistence, and thickness. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0003`; mobile center-band delta is `-0.0159`. Phase 1 remains open for mobile/fog-bed distribution, strict projection/material feel, and transfer interpretation. |
| `cce8f44` | Phase 1 / `OA/CA` Composite Shader Surface | Pulled the work composite fragment surface closer to source `CA` without changing visual constants: the production shader now uses source-style `mixed`, source `blend(...)` entry calls for modes 15/11, source `luminance` and `vignette` helper surfaces, source vignette globals, and the inert `green`/`greenLight` locals. Output probe now hard-checks this `source-CA-mixed-blend-surface`. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0015`; mobile center-band delta is `-0.0153`. Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `72354ec` | Phase 1 / `cg/nA/iA` Bloom Composite Surface | Aligned source `cg/nA` bloom composite uniform and shader surface: work/main bloom composites now use source `tBlur1..tBlur5`, `uBloomFactors[NUM_MIPS]`, `NUM_MIPS=5`, `mediump` precision, source dither helper/branch surface, and no rebuild-only `tBloom*` or `uFactor*` runtime uniforms. Output probe now hard-checks the source uniform mode for both bloom composite materials. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0017`; mobile center-band delta is `-0.0159`. Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `c59c15e` | Phase 1 / `Lo/tl` Sky/Thumb Composite Surface | Aligned the source `Lo/tl` composite vertex surface for sky and thumb composites: `z1-sky-composite` and `x1-thumb-composite` now use the isolated source `tl` matrix fullscreen vertex/mesh path; `skyCompositeFragment` carries the missing `tonemapping_pars_fragment`; shader dump maps `x1-thumb-composite` to source `tl`; and output probe asserts `skyComposite.vertexMode=source-tl-matrix-fullscreen`. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0005`; mobile center-band delta is `-0.0163`. Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `0d82ed5` | Phase 1 / `N1/F1/tl` Displacement Cleanup | Finished the same-chain displacement residual cleanup: source `tScene` is now declared/bound, `F1` vignette constants are global source-style values, displacement uses an isolated source `tl` matrix fullscreen vertex path without changing other stable post passes, output probe asserts `vertexMode`, `tSceneBound`, and source vignette constant mode, and `phase1-shader-residuals.md` now includes `N1-displacement-composite`. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0014`; mobile center-band delta is `-0.0158`. Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `eca7681` | Phase 1 / `k1/O1/N1/F1` Displacement Pass | Aligned the source wavves/displacement render-manager pass: displacement composite now uses `RawShaderMaterial`/`GLSL3`, source fullscreen vertex surface, source-style `in/out` and `FragColor`, tonemapping include surface, `NoBlending`, `transparent:true`, `toneMapped:false`, source `Lo` no-explicit-clear ownership, and target-size/ratio probe assertions. Shader dump now maps `N1-displacement-composite` to source `F1/tl`. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0005`; mobile center-band delta is `-0.0161`. Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `261cc3f` | Phase 1 / `Se` Visual Defaults | Aligned source `xt` / `Se.init()` default visual state ownership: WebGL initial `darken`, `saturation`, and `contrast` now start from source `.2`, `.35`, and `1.1`; home fallback darken/saturation and thumb fallback darkness/color/saturation/mouse-lightness now use source defaults; project/detail fallback constants are named separately for source route semantics; and output/thumb probes now expose and assert those defaults. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0010`; mobile center-band delta is `-0.0136`. Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `1359de8` | Phase 1 Audit / `VA` r164 Bridge Attribution | Hardened `VA-work` shader residual attribution without changing rendering: shader dump now records `vaBridgeCompatibility`, exposes Three r164 `lights_physical_fragment` chunk checks, and `phase1-shader-residuals.md` includes a Bridge Notes column. Current dump classifies the remaining source `SPECULAR` versus rebuild `USE_SPECULAR` delta as an `r164 compile bridge`, because local Three r164 requires `USE_SPECULAR` in `lights_physical_fragment` and does not accept source `SPECULAR`. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0018`; mobile center-band delta is `-0.0139`. Phase 1 remains open; this closes a false implementation target, not a visual blocker. |
| `174141e` | Home WebGL / `VA` Include Surface | Closed the S1-120 `VA-work` include-surface residual without tuning visual constants: ordinary work-block `VA` now keeps source `zA`'s `#include <bsdfs>` surface and uses `#include <opaque_fragment>` before the existing source-style `gl_FragColor.rgb/a` tail mutations. The shader residual summary now generates the `VA-work` reading from current row data instead of hardcoding the older residual. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Shader dump reports no `VA-work` source/rebuild fragment include or uniform residuals; project pages retain five visible media tracks. Desktop center-band delta is `+0.0008`; mobile center-band delta is `-0.0125`. Phase 1 remains open for generated shader text/bridge depth, mobile/fog-bed distribution, strict projection/material feel, and transfer interpretation. |
| `1a5d251` | Phase 1 Shader Residual Audit / Main Fluid Force Surface | Expanded the batch cadence into a source-backed audit step plus one low-risk pass-surface fix: `ag-force` fragment now declares source `$T` uniforms `center`, `scale`, and `px` alongside `force`, without changing the force formula. Added `scripts/summarize-phase1-shader-gaps.mjs`, and `dump-va-shader.mjs` now automatically emits `phase1-shader-residuals.md/json` from every shader dump. The report classifies `OA/A1/thumb/environment/media/main-fluid` core anchors as matched and originally identified the now-closed `VA-work` source-only `bsdfs`/`opaque_fragment` include-surface residual. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retained five visible media tracks. Desktop center-band delta was `+0.0013`; mobile center-band delta was `-0.0137`. |
| `current batch` | Home WebGL / Main Fluid Advection Bounds | Aligned source `GT.createBounds()` by adding the missing `ag-advection-bounds` line pass to the main-fluid advection scene: source `VT/Sf` raw GLSL3 material, source boundary geometry coordinates, shared advection uniforms, `NoBlending`, and two-child advection scene ownership. Output probe now asserts the boundary material mode, GLSL version, shared uniforms, blending, and scene child count; shader dump maps `ag-advection-bounds` to source `VT/Sf`; renderer audit records the source boundary geometry and line-add anchors. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0013`; mobile center-band delta is `-0.0135`, so Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `7e25e89` | Home WebGL / Main Fluid Pass Surface | Aligned the source `ag/GT/qT/jT/KT/JT` main-fluid pass surface: advection, force, divergence, poisson, and pressure now use source-shaped `RawShaderMaterial`/`GLSL3` shaders with `in/out`, `texture(...)`, and `FragColor`; advection/divergence/poisson/pressure use source `NoBlending`, force keeps source `AdditiveBlending`; and main-fluid probes now assert raw GLSL3 material modes, depth flags, blending, and float/depthless FBO ownership. Shader dump now maps five `ag-*` passes to source `Co/XT/Sf/$T/WT/YT/ZT`, and renderer audit records source `ag` plus each pass class. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0013`; mobile center-band delta is `-0.0138`, so Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `b3ac7dd` | Home WebGL / I1 Lensflare Surface | Aligned the source default-disabled `I1/L1/R1/P1` lensflare branch surface: added a source-shaped `L1-lensflare` raw GLSL3 material/scene with `tMap`, `uLightPosition`, `uScale`, `uExposure`, `uClamp`, and `uResolution`; kept `SOURCE_MAIN_LENSFLARE_SETTINGS.enabled=false`; wired the pass behind the source gate with explicit-clear semantics; and kept `C1/A1.tLensflare` bound to the full-resolution lensflare target. Added renderer-audit, output-probe, and shader-dump checks for source `L1`, default uniforms, clear mode, and `P1` core anchors. `git diff --check`, build, renderer audit, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed; output probe passed with `PROBE_WAIT=9000` after the default wait exposed unrelated async texture-wrap timing. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0009`; mobile center-band delta is `-0.0133`, so Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `current batch` | Home WebGL / `Na` Standard Blur Surface | Aligned the source ordinary blur pass surface without changing default visual constants: horizontal/vertical render-manager blur now uses dedicated source `Na/HT/zT` raw GLSL3 material instances with `uBluriness` and the 9-tap `og` blur helper instead of reusing bloom `rg/kT` gaussian blur and kernel defines. Output probe hard-checks `source-Na-raw-glsl3`, `uBluriness`, no `KERNEL_RADIUS/SIGMA`, and source directions `[1,0]` / `[0,1]`; renderer audit records source `Na`; shader dump expands `${og}` into `zT` and reports source/rebuild true `standardBlurCoreChecks`. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0008`; mobile center-band delta is `-0.0120`, so Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `current batch` | Home WebGL / Core Composite Shader Surface | Aligned the default source `OA/C1` composite material surfaces: work `OA` and pre-composite `C1/A1` now use `RawShaderMaterial` with `GLSL3`, source-style `in/out`, `texture(...)`, and `FragColor`, while the query-only debug composite keeps the old `ShaderMaterial` bridge for diagnostics. Output probe now hard-fails if either material mode or GLSL version drifts, and renderer audit records source `OA`/`C1` `glslVersion:lt` anchors. `git diff --check`, build, renderer audit, restarted output probe, shader dump, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0009`; mobile center-band delta is `-0.0132`, so Phase 1 remains open for mobile/fog-bed distribution, projection/material parity, and transfer interpretation. |
| `current batch` | Home WebGL / Sky Composite Shader Surface | Aligned source `V1/H1/z1` sky composite material surface: sky composite now uses `RawShaderMaterial` with `GLSL3`, and its shader surface uses source-style `in/out`, `texture(...)`, and `FragColor` instead of the previous bridge `ShaderMaterial`/`varying`/`gl_FragColor`/`texture2D` form. Output probe now hard-fails on sky material mode or GLSL version drift, shader dump now includes `z1-sky-composite` against source `B1/tl`, and renderer audit asserts `class z1 extends mt` plus `glslVersion:lt`. `git diff --check`, build, renderer audit, restarted output probe, shader dump, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0004`; mobile center-band delta is `-0.0129`, so Phase 1 remains open for mobile/fog-bed distribution, projection/material parity, and transfer interpretation. |
| `current batch` | Home WebGL / Environment-Sky QA Surface | Hardened source `h1/V1/Du` environment geometry and sky target attribution without changing visual output: output probes now assert the source `Du` environment geometry shape (`IcosahedronGeometry`, radius `300`, detail `10`), sky composite sizing (`height*.75` square), and source low-res/live sky time mode. Renderer audit now extracts source `Du` and checks `h1.speed=5e-5`, `new Du(300,10)`, `class Du extends IcosahedronGeometry`, and stored radius/detail parameters. `git diff --check`, build, renderer audit, restarted output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0001`; mobile center-band delta is `-0.0116`, so Phase 1 remains open for mobile/fog-bed distribution, projection/material parity, and transfer interpretation. |
| `current batch` | Home WebGL / Floor Fragment Branch Surface | Restored source `o1` floor fragment branch surface without changing visual constants or enabling unsupported branches: the shader now carries `USE_MAP`, `USE_NORMALMAP`, `USE_FOG`, and `DITHERING` conditionals, while runtime probes assert the source `a1` construction state of `USE_NORMALMAP=true`, `USE_MAP=false`, `USE_FOG=false`, and `DITHERING=false`. Shader dump floor core checks now cover the dithering branch/call, and output probe hard-fails on floor branch drift in both `uniforms.floor` and `reflectionState.floor`. `git diff --check`, build, renderer audit, restarted output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0004`; mobile center-band delta is `-0.0122`, so Phase 1 remains open for mobile/fog-bed distribution, projection/material parity, and transfer interpretation. |
| `current batch` | Home WebGL / Floor Reflection Shader Surface | Aligned source `o1/t1` floor/reflection shader surface without tuning visual constants: floor material now reports `source-o1-raw-glsl3`, reflection blur uses `RawShaderMaterial` with `GLSL3`, and shader dump now includes both `o1-floor-material` and `t1-floor-reflection-blur` from source `r1/s1/e1/QA`. Renderer audit extracts source `o1` and `t1` anchors, and output probe hard-fails if the floor or blur material modes drift. `git diff --check`, build, renderer audit, restarted output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0003`; mobile center-band delta is `-0.0125`, so Phase 1 remains open for mobile/fog-bed distribution, projection/material parity, and transfer interpretation. |
| `current batch` | Home WebGL / `rg` Blur and `ig` FXAA Body Surface | Aligned the source helper shader-body surface for bloom blur and FXAA without tuning constants: work/main bloom chains now create five `rg` materials/scenes with compile-time `KERNEL_RADIUS/SIGMA` defines `[3,5,7,9,11]`, runtime `uKernelRadius/uSigma` writes are gone, and FXAA now uses source `FT` vertex neighbor UV varyings feeding source-shaped `UT` fragment logic. Output probe hard-checks material counts/defines/no-runtime-uniforms and `fxaa.vertexMode=source-FT-neighbor-uv`; shader dump reports source/rebuild true `rgBlurCoreChecks` and `igFxaaCoreChecks`. `git diff --check`, build, renderer audit, output probe, shader dump, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0016`; mobile center-band delta is `-0.0120`, so Phase 1 remains open for mobile/fog-bed distribution, strict `VA/GA` projection/material feel, and transfer interpretation. |
| `current batch` | Home WebGL / Environment Hierarchy | Aligned source `h1/p1` environment hierarchy ownership: source creates an `h1` group, attaches the environment mesh as a child, and applies `position.y=-12.65` plus `rotation.y=-rotationAdjustment` to the group. The rebuild now mirrors that with `environmentGroup`, leaving the mesh local at origin. Added renderer-audit anchors for source `h1` and `p1` environment hierarchy plus output-probe assertions for group-owned transform. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0005`; mobile center-band delta is `-0.0117`, so Phase 1 remains open for mobile/fog-bed distribution and projection/material parity. |
| `current batch` | Home WebGL / Texture Wrapping | Aligned source `Xt.preloadTextures()` texture wrapping ownership: source constants resolve to `ci=RepeatWrapping=1000` and `vo=MirroredRepeatWrapping=1002`, so blue-noise, floor-normal, and A1/C1 `perlin2` stay on repeat wrapping while work-block `perlin1` now uses mirrored repeat wrapping. The runtime `noiseTexture` pointer now updates to the loaded blue-noise texture instead of remaining on the placeholder. Added output-probe fields and hard assertions for all four texture wrapping modes, added renderer-audit anchors for source `Xt.preloadTextures()`, and corrected the older Phase 1 note that had incorrectly described `vo` as clamp wrapping. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0003`; mobile center-band delta is `-0.0128`, so Phase 1 remains open for mobile/fog-bed distribution and projection/material parity. |
| `current batch` | Home WebGL / Render Manager Clearing | Aligned source `Lu/I1` render-manager clearing ownership: source renderer `qw` runs with `autoClear=false`, and source `Lu.update()` / `I1.update()` render raw, blur, FXAA, and composite targets through direct `setRenderTarget(...); render(...)` calls without rebuild-only clears. Removed explicit clears from frame start, work raw/composite, hidden-home work fallback, main pre-composite, main blur, and main FXAA paths while leaving source exceptions intact: media scene still owns `autoClear=true`, and floor reflection still uses the source conditional raw clear. Added output-probe clear-mode markers and assertions plus renderer-audit anchors for source `qw`, `nD`, `Lu`, and `I1`. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0006`; mobile center-band delta is `-0.0125`, so Phase 1 remains open for mobile/fog-bed, projection/material feel, and deeper render-manager parity. |
| `current batch` | Home WebGL / GA Matrix Ownership | Aligned source `GA` rotation-wrap scale ownership: each work item now follows `group -> rotationWrap(scale=.09) -> mesh + rayPlane`, matching source `GA.createInstancedMesh()` and `GA.createPlane()` instead of pre-scaling the mesh and ray-plane geometry directly. Ray-plane geometry and local z are now unscaled source values under the scaled parent, and output probe asserts `rotationWrapScale`, mesh identity scale, ray-plane geometry/z, UV offset, local mouse target sizing, and existing `Ka` source shape. Renderer audit now checks source `GA` anchors for `rotationWrap.add(mesh)`, `rotationWrap.scale.set(i,i,i)`, and `rotationWrap.add(rayPlane)`. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0002`; mobile center-band delta is `-0.0129`, so Phase 1 remains open for mobile/fog-bed, projection/material feel, and deeper render-manager parity. |
| `current batch` | Home WebGL / Sky Environment Surface | Aligned source `V1/H1/z1` sky composite uniform surface: removed rebuild-only runtime bindings for `uShader1Mix3` and `uShader3Scale` while keeping the shader declarations, because source `z1` declares those names in `B1` but only binds `tScene`, `uTime`, `uShader1Alpha`, `uShader1Speed`, `uShader2Speed`, `uShader1Scale`, `uShader2Scale`, and `uShaderMix` at runtime. Added output-probe binding markers and hard assertions for `source-declared-only`, plus renderer-audit anchors for source `V1` target sizing/update timing and `z1` material ownership. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0004`; mobile center-band delta is `-0.0116`, so Phase 1 remains open for mobile/fog-bed, projection/material feel, and deeper render-manager parity. |
| `current batch` | Home WebGL / p1 Update Order | Aligned source `Iu/p1/h1` frame ordering: environment `uTime` now updates in the post-render next-frame component path instead of the pre-render tick path, matching source `Iu.update()` where the render-manager runs before camera/components and source `p1.update()` where work items/about blocks follow `super.update()`. Added renderer-audit anchors for `Iu.update()` and `p1.update()`, plus output-probe metadata and a hard assertion for `environmentUpdateOrder=source-p1-component-post-render`. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0006`; mobile center-band delta is `-0.0124`, so Phase 1 remains open for mobile/fog-bed, projection/material feel, and deeper render-manager parity. |
| `current batch` | Home WebGL / I1 Main Render Manager | Corrected main render-manager sizing back to source `I1`: main luminosity/bloom now start from `Fa(renderSize)/2`, and main fluid resizes from that half-POT size divided by `3`. Work `Lu/kA` remains on source quarter-POT sizing, so the two render managers are no longer collapsed into one sizing rule. Added output-probe assertions for the work `/4` versus main `/2` split and renderer-audit anchors for source `I1` half-POT sizing, lensflare target ownership, main-fluid resize, and `tLensflare` wiring. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, shader dump, home source-vs-rebuild capture, and band analysis passed. Probe confirms main fluid targets are `171x85` at `1440x900`, project pages retain five visible media tracks, desktop center-band delta is `-0.0001`, and mobile center-band delta is `-0.0121`. Phase 1 remains open for mobile/fog-bed, projection/material feel, and deeper render-manager parity. |
| `current batch` | Home WebGL / Lu Render Manager | Aligned source `Lu/kA` fullscreen bloom/luminosity pass clearing: removed rebuild-only explicit clears before work/main luminosity bright passes, horizontal bloom blur passes, vertical bloom blur passes, and the bloom composite pass. Source `Lu.update()` renders those targets directly after `setRenderTarget(...)`, matching the already-source-shaped `Lo` thumb/sky no-clear path. Added output-probe metadata and hard assertions for `bloomPassClearing=source-Lu-no-explicit-clear` on both work and main render managers, and expanded renderer audit extraction so source `Lu` pass-render anchors are covered. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Project pages retain five visible media tracks. Desktop center-band delta is `-0.0001`; mobile center-band delta is `-0.0134`, so Phase 1 remains open for mobile/fog-bed, projection/material feel, and deeper render-manager parity. |
| `current batch` | Home WebGL / VA Material Surface | Aligned ordinary work-block `VA/zA` material macro spellings to the source surface: `USE_SPECULAR_COLORMAP`, `USE_SPECULAR_INTENSITYMAP`, `USE_SHEEN_COLORMAP`, and `USE_SHEEN_ROUGHNESSMAP` are now rewritten to the source `USE_SPECULARCOLORMAP`, `USE_SPECULARINTENSITYMAP`, `USE_SHEENCOLORMAP`, and `USE_SHEENROUGHNESSMAP` names. Added shader-dump checks for source versus modern sheen-map macros. `git diff --check`, build, renderer audit, shader dump, output probe, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Shader dump confirms source-style specular and sheen-map macros are true in source/rebuild, modern underscore macros are false in both, and no shader/WebGL console errors occurred. Project pages retain five visible media tracks. Desktop center-band delta is `+0.0006`; mobile center-band delta is `-0.0118`, so Phase 1 remains open for mobile/fog-bed, projection/material feel, and remaining `VA` bridge depth. |
| `current batch` | Home WebGL / Floor Reflection | Aligned source `i1.update()` reflector camera/clear ordering: the virtual reflection camera now calls `updateMatrixWorld()` before copying the home projection matrix, and the raw reflection target clear now follows the source `renderer.autoClear===false` conditional instead of an unconditional clear. Added renderer-audit anchors for source `i1` and output-probe assertions for reflection clear mode, projection-copy order, and CSS-sized target shape. `git diff --check`, build, renderer audit, output probe, thumb spotlight probe, project-media probe, home source-vs-rebuild capture, and band analysis passed. Output probe confirms the new source-shape markers; project pages retain five visible media tracks. Desktop center-band delta is `0.0000`; mobile center-band delta remains `-0.0130`, so Phase 1 remains open for mobile/fog-bed and spotlight/projection residuals. |
| `current batch` | Home WebGL / VA Material Body | Aligned one narrow source `VA/zA` material-body residual: ordinary work-block `VA` now strips only the modern r164 `sheenEnergyComp` outgoing-light tail while preserving the `USE_SHEEN` declaration/uniform surface. This turns `r164SheenOutgoingTail` from source false / rebuild true into source false / rebuild false in the shader dump, with no shader/WebGL console errors. `git diff --check`, build, shader dump, output probe, project-media probe, home-only source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0007`; mobile center-band delta remains `-0.0129`, so Phase 1 remains open for mobile/fog-bed and broader projection/material feel. |
| `current batch` | Phase 1 Audit/QA Harness | Hardened shader/runtime QA before the next production `VA/GA` or render-manager change: `scripts/dump-va-shader.mjs` now splits ordinary `VA` sheen evidence into `sheenDeclaration` versus the modern r164 `sheenEnergyComp` outgoing-light tail, and `scripts/probe-output-color.mjs` now fails immediately on shader/WebGL console errors instead of only reporting them. Production rendering is unchanged. This records the lesson from a rejected local shader experiment: physical-material chunk edits must be runtime-gated, not promoted from text residuals alone. `git diff --check`, build, shader dump, output probe, and project-media probe passed; Phase 1 remains open for mobile/fog-bed and strict material/projection residuals. |
| `current batch` | Home WebGL / GA Mouse Attribution | Added source-backed `GA/Ka` mouse-plane attribution and QA gates without changing the visual formula: renderer audit now checks source `GA.createPlane()`, local `Ka` construction, ray-plane scale/offset, `resize()`, local `tMouseSim`, and damped mouse-speed anchors; output probe now reports and asserts active local target size, `uCoords`, `uUvOffset`, `uUvOffsetScale`, ray-plane geometry/z, persistence, and thickness. `git diff --check`, build, renderer audit, output probe, project-media probe, full capture, and band analysis passed. Probe confirms active local target `46x30`, `uCoords=[46,30]`, `uUvOffset=[0.25,0.25,0]`, and `uUvOffsetScale=1.5`. Desktop center-band delta is `+0.0005`; mobile center-band delta remains `-0.0144`, so Phase 1 remains open for mobile/fog-bed and strict `VA/GA` material/projection residuals. |
| `current batch` | Home WebGL / DPR Ownership | Aligned source `Pe/p1` DPR ownership: global renderer/main/media/canvas now use source `Pe.dpr` (`2` normally, `1.5` in low-res), while the home work scene follows source `p1.resize(...Math.min(n,1.5))` for work raw/composite targets, work bloom sizing, screen mouse simulation, and work item `uCoords`. Added output-probe metadata for `dprPolicy`, work/main target sizes, and source DPR modes; expanded renderer audit coverage for `Pe` and `p1.resize`; added `DEVICE_SCALE_FACTOR` plus optional `SKIP_SCREENSHOT=1` to the output probe for high-DPR runtime validation. `git diff --check`, build, renderer audit, DPR 1 output probe, DPR 2 output probe, project-media probe, full capture, and band analysis passed. High-DPR probe confirms canvas/main `2880x1800`, work target `2160x1350`, work bloom start `512x256`, and main fluid `171x85`. Desktop center-band delta is `+0.0014`; mobile center-band delta remains `-0.0140`, so Phase 1 remains open for mobile/fog-bed and strict `VA/GA`/projection feel. |
| `current batch` | Home WebGL / Render Manager Sizing | Historical note: this batch correctly kept work `Lu/kA` on `Fa(workRenderSize)/4`, but incorrectly extended that rule to main `I1`. S1-101 corrects the main branch back to source `I1` half-POT sizing. |
| `current batch` | Home WebGL / Floor Reflection | Aligned source `i1.setSize()` floor-reflection sizing semantics: raw/read/write reflection targets now use CSS viewport `width * .75` / `height * .75`, and reflection blur `uResolution` receives CSS `width,height` instead of DPR-scaled render-buffer dimensions. Added probe fields for `reflectionSizing`, expected CSS target size, and `sourceCssSized`. `git diff --check`, build, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Probe confirms `source-i1-css-viewport-0.75` with `sourceCssSized=true`; project pages still retain five visible media tracks. Desktop center-band delta is `+0.0014`; mobile center-band delta remains `-0.0130`, but mobile max-horizontal-delta residual improves to `+0.0096`. Phase 1 remains open for mobile center-luma and strict `VA/GA`/projection feel. |
| `current batch` | Home WebGL / Render Manager Draw State | Aligned source `blending: ot` (`NoBlending=0`) for the remaining fullscreen/helper render-manager passes: luminosity, bloom blur, bloom composite, FXAA, sky composite, and displacement helper materials. Added `uniforms.passMaterials` to `__rogierOutputProbe` so the helper pass draw states are directly visible in QA. `git diff --check`, build, output probe, shader dump, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Probe confirms luminosity, bloom blur/composite, main bloom blur/composite, FXAA, sky composite, and displacement all report blending `0`; project pages still retain five visible media tracks. Desktop center-band delta is `+0.0007`; mobile center-band delta remains `-0.0131`, so Phase 1 remains open for the mobile/fog-bed residual and strict `VA/GA`/projection feel. |
| `current batch` | Home WebGL / Environment Audit | Tightened source `u1/l1` environment shader body attribution: added the source-retained inert `maskColor` and `black` local declarations to the rebuild environment fragment, and expanded `scripts/dump-va-shader.mjs` with `environmentCoreChecks` for sky UV offsets, `tSky` samples, smooth mask bands, `blend(4)`, `blend(16)`, vertical white band, white mix, clamp/square, indirect diffuse, inert locals, and darken color-dodge tail. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Shader dump shows `u1-environment` fragment delta narrowed from `-436` to `-352`, live include/uniform deltas remain empty, and all environment core checks are source/rebuild true. Desktop center-band delta is `+0.0001`; mobile center-band delta is `-0.0131`, so Phase 1 remains open for mobile/fog-bed structure, exact `VA/GA` material-body residuals, and projection feel. |
| `current batch` | Home WebGL / Thumb Projection | Aligned source `T1/w1/E1` thumb strip position ownership: `updateThumbGallery()` now follows source `w1.updateGalleryProgress()` by positioning thumbs with horizontal wrap only, `position.set(x,0,0)`, instead of the rebuild-only y-offset path. Expanded `__rogierThumbProbe` with thumb strip state (`thumbPositionMode`, item/total widths, offset/transition state, per-thumb hooks/positions) and made `scripts/probe-thumb-spotlight.mjs` assert the source shape. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Thumb probe confirms `source-w1-x-only`, `itemWidth=2`, `totalItems=10`, `totalWidth=20`, all thumb y/z positions at `0`, spotlight map present, target `(0,0,-8)`, and intensity `220`. Project pages still retain five visible media tracks. Desktop center-band delta is `-0.0003`; mobile center-band delta is `-0.0116`. Phase 1 remains open for mobile/fog-bed, exact `VA/GA` material-body, and projection-feel residuals. |
| `current batch` | Phase 1 Audit/QA Harness | Tightened `VA/GA` shader attribution before the next production shader change: `scripts/dump-va-shader.mjs` now maps generic `VA-work` dumps to source `zA` and `HA`, writes source/rebuild files for that pair, and reports `vaFragmentCoreChecks` for the source tail output, mouse-lightness, alpha grid, reveal radius, mouse alpha, specular macro spelling, modern physical residuals, and sheen presence. Current evidence shows the active `VA` alpha/reveal/mouse-lightness anchors are present in both source and rebuild; the remaining clear material-body residuals are source `bsdfs`/`opaque_fragment` include shape plus old `USE_SPECULARCOLORMAP`/`USE_SPECULARINTENSITYMAP` macro spelling versus rebuild's modern underscore spelling. No production shader behavior changed. `git diff --check`, build, shader dump, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0002`; mobile center-band delta is `-0.0129`. |
| `current batch` | Home WebGL / Environment | Aligned a focused source `h1/u1/l1` environment shader/runtime batch: environment fragment blend helpers now use the source `blendColorDodge` / `blendNegation` / `blend(...)` naming and call surface instead of rebuild-only `environmentBlend*` helpers; the source-declared but runtime-unbound `uShader1Mix2` remains declared in shader text but is no longer injected as a rebuild-only material uniform; output probe now records `uShader1Mix2Binding=source-declared-only`. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. `u1-environment` fragment delta narrowed from `-519` to `-436`, live include/uniform deltas remain empty, and project pages still retain five visible media tracks. Desktop center-band delta is `+0.0006`; mobile center-band delta is `-0.0126`, so Phase 1 remains open for mobile/fog-bed, `VA/GA` material-body, and spotlight/thumb projection residuals. |
| `current batch` | Phase 1 Audit/QA Harness | Fixed shader-dump core checks so full-line commented source includes are ignored before normalized formula matching, and added separate commented-include reporting for generic fragment shaders. This clears the misleading `u1-environment.compositeCoreChecks.tonemappingTail` residual: source and rebuild are now both false for live tonemapping, while the source-only commented include list is preserved as non-runtime evidence. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0012`; mobile center-band delta is `-0.0128`. |
| `current batch` | Home WebGL | Aligned source render-state `blending: ot` (`NoBlending=0`) for the source-proven `C1/A1` pre-composite, `OA/CA` work composite, `lA/aA` main composite, `x1/v1` thumb composite, `i1/t1` floor-reflection blur, and `o1` floor material. Added probe fields for those material blending values. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Probe confirms the aligned materials now report blending `0`; project pages still retain five visible media tracks. Desktop center-band delta is `+0.0010`; mobile center-band delta is `-0.0126`, so Phase 1 remains open but this removes a real source draw-state divergence. |
| `current batch` | Home WebGL / QA Harness | Aligned source `T1/w1/E1/M1/x1` thumb shader surface and audit coverage: thumb plane fragment now follows source `S1` local `uv`/`color`/`1. - uProgress` form and source `y1` helper naming, thumb composite now uses source `v1` `saturation(...)`/`mixed` tail form, and shader dump now captures `M1-thumb-plane` with source `S1` fragment plus `b1` vertex mapping. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0006`; mobile center-band delta is `-0.0137`. |
| `current batch` | Home WebGL | Aligned source `V1/H1/Lo` sky render-pass clearing: removed rebuild-only explicit clears from sky raw and sky composite target renders, matching source `Lo.update()` for the sky scene. Added an output-probe marker `skyPassClearing=source-Lo-no-explicit-clear`. `git diff --check`, build, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0022`; mobile center-band delta is `-0.0126`, so Phase 1 stays open for mobile/fog-bed and projection residuals. |
| `current batch` | Home WebGL | Aligned source `i1` reflector target/camera initialization: raw floor-reflection target now follows the source create-clone-toggle sequence (`depthBuffer:false` -> clone read/write -> raw `depthBuffer=true`), read/write targets stay depthless, and the virtual reflection camera uses the default `PerspectiveCamera` surface before copying the home projection. Expanded `reflectionStateProbe()` with target/camera/texture ownership fields. `git diff --check`, build, output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Desktop center-band delta is `+0.0013`; mobile center-band delta remains `-0.0133`, so Phase 1 stays open. |
| `current batch` | Home WebGL | Aligned the source `T1/x1/Lo` thumb render-manager pass by removing rebuild-only explicit clears from the thumb raw and thumb composite target renders. Source `Lo.update()` renders those targets directly with `setRenderTarget(...); render(...)`, and this path feeds the home spotlight map. `git diff --check`, build, thumb spotlight probe, project-media probe, full source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0009`; mobile center-band delta is `-0.0129`. |
| `current batch` | Phase 1 Audit/QA Harness | Added `vertexAnalysis.coreChecks` to `scripts/dump-va-shader.mjs` so source `HA` and rebuild `VA` vertex shaders are checked on normalized core anchors: screen UVs from clip coordinates, local mouse sampling, perlin height/reveal mix, pre-perlin mouse scale, mouse z transform, spread scale, and source world-position divide. Current evidence shows all core anchors present in both shaders, so the older `transformed *= 1.0 - mouse` key-check mismatch is a diagnostic false positive rather than a production difference. |
| `current batch` | Phase 1 Audit/QA Harness | Added `compositeCoreChecks` to `scripts/dump-va-shader.mjs` so dumped composite shaders report source-vs-rebuild parity for core anchors such as scene rgbshift, bloom addition, fluid luminance, darken opacity, multiply/lighten blends, saturation, and tonemapping. The current `OA-work-composite` check shows all core anchors present in both source and rebuild, so the remaining transfer gap should not be attacked by rewriting the `OA/CA` formula or promoting debug gamma transfer. `git diff --check`, build, shader dump, and project-media probe passed. |
| `current batch` | Home WebGL | Aligned source `IT` pointer sampling lifecycle: camera/mouse input now updates on `pointerdown`, `pointermove`, and `pointerup`, matching source `IT.addListeners()` and its down/up delegation to `onPointerMove`. This is interactive input parity rather than a static brightness fix. `git diff --check`, build, output probe, thumb spotlight probe, project-media probe, full source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0008`; mobile center-band delta is `-0.0133`. |
| `current batch` | Home WebGL | Aligned source `IT/p1` camera-controller matrix ownership: home camera now follows the source `group -> rotateGroup -> innerGroup` path with manual matrix updates and `innerGroup.matrixWorld` decomposition instead of directly mutating `camera.rotation.z`. Output probe now reports camera quaternion/controller/rotate-group state. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0009`; mobile center-band delta is `-0.0129`. |
| `current batch` | Home WebGL | Aligned a source `HA/VA` work-cube vertex ordering residual: the `mouseSim.r * .05` scale now happens before the Perlin reveal mix like source, instead of after reveal/side masking. This keeps mouse deformation and cube reveal weighting closer to the original shader. `git diff --check`, build, shader dump, output probe, thumb spotlight probe, project-media probe, full source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0018`; mobile center-band delta is `-0.0143`. |
| `current batch` | Home WebGL | Aligned source `Iu/p1/kA/GA` update order: the screen-space mouse simulation now runs in the render-manager path before work-scene rendering, while camera controller, spotlight parallax, visible-block side reveal, pointer projection, and per-block mouse simulation are prepared after rendering for the next frame like source `Iu.update() -> p1.update()`. Added output-probe metadata for the source update order. `git diff --check`, build, output probe, thumb spotlight probe, project-media probe, full source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0024`; mobile center-band delta is `-0.0143`. |
| `current batch` | Home WebGL | Aligned the default source `I1/Lu` main screen path: when main blur/bloom/fxaa are disabled like source defaults, the completed `A1/C1` pre-composite now renders directly to the canvas instead of passing through an extra rebuild-only generic main composite with rgbshift/fluid-light tail. `git diff --check`, build, output probe, thumb spotlight probe, project-media probe, full source-vs-rebuild capture, and band analysis passed. Desktop center-band delta is `-0.0015`; mobile center-band delta is `-0.0168`. |
| `current batch` | Home WebGL | Aligned source `SD/p1/T1` home spotlight target depth: home spotlight still uses the thumb composite map and `(0,0,3.7)` position, but its target now matches source `(0,0,-8)` instead of the rebuild-only origin target. `git diff --check`, build, thumb spotlight probe, project-media probe, output probe, full source-vs-rebuild capture, and band analysis passed. |
| `current batch` | Home WebGL | Corrected the source `a1` floor geometry from the rebuild-only rectangular `PlaneGeometry(60,32)` to source `CircleGeometry(60,32)` and added reflection-state probe metadata. Build, diff check, source-vs-rebuild home capture, band analysis, focused attribution, project-media probe, and full capture passed in the prior verification cycle. Desktop center-band delta is now roughly `+0.001` against source; Phase 1 remains open for mobile/background and cube/thumb projection parity. |
| `current batch` | Phase 1 Audit/QA Harness | Added query-only reflector diagnostics for `debug-floor-reflection=no-clip`, `no-blur`, and `raw-sample`, plus focused brightness-attribution variants. Build, diff check, default output probe, focused attribution, and project-media regression passed. The matrix rejects all three as production fixes: no-clip worsens reflection target bands, no-blur keeps the same boundary, and raw-sample does not improve the final result. |
| `current batch` | Phase 1 Audit/QA Harness | Added render-target horizontal band statistics to `__rogierOutputProbe` and made `scripts/analyze-home-bands.mjs` accept an explicit capture directory plus source-vs-rebuild band deltas. Build, diff check, home output probe, and home source-vs-rebuild capture passed. The new evidence shows the hard horizon already exists in `workRaw` and is strongest in `floorReflectionRead`, while `skyComposite` is smooth; the next source work should target `i1/a1` reflector target content and floor sampling/camera ownership, not global brightness or final composite tuning. |
| `current batch` | Home WebGL | Aligned source `i1/t1` floor-reflector blur and camera surface: the second reflection blur pass now uses source `(0,15)` instead of rebuild-only `(0,0)`, and the reflector camera no longer assigns a separate non-source `near` value after copying the source camera projection. Source `t1/QA` blur shader body was audited and kept unchanged. Build, diff check, output probe, brightness attribution, project-media probe, and full capture passed. |
| `current batch` | Home WebGL | Aligned source `o1` floor material surface by moving the floor shader to `RawShaderMaterial + GLSL3`, explicit source-style shader declarations, `USE_NORMALMAP`, `mediump` fragment precision, and GLSL3 `texture(...)`/`FragColor` output. Build, diff check, output probe, brightness attribution, project-media probe, and full capture passed. |
| `current batch` | Home WebGL | Fixed source `I1/ag` main-fluid resize ownership: the source-shaped `I1.resize()` bridge already passes `floorPowerOfTwo(renderSize) / 2 / 3`, so `resizeMainFluidPass()` no longer multiplies those dimensions by `.005` again. Main fluid FBOs now resize to `171x85` at `1440x900` instead of staying `1x1`. Build, diff check, output probe, project-media probe, and full capture passed. |
| `current batch` | Phase 1 Audit/QA Harness | Expanded the current step into three related floor/environment diagnostics: `debug-floor=off`, `debug-floor-reflection=off`, and `debug-environment=off`, plus brightness-attribution variants and output-probe metadata. Default rendering is unchanged. The matrix shows environment is the dominant visible contributor (`environment-off preComposite=0.1232` vs default `0.2818`), while the floor masks/darkens it (`floor-off preComposite=0.5343`) and reflection contributes additional darkening (`floor-reflection-off preComposite=0.2306`). Next source work should inspect `a1/i1/h1/u1` draw-state and render-order parity, not brightness tuning. |
| `current batch` | Home WebGL | Restored source `a1/i1` floor hierarchy and reflector ownership: floor now uses `floorGroup -> floorPlane -> floorReflector`, the `-1.65` y offset lives on the group, reflection matrices/clip planes read from the reflector object's `matrixWorld`, and reflection/debug visibility hides the group like source `a1.onBeforeRender`. Build, diff check, output probe, brightness attribution, project-media probe, and full capture passed. |
| `current batch` | Home WebGL | Aligned source `GA/VA` base material color parsing: work cubes now construct the `#808080` diffuse through Three's normal `Color` path like source `new VA({ color: new Color("#808080") })`, while project block/emissive setter colors remain on the source-owned raw RGB path. Output probe now reports active work material color/emissive fields. Build, diff check, output probe, project-media probe, and full capture passed. |
| `current batch` | Home WebGL | Aligned source `I1/Lu` main-fluid gate semantics: `SOURCE_MAIN_RENDER_SETTINGS` now keeps the source default disabled template, runtime main settings enable fluid only when the local source GPU-tier bridge resolves to tier 3, `sourceLowRes()` uses the same tier bridge, and main resize/update/probe paths read the instance settings instead of the static template. Build, diff check, output probe, project-media probe, and full capture passed. |
| `current batch` | Home WebGL / QA Harness | Aligned main `I1/Lu` render-manager setting ownership by using source main blur/fxaa settings instead of work `kA` settings in resize, screen composite input selection, and tick blur gating; completed the source `aA` main composite bloom branch; and corrected shader-dump mapping so `Lu-main-composite` compares against source `aA` rather than `Lo/m1`. |
| `current batch` | Project/Main WebGL / QA Harness | Aligned source `$1/j1/W1` media composite and `UD/FD/LD` project-media material surface: shader comparison now maps `j1` to source `G1`, media composite exposes the source pass-through uniforms, project media opacity is owned by `C1.uMediaReveal` instead of a non-source per-plane `uSceneOpacity`, and a dedicated project-media probe now checks `/gc-2026/` plus `/hashgraph-vc/` for media presence/output. |
| `current batch` | Home WebGL / QA Harness | Cleaned the `VA/A1` shader surface by restoring the source-shaped `mouseSim` world-position expression, removing production `debug-va-*` and `debug-spotlight-map-transfer` shader branches plus obsolete comparison scripts, and adding the missing `A1.tScene` shader declaration. |
| `fd76927` | Home WebGL | Aligned the source `I1/C1` render-manager surface by adding the default-disabled lensflare settings/target, wiring `C1.tLensflare` to the full-resolution target, adding source-surface `tScene`/`tPortal` uniforms, using explicit main bloom factors, and keeping source `I1` half-POT main bloom sizing separate from source `Lu/kA` quarter-POT work bloom sizing. |
| `b9db4fd` | Home WebGL | Removed the unused rebuild-only pre-composite bloom branch (`preBloom*` targets/materials/probes/dead method) because source `Lu` only has one bloom chain per render manager and main `I1/C1` bloom is disabled by default. Active work bloom and disabled main bloom placeholders remain intact. |
| `707aac0` | Home WebGL | Aligned the source shader surface for `OA/kA` work composite and `x1` thumb composite by restoring their `tonemapping_pars_fragment` / `tonemapping_fragment` include pair while keeping `toneMapped:false`; shader dump and full QA passed, and the change is structural rather than a brightness fix. |
| `eddc030` | Phase 1 Audit/QA Harness | Expanded shader-dump coverage from only `VA` to the active Phase 1 shader chain (`VA`, `A1/C1`, `OA/kA`, `Lu/lA`, thumb/media composites, and `u1/l1` environment), added machine-readable source-vs-rebuild residual summaries, and recorded color-setter semantics as a source-checked non-fix. Production rendering is unchanged. |
| `bfac2e5` | Project/Main WebGL | Batched source `$1/j1/W1` media render-manager alignment: added `mediaRawTarget -> mediaCompositeMaterial -> mediaTarget`, kept `C1.tMedia` permanently wired to the media composite texture like source `nD`, and moved A1/main composite execution outside the home-only branch so project media pages continue rendering when the home work scene is hidden. |
| `f0c4bd7` | Home WebGL | Batched source `Lu/lA/aA` main output-chain alignment: added the source default main composite shader/pass after `A1/C1`, routed `compositeTarget` through `tScene`, fed source main-fluid UV distortion and static contribution, kept `kA/OA` scoped to the work-scene offscreen composite, and preserved project media regression gates. |
| `cb499d9` | Home WebGL | Added source-style `wavvesScene/k1` displacement pass for work cubes. |
| `cd1a3a9` | Home WebGL | Aligned main home composite with source `A1/C1` shader behavior using `tNoise`, `tPerlin`, RGB shift, mouse-flow distortion, and blend operations. |
| `1c31149` | Home WebGL | Refined mouse simulation toward source `Ka`: old/new/target pointer state, delta-based lerp, persistence, brush thickness, and flow channels. |
| `d1a8f00` | Home WebGL | Drove work cube displacement with source-style Perlin texture and restored stronger mouse Z displacement. |
| `7f35f97` | Home WebGL | Aligned mouse UV projection with source `uUvOffset/uUvOffsetScale` and enlarged ray plane mapping. |
| `efe5786` | Home WebGL | Added lightweight work-scene bloom compensation based on source `kA/OA` luminosity and bloom settings. |
| `9ff9453` | Home DOM | Added source-style work CTA magnet movement from source `gD`. |
| `ed80a14` | Home DOM | Added source-style temporary `is-dragging` state during gallery wheel/touch navigation to suppress accidental link interaction, matching source `yD` pointer gating. |
| `2eaa8e5` | Home DOM | Added source-compatible progressbar `data-slug` support while preserving existing `data-progress-slug` behavior from the rebuild. |
| `7f41fba` | Home DOM | Aligned homepage title/work/footer intro animation targets with source `Rg/vD/Ou` inner-element structure. |
| `f7531e4` | Home DOM | Aligned work title click behavior with source `vD`: project titles select the active project while the CTA remains the detail-page entry point. |
| `66c6970` | Home DOM | Added source-style `gD` CTA text and opacity animate-in/out when the active work item changes. |
| `71f2403` | Home DOM | Aligned touch gallery movement with source `yD` virtual-scroll deltas by driving project changes during touchmove instead of only on touchend. |
| `a0d4955` | Home DOM | Matched source `Ou` footer contact resize behavior by shortening the mobile contact label to `E-mail`. |
| `f9840d9` | Home WebGL | Moved gallery velocity roll from the rotating project ring to the work scene container to better match source `yD.updateScene`. |
| `e8a811d` | Home DOM | Added source-style header description and availability inner-line animation targets from `Ki/SD.animateIn`. |
| `e7997ec` | Home DOM | Added source-style `.ui-nav-a-inner` targets and nav animate-in timing from source `Tr`. |
| `833594d` | Home DOM | Aligned mobile nav link/state structure with source `Ar` using `.ui-nav-mobile-a`, inner spans, `data-slug`, and `html.is-nav-mobile-open`. |
| `44f341b` | Home DOM | Rebuilt the mobile nav shell closer to source `Ar`, including `.ui-nav-mobile-content`, `.ui-nav-mobile-content-bg`, footer links, toggle-line state, fade-in, and delayed link active/close timing. |
| `3fbf535` | Home WebGL | Matched source `Se.setThumbMouseLightness` propagation by animating `uMouseLightness` on every work block instead of only the active block. |
| `e710154` | Home WebGL | Split source-style overview/detail darken from thumbnail darkness so main composite uses `darkenOverview`/`darkenDetail` while thumb composite and block projection use `thumbnail.darkness`. |
| `1cac355` | Home WebGL | Moved work spotlight parallax closer to source `p1.update` by deriving spotlight x/y from the camera position instead of directly from pointer coordinates. |
| `53e7e31` | Home WebGL | Renamed the work block screen-coordinate uniform to source-compatible `uCoords` while keeping thumb-plane `uResolution` aligned with source `E1`. |
| `cc52ec8` | Home DOM | Kept work gallery pointer gating closer to source `yD` by holding `is-dragging` through the snap window and marking keyboard-driven gallery steps too. |
| `9930ced` | Home DOM/WebGL | Added source-style work gallery event semantics with `rd:nav-click`/`rd:project-active` and carried split darkness payload fields through active project changes. |
| `41bbd7d` | Home DOM | Added source-style `.ui-work-container` around the home work content and aligned fixed container CSS with source `yD`/bundle styles. |
| `0a77fdf` | Home DOM | Aligned work gallery virtual-scroll diff handling with source `yD` so sub-threshold deltas still feed scene inertia while only larger deltas trigger next/prev. |
| `2bdb0ce` | Global DOM | Added source-style button SVG formatting from `gD.formatButton()` so `.c-button` background SVG/rect dimensions match rendered button size on init and resize. |
| `8e65755` | Home DOM | Added source-style mobile `touchstart` selection for work titles and progressbar items from `vD/_D`, with click dedupe to avoid double navigation. |
| `0ba12a7` | Home DOM | Added session-backed work gallery state restoration as a local equivalent of source `Qe.workState` for active slug/index and virtual scroll position. |
| `31e3001` | Home DOM | Added source-style home gallery `mousedown` default suppression from `yD.onMouseDown`, scoped to the home view. |
| `0c3c407` | Home WebGL | Restored source `GA` work-block displacement strength constants: Perlin reveal mix `10.25` and unrevealed wave depth `9.0`. |
| `9021b23` | Home WebGL | Split local cube mouse simulation from source-style screen-space `tMouseSim2`, so work-block fragment light/alpha and main composite sample screen mouse flow separately from vertex displacement. |
| `e076340` | Home DOM/WebGL | Passed gallery frame delta into `setGalleryProgress` and matched source `yD.updateScene` delta-based smoothing for work-scene roll and zoom. |
| `fdaeff0` | Home WebGL | Matched source `w1.updateGalleryProgress` by keeping thumb `uProgress` at its default value during gallery wrapping instead of dimming non-active thumbs. |
| `19cdcbb` | Home WebGL | Moved the home composite tail closer to source `OA`: final darken now uses screen-space `mouseSim.r`, applies source-style lighten blend with black, and finishes with saturation after darkening. |
| `33b2c9e` | Home WebGL | Added a separate low-resolution home bloom pass so final composite samples `tBloom` like source `Lu/kA` instead of deriving all glow inline from `tWork`. |
| `5ac8713` | Home WebGL | Matched source `p1.resize` mobile behavior by keeping the work WebGL scene active below `LG` and shifting `sceneWrap.position.y` to `.3` below source `MD=800`. |
| `2af884f` | Home DOM | Moved work gallery pointer gating closer to source `yD.checkSpeed()` by tracking pointer activity and last deltas, then toggling `.is-dragging` from the RAF loop instead of using a fixed timeout for every gallery delta. |
| `452bf21` | Home DOM | Matched source `yD.onVirtualScroll` snap state more closely by only easing `scroll.diff` back toward zero after gallery input has enabled the snap flag and by clearing/restarting the 100ms snap timeout on each delta. |
| `fd92af1` | Home DOM | Extended the session-backed work gallery state toward source `Qe.workState` by preserving `index.current/prev/next`, `activeHook`, and `targetHook` alongside scroll state. |
| `8d99bdb` | Home DOM/WebGL | Added source-style `SD.animateIn -> Se.showScene()` wiring so the home composite `uReveal` animates in over 1.6s with `expo.out` after WebGL initialization. |
| `489ca4c` | Home DOM | Added source-style `SD.onLeave -> Ki.animateDescriptionOut/animateAvailibilityOut` behavior to the home CTA transition path with 0.5s opacity fades. |
| `ec26bfe` | Home WebGL | Matched source `yD.onWorkGalleryOut()` more closely by killing active work-block reveal tweens before running the gallery-out spread and project fade. |
| `1cb6a6b` | Home WebGL | Matched source `p1.update()` mobile spotlight parallax by adding the `.3` y-offset below `MD=800` while keeping camera-derived spotlight movement. |
| `e93e165` | Home WebGL | Matched source `p1.setLights()` spotlight cone by using `angle = PI / 4` for the shader thumb-map projection instead of the narrower half-angle approximation. |
| `c183c76` | Home WebGL | Made the auxiliary thumb projection plane follow source-style scene reveal/out timing instead of staying permanently visible outside the spotlight-map lifecycle. |
| `15b1ad9` | Home WebGL | Moved the lightweight home bloom pass closer to source `kA` by adding a `uRadius` uniform set to source bloom radius `1.5` for luminosity blur offsets. |
| `95ebb65` | Home WebGL | Moved final home bloom compositing closer to source `OA` by adding `tBloom` directly and adding a shifted bloom sample instead of attenuating bloom a second time. |
| `32ef9ec` | Global Audio | Split CTA click audio from gallery-out soft-woosh behavior using source `gD`/`yD` Howler assets and playback rates. |
| `f200c8f` | Home DOM/WebGL | Cleared local CTA preview state before work-gallery leave so the source-style `yD.onWorkGalleryOut()` transition is not tinted by hover/focus preview leftovers. |
| `7a49018` | Global Audio | Split source-style hover and gallery-enter plucks cues so `yD.animateIn()` can play plucks without reusing the hover sound. |
| `7ead336` | Global Audio | Moved the source-style home gallery plucks trigger behind audio listener initialization so dynamic import ordering cannot drop `yD.animateIn()` audio. |
| `a0fcfe7` | Home WebGL | Aligned the source-style `p1` floor and environment planes to the original large geometries, y offsets, floor rotation, and demorgen environment rotation adjustment. |
| `c68adb6` | Home WebGL | Moved the home bloom pass closer to source `Lu/kA` by rendering five downsampled bloom mips and recombining them with source-style bloom radius factors before final `OA` compositing. |
| `e3195db` | Home DOM/Audio | Batched Phase 2 source alignment for `vD/_D/yD`: click audio now only binds `data-sound-click`, project changes emit source-style woosh, redundant active project selections short-circuit, and session work state stores `activeProject`/`sceneRotation`. |
| `bd18bda` | Home WebGL/DOM | Batched source `yD/gD` mouse-factor lifecycle alignment: centralized mouse-factor tween ownership, removed constructor auto-start, made home entry explicitly animate mouse factor from 0 to 1, reused the same path for CTA preview enter/leave, and reset mouse factor on gallery leave. |
| `342bb68` | Home WebGL | Batched source `IT/p1/yD` camera-controller alignment: added source-style camera controller settings, pixel-space pointer tracking, mobile camera-origin z behavior, delta-driven camera roll, and home entry wiring for `targetXY=(1,.5)` with `rotateAngle=20`. |
| `aa8a85f` | Home WebGL | Batched source `Lu/kA` bloom pipeline alignment: split luminosity threshold into a bright target, added source-style horizontal/vertical bloom render targets for five mips, matched floor-power-of-two quarter sizing, and fed vertically blurred mips into the existing source-factor bloom composite. |
| `4210264` | Home WebGL | Batched source `T1/w1/E1/SD/p1` thumb projection alignment: matched thumb camera near/far and square render-target sizing, reset thumb map metadata to source `1x1`, changed thumb composite darkening to multiply, reduced the auxiliary projection overlay, and moved it closer to the spotlight-map path. |
| `d378e2f` | Home WebGL | Batched source `A1/C1/OA/p1` composite alignment: removed local procedural/background/glow compensation from the main composite, restored texture-driven `uPerlin=.1` displacement, shifted bloom sampling through fluid UVs, fixed source work/composite background colors, and removed stale composite color uniforms. |
| `e95069f` | Home WebGL | Batched source `Ka/Lu/A1` mouse-simulation alignment: replaced CPU `DataTexture` brush updates with source-style low-resolution ping-pong render targets, added the original mouse simulation shader inputs, resized simulation targets from render resolution like `Lu`, fed both mesh and screen mouse textures through the WebGL render path, and restored source-style `mouseSim.rg` composite flow sampling. |
| `c312ef6` | Home DOM | Batched source `vD/_D/yD` nav semantics alignment: split immediate DOM active/CTA updates from scene project activation during nav/progress clicks, delayed scene activation until the source-style transition window completes, preserved keyboard navigation as direct next/prev gallery motion, and fixed mobile touch-end delta tracking from the original touch start point. |
| `f844355` | Home WebGL | Batched source `p1/SD/T1` spotlight-map alignment: projected thumb sampling from source-style spotlight position and target `(0,0,-8)`, added per-frame spotlight basis uniforms, synchronized spotlight target/right/up during intensity changes, and further reduced the auxiliary projection-plane overlay so cube shading carries the thumb-map projection. |
| `3306bf0` | Home DOM | Batched source `gD/yD/SD` gallery-out interaction alignment: moved CTA magnet smoothing to source `yf(..., 2.5, delta)` exponential damping, centralized CTA preview cleanup, introduced a source-style `rd:work-gallery-out` event consumer for leave animation, guarded duplicate gallery leaves, and reset stale leave state on restored home pages. |
| `78838b6` | Home DOM/WebGL | Batched source `yD/SD` home-entry and gallery-scroll alignment: delayed work-gallery scroll activation until home entry, moved scroll smoothing to source `Yi` exponential damping, gated gallery input and WebGL progress updates behind active state, matched WebGL roll/zoom damping to source `updateScene`, and tied plucks playback to the gallery entry event with import-order protection. |
| `5a9d6a6` | Home DOM/WebGL | Batched source `yD/Qe.workState` restoration alignment: restored active project identity before first gallery tick, prevented restored pages from replaying duplicate active-project scene transitions, preserved source-style scene roll/zoom state separately from carousel progress, reset restored `scroll.active` until home entry, and added a narrow WebGL gallery-state restore hook for scene rotation/thumb progress. |
| `e07db5d` | Home WebGL | Batched source `OA/kA` composite alignment: reduced the home final composite to the source `OA` scene/bloom/mouseSim tail, removed local noise/perlin/contrast/background-lighten compensation from the composite pass, kept the existing source-style bloom mip chain, changed fluid contribution to `length(mouseSim.xy) * .015`, and removed stale composite uniforms/update paths. |
| `1e4fd91` | Home WebGL | Batched source `A1/C1` pre-composite alignment: added a separate A1-style pre-composite pass before the final OA pass, restored source perlin/noise/mouseSim/contrast preprocessing in that layer, kept bloom in the source OA/kA branch to avoid double bloom, routed raw home scene through `workRawTarget -> preCompositeTarget -> finalComposite`, and fixed stale `tPerlin` writes after the previous OA cleanup. |
| `c7cf4ac` | Home WebGL | Batched source `GA/p1` work-block material and spotlight-map alignment: moved cube fragment UVs back toward source `vUv / uGridSize + instanceOffset`, restored the source-style random grid alpha formula without local seed offsets, reduced non-source thumb/projection color amplification, softened spotlight-map contribution so it behaves more like projected map lighting, and clamped local normalized spotlight intensity to the source max-intensity range. |
| `0757011` | Home DOM | Batched source `yD` lifecycle and input-state alignment: normalized restored gallery scroll/progress/remainder before first frame, added source-style timeout ownership for nav/next/prev transitions, blocked wheel/touch deltas during next/prev transition windows, gated keyboard navigation behind active gallery state, and added cleanup paths for global gallery listeners/timers while preserving `pagehide` state saves. |
| `cfbf96b` | Project DOM/WebGL | Batched source `OD/CD` project-state alignment: added next-project color payloads to project pages, implemented source-style next-project scroll threshold switching for main color/ambient/media background/saturation/contrast, exposed a narrow WebGL project-scroll state setter, added project-leave media opacity/fluid reset, and preserved project media track counts during smoke checks. |
| `7ca3a77` | Project DOM | Batched source `RD/ND` project reveal alignment: added source-compatible title span structure for `.ts-2 > span`, split project header intro animation from the generic content fade so title/info/link targets follow source `RD.animateIn`, added overflow/display guards for project title and info spans, and moved mobile DOM media reveal timing closer to source project animation duration while keeping desktop WebGL media tracks stable. |
| `88c7db0` | Project WebGL | Batched source `FD/ND` project media-plane alignment: cached source-style `parallaxTop` state per media plane, updated desktop media `uCameraDistance` to use `mediaCamera.position.y - plane.position.y` like source `FD.update`, preserved top-parallax override as `-scroll`, and forced a media-plane position refresh before reveal tweens start. |
| `9899681` | Home DOM | Batched source `yD/vD` gallery nav activation alignment: title/progress nav clicks now immediately activate the selected project scene and state like source `yD.onNavClick`, while preserving the 1200ms transition lock. |
| `7e35956` | Home DOM/WebGL | Batched source `Se/yD` state payload alignment: split project saturation from thumbnail saturation, carried `data-thumb-saturation` through DOM payloads into WebGL thumb materials/composite, preserved more source-shaped `Qe.workState.scroll` fields, and corrected pointer-speed gating math toward source `checkSpeed()`. |
| `1fe3bf9` | Home WebGL | Batched source `Se/p1` visual-state control alignment: added tween ownership for scene reveal, darken, reveal spread, and spotlight intensity, moved `showScene()` back to main-composite reveal only, changed darken timing/easing to source defaults, and stopped applying thumbnail saturation directly to work-block materials so the thumb composite remains the source of that adjustment. |
| `6936bff` | Home DOM | Batched source `Cg/yD` input lifecycle alignment: restored source pointer-speed gating expression, removed local swipe-end threshold navigation in favor of source zero-delta touchend semantics, made touch listeners non-passive like source virtual scroll, and guarded repeated mouse/touch starts while a pointer is already active. |
| `29251c7` | Home DOM | Batched source `vD/_D/gD` component-lifecycle alignment: made work title, progress, CTA hover/focus/click, and CTA magnet handlers explicitly removable during gallery cleanup, and killed CTA timelines/tickers like source component destroy paths. |
| `e972926` | Home DOM | Batched source `Rg/Ou/SD` page chrome lifecycle alignment: matched title intro stagger to source `Rg`, delayed footer contact first resize update like source `Ou`, added cleanup for footer resize and Lenis RAF/destroy, and wired motion cleanup into pagehide/beforeunload. |
| `d347697` | Home/Project WebGL | Batched source `Se` shared visual-state tween alignment: added tween ownership and instant paths for main color, ambient light, saturation, contrast, media background, and active block color so rapid home/project state switches do not leave stale GSAP updates fighting the current visual state. |
| `c478490` | Home/Project WebGL | Batched source `Se/OD/yD` lifecycle tween ownership: added source-style kill/instant paths for fluid strength and media opacity, owned project media translation tweens, owned work-scene revealProject tweens, and centralized active project thumb/block state tweens to prevent stale GSAP writes during fast gallery and route transitions. |
| `5ee54a0` | Home DOM | Batched source `yD` active-project lifecycle alignment: made project activation idempotent unless the slug changes or initialization forces it, aligned `rd:project-active` payload slug with the active project id, guarded invalid nav indexes, made home gallery entry idempotent, and removed stale CTA timelines during gallery cleanup. |
| `637762d` | Home/Project DOM | Batched source component destroy/lifecycle cleanup alignment: made button formatting, work gallery, scroll state, mobile project media observers, next-project state switching, and project-leave link handlers return cleanup callbacks that are collected by pagehide/beforeunload lifecycle teardown. |
| `b5e10e5` | Home WebGL | Batched source `p1/T1` resize and projection scale alignment: introduced the source DPR cap of `1.5`, reapplied renderer pixel ratio during resize, matched the work camera near/far plane to source `p1.setCamera`, and routed work-block screen-coordinate uniforms from actual render target dimensions. |
| `ee79ab3` | Home WebGL | Started source `p1.setLights` / `Se.setDirectionalLightIntensity` parity by adding tween-owned directional-light state, syncing it into work-block shaders, and preserving the source default intensity of `1.5` when projects activate. |
| `1bad2ac` | Home WebGL | Batched source `GA/p1/Ka` work-block simulation alignment: restored source low-res layer count fallback, changed local cube mouse simulation to source `persistance=.85` and `thickness=.1`, moved mouse-lightness darkening toward source `mouseF`, restored source screen-y gated alpha boost, and kept the screen mouse simulation on the existing broader composite brush. |
| `6a8b206` | Home DOM/WebGL | Batched source `yD/SD/Cg/gD` home-entry and input lifecycle alignment: added a WebGL gallery-entry hook that resets block reveal and animates `uRevealProject` like `yD.animateIn`, moved gallery scroll activation after WebGL entry state restore, preserved source touchstart/touchend zero-delta semantics, kept CTA clicks from starting a nav scroll before gallery-out, and removed the duplicate boot-time mouse-factor animation. |
| `c4cd296` | Home DOM/WebGL | Batched source `vD/_D/gD/yD` preview and event semantics alignment: removed the non-source DOM `.work-preview` layer and `html.is-work-previewing` CSS/JS state, kept CTA hover/focus preview routed only through WebGL mouse-factor state, made progressbar selection update DOM active state immediately like `_D.onListItemClick`, restored slug-shaped `rd:project-active` event detail, and split payload delivery into a separate compatibility event. |
| `280cccc` | Home WebGL | Batched source `GA/T1/E1/Ka` shader and simulation alignment: restored source unrevealed work-block wave depth, made block alpha finish with `uRevealSides` like `GA`, matched thumb transition fallback alpha to `S1`, moved the screen mouse simulation to source `persistance=.85` and `thickness=.1`, and removed the local auxiliary projection plane from the home scene so projected thumb lighting is carried by the cube shader. |
| `4d0c93d` | Home WebGL/DOM | Batched source `A1/OA/kA/gD` fluid and preview semantics alignment: split `tFluid` back out from `tMouseSim` in the pre-composite and final composite shaders, supplied a black source-style disabled-fluid texture for home `kA`, kept final `OA` mouse simulation use scoped to darkening instead of flow brightness, kept `A1` displacement UVs tied to disabled fluid unless a real fluid pass exists, and made CTA preview hover/focus only animate work-scene mouse factor like `gD`. |
| `8362ea8` | Home DOM | Batched source `vD/_D/gD` selection-event semantics alignment: removed non-source card hover/focus project activation, kept work-title selection driven by click/mobile touchstart, kept CTA focus routed only to preview mouse-factor state, removed the duplicate progressbar DOM-active write after nav selection, and bound title/progress touchstart handlers only under the mobile breakpoint like the original components. |
| `5492d96` | Home WebGL | Batched source `GA/VA` work-block geometry/material alignment: restored source-style `instanceIndex` attributes, moved instance color/alpha seeds back to per-instance `Math.random()` values, matched the rounded cube geometry segment count to source `mg(size,size,size,.05)`, disabled block material depth testing like `VA`, and routed the screen mouse alpha boost through `uMouseFactor` instead of thumbnail mouse-lightness. |
| `165051c` | Home WebGL | Batched source `GA` vertex displacement alignment: removed local procedural noise/wave mixing from the work-block vertex shader, restored source perlin-only reveal displacement, multiplied perlin displacement by the source fade term, changed reveal displacement mixing back to `.25`, and kept wave displacement sourced directly from `tDisplacement.r` before the source mouse transform. |
| `a833dcd` | Home DOM | Batched source `vD/_D/yD/Se` event semantics cleanup: removed unconsumed rebuild-only `rd:project-active-payload` and `rd:nav-click` browser events, kept `rd:project-active` as a slug-shaped event only for emitted state changes, kept `rd:woosh` scoped to real project switches, and suppressed boot-time active-project event/audio emission while preserving initial DOM color state. |
| `d348ebc` | Home DOM | Batched source `yD` scroll and pointer lifecycle alignment: restored source-shaped scroll `limit/step/offset` fields in runtime state, routed gallery hook math through those scroll fields, moved `is-dragging` to the `.ui-work-content` component root like source `yD.el`, added matching CSS so pointer suppression actually applies there, and preserved source zero-delta touchstart/touchend snap semantics. |
| `f5974b3` | Home WebGL | Batched source `Se` shared visual-state alignment: removed the rebuild-only minimum clamp from project saturation so low-saturation source data is honored, made `setBlocksColor()` affect every work block like source `Se.setBlocksColor`, removed active-block duplicate writes for block color and thumb mouse-lightness, and kept those global setter tweens from being killed by project reveal cleanup. |
| `fae41f0` | Home DOM | Batched source work-list CSS parity: matched desktop `.ui-work-ul li` block/overflow structure, restored source `ui-work-a` letter spacing and inner-span transform baseline, kept intro animation transform ownership so titles do not snap back after GSAP, and aligned work CTA width/opacity transition plus wide-aspect override with the source stylesheet. |
| `971e955` | Home DOM | Batched source `Ki/Tr/Rg/Ou/Ar` page chrome lifecycle parity: desktop nav links start with source-style disabled pointer events and are enabled by animate-in, intro tweens preserve transform ownership instead of clearing source baselines, header description/availability out tweens kill active in tweens before fading, mobile nav active/close timing follows source `Ar`, and mobile nav listeners are cleaned up on page teardown. |
| `c668b4f` | Home WebGL | Batched source `GA/p1/Ka` alpha, projection, and mouse-simulation parity: work-block alpha now follows the original two-grid random formula, local logo alpha boosting was removed, spotlight projection uses the source mouse-expanded world position, both mouse simulation buffers use source-style viewport `/4` sizing, and mesh/screen simulation `uCoords` now stay aligned with that source buffer size. |
| `de7cba8` | Home DOM | Batched source `sl/SD` view lifecycle parity: `[data-view]` roots now start hidden and fade in over the source 0.5s linear timing, work-gallery and project-route leaves fade the active view out, html receives source-style `is-${view}` state, header-name pointer events are explicitly restored during view entry, and lifecycle cleanup removes those temporary view states. |
| `eed7f1b` | Home WebGL | Batched source `A1/OA/kA` composite-shader parity: final `OA` bloom now uses the source `boolBloom` branch and `uBloomDistortion`, pre-composite `A1` keeps original base/perlin UV separation while routing scene sampling through fluid UVs, the source `mix(uBgColor, sceneMixed, 1.)` stage is restored, the work raw render target keeps a depth buffer like source renderTargetA, and composite uniforms are synchronized before the final pass. |
| `cef11f8` | Home WebGL | Batched source `Se` shared thumb-state ownership parity: project block reveal no longer kills shared thumbnail darkness, darkness-color, or saturation tweens; reveal cleanup stays scoped to project/block-local state; duplicate thumb composite writes were removed from reveal; thumb darkness color now follows the source shared setter path; active block tint, darkness, and contrast transitions remain local to the active project block. |
| `ec296b7` | Home WebGL/DOM | Batched source bootstrap and mobile WebGL lifecycle parity: WebGL initialization now follows source-style capability gating instead of viewport-width gating; reduced-motion no longer removes the GL layer; low-memory/save-data handling remains a low-resolution WebGL path instead of a hard disable; mobile home can initialize the same WebGL scene that `p1.resize` already positions; browser smoke was expanded with a mobile viewport check. |
| `2f46152` | Home DOM/WebGL | Batched source `yD` gallery-out and work-state parity: gallery leave no longer applies non-source fluid-strength or mouse-factor resets, work-state persistence now matches source fields by keeping `sceneRotation` without the local zoom extension, WebGL gallery restore no longer rehydrates a stale zoom offset, and the source `onWorkGalleryOut` reveal-spread/spotlight/project-reveal behavior remains intact. |
| `63d8a3d` | Home WebGL/DOM | Batched source `SD` home-entry spotlight parity: home boot now explicitly initializes the source spotlight position `(0,0,3.7)`, target `(0,0,-8)`, and max intensity before scene reveal; the spotlight projection reveal is reset for the home spotlight-map lifecycle; gallery-state restore now exposes only progress and scene rotation instead of the removed local zoom extension. |
| `d8c95a7` | Home DOM/Audio | Batched source `Tr/Ar` nav interaction parity: desktop and mobile nav links now participate in the Howler hover/click audio path like the source component handlers, the mobile nav toggle gets click audio, delayed mobile nav close state is timer-owned and cleaned up during teardown, and marker QA now expects the added nav click targets. |
| `d7f4dcc` | Project WebGL | Batched source `OD/Se` project-entry visual-state parity: project-route boot now enters source-shaped project visual state without running the full home `setProject()` path, avoiding home thumb/block/spotlight/reveal-spread writes on project pages while preserving project main color, darken, ambient light, media background, saturation, contrast, fluid strength, and media animate-in behavior. |
| `cdaa177` | Home DOM/WebGL | Batched source `SD/yD/Cg` home-gallery entry and input lifecycle parity: gallery entry now emits the current active-project state before restoring/entering WebGL, keyboard and pointer listeners rely on the home component lifecycle instead of duplicate `body.is-home` gates, keyboard next/prev follows source gallery handler timing more closely, and stale local scene-zoom bookkeeping was removed from the DOM controller. |
| `31c5f99` | Home DOM | Batched source `Cg/yD` virtual-scroll input branch parity: home gallery input now follows the source touch-vs-mouse listener split, pointer moves are ignored unless the source-style pointer is active, mouse start/end emit zero-delta gallery scroll events like `Cg`, keyboard next/prev can issue direct gallery steps without the virtual-scroll transition guard, and passive mouse listener options were aligned with the source virtual-scroll implementation. |
| `390fec5` | Home WebGL | Batched source `Se` visual-state responsibility parity: `setMainColor` now stays scoped to DOM `.c-color` updates instead of tinting rebuild-only background, floor, and particle materials; `setAmbientLight` no longer mutates active work-block tint or particle opacity, leaving block coloration to the source-shaped `setBlocksColor` and project-reveal paths while retaining ambient environment uniforms. |
| `42e0d52` | Home WebGL | Refocused active rebuild work on Phase 1 Home WebGL source parity before returning to Phase 2 DOM/interaction cleanup, and removed the rebuild-only work-scene particles layer so the home scene composition stays closer to source `p1`, which adds blocks, floor, environment, about blocks, and floating blocks but no standalone points particle system. |
| `8045258` | Home WebGL | Batched source `Ka/Lu/GA` mouse-simulation sizing alignment: split screen-space composite mouse simulation from cube-local mesh simulation, resized the screen simulation from the render target at source `/10`, resized the cube-local simulation from the source work-grid plane dimensions, and centralized the source `1.3` plane scale plus `1.5` ray expansion constants used by local pointer projection. |
| `ccd9ce1` | Home WebGL | Batched source `SD/p1/GA/Ka` spotlight and pointer-projection cleanup: removed the rebuild-only auxiliary thumb projection overlay path so thumbnail lighting is carried by the cube shader/spotlight-map path, and replaced the global mouse ray plane with per-work-block hidden ray planes so cube-local mouse simulation uses source-style block-local UV intersections. |
| `6675877` | Home WebGL | Batched source `A1/k1/GA` render-target and shader-coordinate alignment: restored A1 pre-composite noise sampling to original `vUv` space instead of fluid-warped UVs, resized the work displacement target to source `k1`'s height-based square target, and scaled the per-block ray-plane z offset through the work-block scale like the source `GA.rotationWrap`. |
| `60ad21f` | Home WebGL | Batched source `Se/h1/u1` environment-state alignment: reshaped the simplified environment shader around source-style `uDarkenColor`, `uDarken`, and shader-pattern constants, and narrowed ambient updates so the environment layer follows `Se.setAmbientColor()`'s darken-color path instead of a rebuild-only ambient-intensity tint. |
| `921efda` | Home WebGL | Batched source `a1/o1/i1` floor-reflector alignment: added a source-style low-resolution floor reflection render target and mirrored camera pass, temporarily hides the floor while rendering the reflection texture, and reshaped the simplified floor shader around source `uReflectivity=.97`, `uMirror=1`, `uFloorMixStrength=15`, normal distortion, and Fresnel-style reflection mixing. |
| `a8f7c2e` | Home WebGL | Batched source `u1/l1/c1` environment-shader alignment: routed the preloaded blue-noise texture into the environment as source-style `tSky`, set repeat wrapping like `Xt.blueNoise`, and reshaped the simplified environment fragment around source two-layer sky UV offsets, `smoothMask` vertical bands, sky-mask modulation, white mix, and `uDarkenColor/uDarken` final blending. |
| `e098f4c` | Home WebGL | Batched source `Lu/kA` bloom-kernel alignment: replaced the rebuild-only fixed `blur13` bloom blur with source-style gaussian blur, and drove the five bloom mips with source kernel/sigma values `[3,5,7,9,11]` during horizontal and vertical blur passes. |
| `6f9c9e8` | Home WebGL | Batched source `A1/C1` pre-composite uniform-shape alignment: added source-compatible disabled media/lensflare/blur uniforms, `uMediaReveal`, `uDisplacement`, `uDisplacementSize`, and `uContainerSize`, and restored the source-position media mix as a default no-op so home behavior stays unchanged while the shader interface matches the original render-manager contract more closely. |
| `4b2c057` | Home WebGL | Batched source `GA/VA` work-block material alignment: reshaped the local cube fragment away from rebuild-only logo/hot-mask amplification, added source-shaped `uDiffuseColor`, `uEmissiveColor`, and `uEmissiveIntensity` uniforms, kept diffuse at source `#808080`, and made `setBlocksColor()` animate the emissive path while preserving the old compatibility uniform. |
| `dfc65e5` | Home WebGL | Batched source `p1/GA` scene-update alignment: restored source `Fog("grey", 0, 100)`, loaded the `/images/cubemaps/01` environment map into the home scene like `p1.addEnvironment()`, and moved per-frame work-block uniforms/mouse textures behind the source-style visibility branch so hidden blocks do not keep running `GA.update()` work. |
| `44f4bb6` | Home WebGL | Batched source `T1/w1/E1` thumb-gallery state alignment: added source-shaped thumb strip state (`progress`, `itemWidth`, `totalItems`, `offsetY`, `isTransitioning`, `xHook`, `yHook`), routed gallery wrapping through the source transition guard, recalculates item width on resize like `w1.calcItemWidth()`, and resets offset/transition state on gallery entry and restore. |
| `789f25e` | Home WebGL | Batched source `p1/Se/VA` light-setter alignment: added real source-style ambient and directional lights to the home scene, made `setAmbientLight()` update the actual ambient light as well as existing simplified material uniforms, made `setDirectionalLightIntensity()` drive the real directional light, and removed the rebuild-only per-block directional-light uniform from the work-block shader path. |
| `a7b665f` | Home WebGL | Batched source `GA/VA` shader-interface alignment: replaced remaining hard-coded work-grid dimensions in the cube fragment with `uGridSize`, added source-compatible `uMouseSpeed`, and damped the mesh mouse-simulation speed with the source `10` factor before writing it to visible work blocks. |
| `9d22513` | Home WebGL | Batched source `OA/kA` composite-interface alignment: renamed the final composite scene input to source-style `tScene`, added source-compatible `tBlur`, `boolFluid`, `boolLuminosity`, and `boolFxaa` uniforms, and synchronized those render-manager flags before the final composite pass while keeping current default behavior unchanged. |
| `b484da5` | Home WebGL | Batched source `Lu/kA/OA` render-manager settings alignment: added a source-shaped home render settings object, routed composite/luminosity/bloom flags and factors through it, kept source bloom kernels `[3,5,7,9,11]`, and made the render loop follow the source settings-gated luminosity/bloom/mousesim branches while preserving current home defaults. |
| `c33641f` | Home WebGL | Batched source `GA/VA/p1` material and spotlight-map alignment: added source standard-material constants for work blocks, reset reveal-side defaults to source `VA` values, reduced non-source thumbnail projection compensation, added a real Three `SpotLight` with `thumbCompositeTarget` as its map, and synchronized spotlight position/target/intensity with the shader path. |
| `a41e642` | Home WebGL | Batched source `VA` fragment-path alignment: treated source standard lighting as the material baseline, added an explicit transitional `uSpotMapIntensity`, reduced thumbnail sampling so it no longer dominates base work-block color/darkness, and kept the source alpha/mouse-lightness/reveal logic as the primary fragment tail. |
| `4faf8c5` | Home WebGL | Batched source `VA extends MeshStandardMaterial` bridge: changed work blocks from local `ShaderMaterial` objects to `MeshStandardMaterial` instances with retained source-shaped custom uniforms, moved source roughness/metalness/emissive/env-map values onto the actual material, and kept `setBlocksColor()` synchronized with the real material emissive channel. |
| `02cca9b` | Home WebGL | Batched source `VA.onBeforeCompile` chunk-injection alignment: replaced the full work-block shader override with MeshStandardMaterial shader chunk injection for `begin_vertex` and `opaque_fragment`, preserving Three standard lighting/env/fog/color-space pipeline while injecting the source displacement, mouse, alpha, reveal, and transitional spotlight-map tail. |
| `d610e58` | Home WebGL | Batched source spotlight-map weighting alignment: confirmed Three's standard material path samples `SpotLight.map`, lowered the transitional fragment-side thumbnail projection intensity, and reduced manual projection/color compensation so projected thumb influence is carried more by the real spotlight-map lighting path. |
| `c2de722` | Home WebGL | Batched source `Ka/sA` mouse-simulation timing and target alignment: simulation render targets now explicitly use no mipmaps, clamp wrapping, and linear filtering like the source ping-pong targets; mouse brush `uTime` is driven by RAF time; and the non-source active diffusion blur path was removed while keeping default behavior stable. |
| `15337ea` | Home WebGL | Batched source `VA` fragment-tail alignment: removed the remaining transitional thumbnail/projection/color compensation from work blocks, deleted the non-source spotlight projection uniforms from the work-block shader interface, kept thumbnail influence on the real Three `SpotLight.map` path, and restored source-shaped fragment alpha/mouse-lightness behavior. |
| `ddf3a49` | Home WebGL | Batched source `VA` alpha-tail parity: restored work-block fragment alpha to use `uReveal * uRevealProject`, `uRevealSides`, and the source `.5` mouse-simulation alpha boost, and removed unused transitional varyings left after the spotlight-map cleanup. |
| `b67b94e` | Home WebGL | Batched source `VA` shader-interface correction: removed the over-broad `uScrollOpacity` addition from ordinary home work blocks, and restored source-shaped `uGridOffset` plus `uMouseSpeed` declarations/uniforms on the standard-material work-block shader path. |
| `a10e685` | Home WebGL | Batched source `A1/OA/Se.showScene` reveal ownership alignment: final `OA` no longer carries non-source `uReveal/uBgColor`; scene reveal now writes the pre-composite `A1/C1`-shaped material like source `Se.showScene`; and the final composite returns to the source darken/bloom/mouse-sim tail. |
| `8dd9e09` | Home WebGL | Batched source `p1/Se/GA/VA` scene and shader-coordinate alignment: restored the source `sceneWrap -> blocksWrap -> GA` object hierarchy with hidden about/floating block containers, added source-shaped `Se` control paths for environment rotation and the second directional-light state without adding non-source lighting, and moved work-block displacement/perlin/mouse/alpha UVs from rebuild-only `instanceGrid` sampling to source-style `instanceOffset`/`newUv` coordinates. |
| `51a23a0` | Home WebGL | Batched source `GA/VA/Se` shader-interface cleanup: restored fragment alpha/vignette UV reconstruction from `vUv / uGridSize + vOffset`, removed the extra vertex-to-fragment `vNewUv` path, aligned vertex displacement/mouse sampling to the source `instanceOffset` baseline, deleted unused rebuild-only work-block uniforms so ordinary `VA` matches the source custom-uniform surface more closely, and removed non-source spotlight intensity clamping so `Se.setSpotLightIntensity()` writes real Three spotlight values. |
| `b102335` | Home WebGL | Batched source `p1/$A/ZA` auxiliary-scene structure alignment: replaced empty hidden about/floating groups with source-shaped instanced block objects, added about circular `23x23x4` rounded-cube layout, added floating `30x30x1` box-grid layout with source-style z drift/hole masking, wired default hidden visibility, resize, update, and disposal lifecycle so the `p1` scene graph now carries the same auxiliary block structure without changing the visible home gallery. |
| `a10e685` follow-up | Home WebGL | Batched source `TD/Fg/p1` route-specific auxiliary-state alignment: added narrow WebGL APIs for about visual entry/leave, wired the about route to activate hidden about/floating blocks, restored source-style spotlight parallax ownership by pausing home spotlight parallax while about blocks are active, moved the spotlight position/target to the about-block-relative coordinates from `TD.updateSpotLight()`, animated about/floating reveals through their material uniforms, and kept project media/home gallery smoke stable. |
| `534c231` | Home WebGL | Batched source `T1/w1/E1/SD` thumb and spotlight ownership alignment: configured thumb render targets with source-style non-mipmapped linear/clamped texture sampling, made the thumb composite material transparent like source `_1/v1`, restored home-entry spotlight map/parallax ownership instead of assigning the map every frame, and moved thumb/displacement offscreen rendering after the main home render so work blocks consume the previous frame's spotlight/displacement textures like the source scene update order. |
| `839e480` | Home WebGL | Batched source `Lu/kA/OA` render-manager pipeline alignment: added source-shaped full-resolution FXAA target/material, optional blur A/B targets and horizontal/vertical blur passes, non-mipmapped linear/clamped render targets for the main composite chain, and a gated home render flow matching source order: scene, optional blur, luminosity, bloom mips, final composite, optional FXAA. |
| `5340d11` | Home WebGL | Batched source `GA/VA/Ka` vertex mouse-coordinate alignment: moved the work-block vertex `newUv`/`mouseUv` baseline to source `vUv / uGridSize + instanceOffset`, replaced hard-coded ray-plane UV offset with the source ray-plane formula, carried `vMouseSim` through the standard-material chunk bridge, and injected the source `transformed /= 1. - mouseSim.r * .2` world-position correction before Three's shadow/spotlight coordinate calculation. |
| `a1bd779` | Home WebGL | Batched source `TD/characterScene` spotlight-map ownership alignment: added a source-shaped offscreen character spotlight render target driven by `public/models/me/model_T.jpg`, renders it through a small character composite pass, sizes it with the square thumb/character target path, and switches the work spotlight map to that target on about entry while home entry keeps restoring the work-thumb target. |
| `51209b6` | Home WebGL | Batched source `Se/A1` media reveal and pre-composite bloom ownership alignment: `setMediaOpacity()` now writes the A1-shaped `uMediaReveal` uniform, the pre-composite shader exposes source-shaped `tBloom`/`boolBloom` plus render-manager bool flags, and the A1 layer samples bloom before contrast like the original composite chain. |
| `7c7d4aa` | Home WebGL | Batched source `I1/C1/A1` and `Lu/kA/OA` bloom-chain separation: added a dedicated half-resolution A1 pre-composite bloom chain fed by `workRawTarget`, kept the existing quarter-resolution OA bloom chain fed by the pre-composited scene, and shared the source gaussian mip/composite helpers without merging their render-target ownership. |
| `cc3c077` | Home WebGL | Batched source `Ka/GA/Lu` mouse-simulation ownership alignment: restored screen-space render-manager `Ka` defaults to `persistance=.75` and `thickness=.25`, kept mesh-local `GA` simulation on `.85/.1`, stopped global pointer movement from writing mesh-local UVs directly, and left `mouseFactor` scoped to the `VA` shader path instead of changing the simulation brush. |
| `efdefaf` | Planning | Documented the Phase 1 source-parity audit, completion criteria, remaining must-fix areas, risk tiers, and recommended batch strategy. |
| `5d1f1b1` | Home WebGL | Batched source `GA/Ka` local simulation ownership alignment: each work item now owns its own local mouse simulation material, scene, ping-pong targets, UV target state, and mouse-speed state; ray-plane hits update the matching work item while shared screen-space `tMouseSim2` remains render-manager owned. |
| `863fcd9` | Home WebGL | Batched source `A1/OA` blend-call alignment: A1 perlin/background blend and OA darken/lighten now call a source-shaped `sourceBlend(mode, ...)` dispatcher for the modes used by the original shaders (`1`, `11`, `15`) instead of direct local helper calls. |
| `57ff4bc` | Home WebGL | Batched source `Se` setter ownership alignment: `showScene()` follows source state tween behavior, `darken/saturation/contrast/media background/directionalLight2` now tween local source-shaped state before writing uniforms/lights, and composite defaults read from those state fields. |
| `be67712` | Home WebGL/Project WebGL | Batched source `OD/Se` project-entry order alignment: project WebGL visual init now resets media opacity to `0`, media reveal is deferred to the page-enter boundary like source `OD.animateIn`, and home `setProject()` no longer carries a project-page media reveal side effect. |
| `1a4c280` | Home WebGL | Batched source `SD/TD/Fg` entry-timing alignment: home `showScene()` and gallery entry now wait for the page-enter boundary like source `SD.animateIn`, and about auxiliary visual setup is separated from `TD/Fg` reveal animation so about blocks reveal at page enter instead of during WebGL boot. |
| `a6fdde2` | Home WebGL | Batched source `TD/Fg` leave/destroy timing alignment: about route links now trigger auxiliary reveal-out before navigation, while page cleanup uses immediate destroy-style track/visibility/parallax reset instead of running a long leave animation during teardown. |
| `64867c8` | Home WebGL | Batched source `BD/zD/yD` home-route leave alignment: internal home links outside the work gallery now route through the work-gallery-out transition so header/nav leaves run the same soft-woosh, scene fade, reveal-spread, spotlight fade, and block reveal-project animation path as CTA leaves. |
| `33cdcdd` | Home WebGL | Batched source `OA/C1/Lu` material-flag alignment: final `OA` keeps source-owned `uDarken/uSaturation` and now uses source-shaped `toneMapped:false`, `transparent:true`, and normal blending; A1/C1 pre-composite and Lu screen-pass materials now explicitly align proven tone/blending flags. |
| `2eda7e7` | Home WebGL | Batched source `VA` material-flag audit: work and auxiliary block materials now set source `dithering=true`, while full shader override remains a documented high-risk deviation because chunk injection preserves current Three lighting/color-space/fog/env/spotlight compatibility. |
| `0cf78b1` | Home WebGL | Batched source `TD/Fg/ZA` auxiliary scroll alignment: floating blocks now accumulate source-style scroll-velocity z drift while visible on the about route, with entry/destroy resets to prevent cross-route carryover. |
| `6b8c626` | Home WebGL | Batched source `characterScene/TD` spotlight-map alignment: about spotlight now renders `public/models/me/me.gltf` into a dedicated character target with camera/lights and keeps the previous `model_T.jpg` composite plane as a fallback. |
| `uncommitted` | Home WebGL | Matched two source-confirmed home background pieces from `p1/a1/V1/H1`: the floor now uses source-style projector coordinates, opaque reflector sampling, and oblique clipping instead of rebuild-only transparent UV reflection; sky composite noise now uses the source `noiseShader`/`pattern`/weighted-FBM chain. Build and home source-vs-rebuild smoke passed. Visual comparison confirms the remaining "fog bed" gap is mainly `h1/u1/l1` environment material parity, not floor alpha. |
| `current batch` | Phase 1 Audit/QA Harness | Added configurable source comparison captures, recorded the first full post-preloader difference table, and rejected two non-source brightness/reveal experiments: `VA.envMapIntensity` remains `.75`, and scene reveal remains owned by `C1/A1` rather than final `OA`. |
| `current batch` | Home WebGL | Aligned source runtime color semantics for `Se`-owned setters by parsing project colors as raw `sr()` RGB channels, and matched the source thumb-composite `toneMapped:false` flag. Full QA passed, but the luma gap remains, pointing the next batch toward `VA` full shader/lighting and render-target color-space behavior. |
| `uncommitted` | Phase 1 Audit | Added a next-difference attribution table tying the remaining brightness gap to source `V1/H1/z1/B1` sky render-target ownership, `h1/u1/l1` environment material parity, `VA` shader risk, render-target color-space assumptions, and project-page regression checks. |
| `uncommitted` | Home WebGL | Implemented the source-shaped `V1/H1/z1/B1` sky render-target bridge and moved environment `tSky` from raw blue-noise to the sky composite target; also removed the rebuild-only low-alpha environment band. Full QA passed, but home luma only improved slightly, so the next batch should move to `VA` fragment/tone-mapping or render-target color-space assumptions. |
| `uncommitted` | Home WebGL | Tightened the `VA` chunk-injection bridge by sharing one work/aux shader patch, aligning the source fragment `tDisplacement` surface, and removing Three 0.184 output tail chunks that the source `VA` omits. Full QA passed, but luma remained unchanged, so the next batch should prioritize render-target/color-space assumptions before a full shader replacement. |
| `current batch` | Home WebGL | Aligned source `h1/u1/i1` background ownership: environment now uses `IcosahedronGeometry(300,10)` with `BackSide` MeshStandardMaterial, source-style full `c1/l1` shader replacement, and floor reflector output now runs the source two-pass blur target path. Build, diff check, and home capture passed. The previous wedge-shaped bright mark disappeared, confirming it was a rebuild artifact rather than source intent. The remaining issue is now overbright/hard horizon output, pointing at render-target/color-space/composite input rather than more environment geometry tuning. |
| `current batch` | Home WebGL | Restored source `nD -> U1/C1` texture wiring by feeding the main pre-composite from `workScene.renderManager.renderTargetComposite.texture` instead of raw work scene output, and aligned renderer construction with source `qw` (`antialias:false`, `depth:false`, `stencil:false`, `preserveDrawingBuffer:false`). Build, diff check, home capture, and full capture passed. Home output is materially closer and no longer overexposes, while the remaining hard horizon/reflection boundary is still open. |
| `current batch` | Phase 1 Audit | Audited source `a1/i1/kA` floor/reflection defaults against the current rebuild. Confirmed source `i1.setSize()` renders reflection targets at `0.75 * renderSize` while its blur material keeps full render-size `uResolution`. Corrected the earlier `kA/Lo` depth reading: source `Lu.initRenderer()` creates `renderTargetA` from a no-depth target but then explicitly sets `renderTargetA.depthBuffer = true`; because `kA` extends `Lu`, the work scene primary target should keep depth. The rejected `workRawTarget.depthBuffer=false` trial was therefore not source-correct for `kA`.
| `current batch` | Home WebGL | Moved floor-reflection rendering into the floor mesh `onBeforeRender` lifecycle, matching source `a1.init()` ownership where the floor hides itself, updates its reflector, then resumes rendering. This keeps the previously stable camera/target settings but removes the rebuild-only eager reflection pass from the main tick. Build, diff check, and full capture passed with no project media regressions. |
| `current batch` | Home WebGL | Ported source `i1.update()` reflector camera math inside the accepted floor `onBeforeRender` lifecycle: reflection now derives plane normal, camera position, reflected view, reflected target, up vector, and oblique clip plane from `matrixWorld` instead of the rebuild-only y-axis mirror approximation. Build, diff check, home capture, and full capture passed. Visual output is stable; remaining horizon gap likely needs target sizing/blur and `kA/Lo` render-target ownership to be moved together. |
| `current batch` | Home WebGL | Re-applied source `i1.setSize()` blur-resolution behavior after the lifecycle/camera-math port: reflection targets remain `0.75 * renderSize`, while the blur material now receives full render-size `uResolution`. Build, diff check, home capture, and full capture passed. The hard horizon is not solved, but this source value no longer causes the earlier white-floor regression in the accepted lifecycle context. |
| `current batch` | Phase 1 Audit | Audited source `Lu.update()` pass clearing against the rebuild. Source render-manager passes rely on `autoClear=false` and do not explicitly call `renderer.clear()` before each work-scene render/bloom/composite target, but a narrow rebuild trial that removed those clears from only the work-scene path produced no visible improvement and added local complexity. The trial was reverted; any future no-clear alignment should come from porting the complete `Lu/kA` render-manager structure, not from ad hoc per-pass switches. |
| `current batch` | Home WebGL | Aligned two source-proven `GA/VA/Xt` details: ordinary work-block spread now uses source `float spread = 3.` instead of the rebuild-only `5.0`, and `VA.tPerlin` now loads `/images/textures/perlin-1.webp` with ClampToEdge wrapping while the A1/C1 pre-composite keeps `/images/textures/perlin-2.webp` with Repeat wrapping. Build, diff check, and full source-vs-rebuild capture passed with no network/runtime failures across home desktop/mobile, about, `/gc-2026/`, and `/hashgraph-vc/`. |
| `current batch` | Phase 1 Audit | Rechecked likely-but-false runtime suspects against the source bundle: `directionalLight2` is intentionally not added to `p1.scene`, environment `skyMask2` uses `skyMask` in source, sky/environment mix constants are split across source constant groups, work-scene DPR remains capped at `1.5`, and `LOW_RES` still needs a source-equivalent GPU-tier bridge before changing runtime behavior. No rendering code changed. |
| `current batch` | QA Harness | Corrected the stale brightness-matrix pass-order variant from the no-op `debug-pass-order=source-work-composite` to the real `debug-pass-order=raw-work-composite` fallback, and added `settings.passOrder` to the debug output probe. The current matrix confirms production is already using the source-shaped `workRaw -> OA/CA workComposite -> A1/C1` path; the raw fallback brightens A1 input but is not source-shaped. |
| `current batch` | Phase 1 Audit/QA Harness | Expanded the current `GA/VA` + `OA/CA` attribution batch to cover multiple related non-fixes in one cycle, and changed `scripts/compare-home-brightness-attribution.mjs` to print a compact luma matrix while keeping full JSON on disk. Current evidence keeps `spotlight-map-off`, `spotlight-transfer`, `scene-transfer`, `raw-work-composite-pass`, `va-physical-*`, `sky-off`, `texture-srgb-colorspace`, and `darken-off` as diagnostics only; no production rendering code changed. |
| `current batch` | Home WebGL | Aligned source `i1/a1` reflector render-state ownership by disabling XR and shadow auto-update during the floor reflection pass, forcing the reflection depth mask writable, and restoring renderer state afterward. Also expanded the output probe with floor/environment uniforms and target sizes so the remaining hard-horizon gap can be attributed without brightness tuning. |
| `current batch` | Home WebGL/QA Harness | Aligned `p1.addEnvironment()` cubemap ownership by leaving CubeTexture color space to the loader like the source bundle, added scene-environment and floor-reflection target metadata to the output probe, and added `scripts/analyze-home-bands.mjs` for reusable horizontal-band screenshot analysis. Source-vs-rebuild band analysis shows this is source-correct but not the main hard-horizon fix, so the next batch should inspect reflection/environment content around the mid-page boundary. |
| `current batch` | Home WebGL | Split source work/main render-manager ownership: work keeps `kA/OA` bloom/luminosity/mousesim, while main follows default `I1/Lu` with bloom/luminosity/mousesim off and renders the `A1` pre-composite directly to screen. Removed the non-source final reuse of `OA` as a screen darken pass and stopped running the unused extra preBloom production pass. QA stayed clean, but screenshot band analysis shows the mid-screen hard boundary remains exposed, so the next batch should diagnose floor/environment/A1 input composition rather than reintroducing final darkening. |
| `current batch` | Home WebGL/DOM | Aligned source `p1/IT/yD` camera-controller ownership: base home visual preparation now keeps `p1` defaults `targetXY=(.25,.25)` and `rotateAngle=10`, while work-gallery entry applies source `yD.animateIn()` settings `targetXY=(1,.5)` and `rotateAngle=20`. The home gallery entry lifecycle now waits for WebGL before marking scroll active, so the source WebGL entry state is not skipped. Build, diff check, output probe, and full capture passed; this fixes attribution, not the remaining hard horizon. |
| `current batch` | Home WebGL | Audited the remaining hard-horizon candidates across source `l1/u1/h1`, `V1/H1/z1/B1`, `A1/C1`, and `a1/o1/i1`. Only one source-backed runtime difference was promoted: environment `u1` no longer forces the rebuild-only `toneMapped:false` material flag. Build, diff check, output probe, and home capture passed, but band analysis shows the mid-screen boundary remains, so the next batch should target render-target/color-transfer interpretation or generated shader diffs rather than more formula-level environment/floor tweaks. |
| `current batch` | Phase 1 Audit/QA Harness | Expanded the next source-difference audit across `VA`, spotlight/thumb transfer, and composite stages without promoting unsupported visual shortcuts. Added focused `VARIANTS` support to the brightness attribution script and compact output for composite-stage diagnostics. Evidence keeps `debug-va-*`, `debug-spotlight-map-transfer=srgb`, and `debug-composite-transfer=*` as diagnostics only until source-backed render-target/color interpretation is found. |
| `current batch` | Home WebGL | Promoted two source-confirmed `VA/HA` vertex differences under Three r164: work-block vertex UVs now start from `gl_Position.xy / uCoords.xy`, and mouse displacement uses the source world-position division path. Query-only fallbacks remain for attribution. |
| `current batch` | Home WebGL | Batched source `VA/zA` fragment-tail alignment: ordinary work blocks now default to source-style `opaque_fragment -> gl_FragColor.rgb/a` mutation, keep a query-only compat fallback, remove auxiliary reveal/scroll branches from ordinary `VA`, split auxiliary-only fragment uniforms into the auxiliary bridge, and restore the source ordinary `uTime` fragment surface. |

## Current Focus

Finish Phase 1 Home WebGL source parity before returning to Phase 2 DOM parity. Phase 1 is not complete: the previous closeout language in `PHASE1_AUDIT.md` has been superseded because the current 1:1 target does not allow known source mismatches to be treated as accepted deviations.

Current governance note: do not mark remaining `VA` bridge, spotlight-map transfer, final `OA/CA` transfer, project-media brightness, floor/environment, or about bridge depth as accepted deviations. If no source-backed runtime fix is found in a batch, update the audit with the evidence and keep Phase 1 open. Visual review can identify mismatches and regressions, but it cannot close a known source mismatch.

Immediate source targets:

- `V1/H1/z1/B1` and `h1/u1/l1`: source-shaped sky render-target ownership is implemented, the source `noiseShader`/`pattern` chain is ported, and `h1/u1` now uses the source icosahedron/backside/full-shader structure. The old wedge-shaped light was confirmed as a rebuild artifact. The current source-vs-rebuild screenshots now show full-screen fog coverage but rebuild is overbright and has a hard horizon/reflection boundary, so the next edit should target render-target/color-space/composite input instead of more environment geometry or invented color tuning.
- `p1`: validate route-specific visibility and spotlight-map ownership for about/floating blocks, now that the source-shaped auxiliary block objects can be activated on the about route and the about spotlight map renders the local GLTF character target.
- `GA/VA`: active priority. Ordinary work blocks now use direct source-shaped `HA/zA` templates, MeshStandardMaterial state, a real spotlight-map path, source-style `instanceOffset` sampling, source vertex mouse UVs, source world-position correction, per-work-item local `Ka` simulation targets, source material flags, source-shaped output tail, source `spread = 3.`, source `Xt.perlin1` work-block texture ownership, and restored source `SPECULAR` with inactive `PHYSICAL` proof. The remaining ordinary-work bridge is `uUvOffset vec2`; cube/thumb projection and material feel still need source-backed work, so continue from spotlight/thumb transfer/content evidence or `OA/CA` render-target attribution rather than accepting the bridge visually.
- `T1/w1/E1`: thumbnail scene, render target sizing, thumb strip progress, and spotlight-map texture path; the current pass now matches source-style render target sampling and previous-frame feedback order more closely.
- `A1/OA/kA/Lu`: pre-composite, final composite, bloom chain, settings-gated render-manager ordering, fluid/mouseSim inputs, render target color-space assumptions, and remaining optional blur/fxaa behavior; the current pass now separates the source `I1/C1/A1` half-resolution bloom chain from the source `Lu/kA/OA` quarter-resolution final bloom chain, routes active blend modes through source-shaped mode dispatchers, aligns proven material tone/blending flags, removes the rebuild-only `A1` final reveal mix, initializes source `C1.uBgColor` with the source linear-to-sRGB path, restores source `workScene.renderTargetComposite -> mainScene.C1.tWork` wiring, aligns renderer construction with source `qw`, enables a source-shaped main `I1/ag` fluid pass for the normal high-tier path, and uses source fullscreen-triangle screen geometry for render-manager postprocess passes. The main brightness gap improved earlier, but the hard horizon/reflection boundary remains the next output-flow issue. `workRawTarget.depthBuffer=true` is now confirmed source-correct for `kA/Lu`.
- `Ka`: low-resolution mouse simulation sizing, pointer projection, persistence/thickness, and screen-vs-local simulation feeds; the current pass now gives visible work items source-shaped local simulation ownership instead of sharing one mesh-local buffer.
- `Se`: source-style visual-state setter ownership without non-source side effects; core setter state ownership plus project/home/about entry timing, about leave/destroy cleanup, and home route leave dispatch are now closer and pending browser route QA.

Current non-fix notes:

- Do not add `directionalLight2` to the home scene; source creates it but only adds `directionalLight`.
- Do not rewrite the environment `skyMask2 = max(skyMask, ...)` line; source uses the same expression.
- Do not change work-scene DPR solely because source `Pe.dpr` can be `2`; source `p1.resize()` clamps the work scene to `1.5`.
- Do not change `sourceLowRes()` heuristics until there is a source-equivalent GPU-tier bridge.
- Do not use `debug-pass-order=raw-work-composite` as a production brightness fix. It is now the explicit non-source fallback diagnostic; default production already reports `settings.passOrder = source-work-composite`.

Latest verification:

- `ASTRO_TELEMETRY_DISABLED=1 npm run build`
- `git diff --check`
- Full source-vs-rebuild capture output: `/tmp/rogier-phase1-ga-va-perlin`
- Home dist markers: `data-project-card=10`, `data-sound-click=30`, `data-webgl-root=1`, `ui-work-container=1`
- Project dist markers on `/gc-2026/`: `data-media-src=5`, `data-mobile-media=5`, `data-webgl-project=1`
- Browser WebGL smoke with Chrome `--use-gl=swiftshader --enable-unsafe-swiftshader`: home, about, `/gc-2026/`, and `/hashgraph-vc/` initialized WebGL, displayed visible full-viewport `.gl-canvas` screenshots, reported no failed network requests, and retained the expected home/project markers.
- About character resources loaded during WebGL smoke: `/models/me/me.gltf`, `/models/me/me.bin`, and `/models/me/model_T.jpg`.
- Source-vs-rebuild visual QA now has a QA-only content JSON fallback: run the mirror service with `ENABLE_CONTENT_JSON_FALLBACK=1` so the original bundle's missing `/src/content/**` and `/opt/build/repo/src/content/**` collection JSON requests are synthesized from `src/data/projects.json` and `src/data/awards.json`.
- With that fallback and Chrome SwiftShader, the mirrored original naturally reaches post-preloader state on home and `/gc-2026/`; home and project both report full-viewport canvases and no runtime exceptions in the diagnostic pass.
- A longer full pass with `CAPTURE_WAIT=4200` and `CAPTURE_SET=full` captured home desktop/mobile, about, `/gc-2026/`, and `/hashgraph-vc/` for both original and rebuild. Both sides reached post-preloader full-canvas states with no runtime exceptions. The contact sheet at `/tmp/rogier-compare-phase1-full/contact.png` shows the main remaining Phase 1 differences: original home cubes/thumb media are much brighter and more present than rebuild; project media exists but the rebuild composite/background is darker. An 8s home-only pass did not remove the home brightness gap.
- Source audit correction from this pass: `VA.envMapIntensity` is `.75` in the source and should stay `.75`; `uReveal` belongs to the source `C1/A1` pre-composite material, while `OA` final composite has no `uReveal`.
- Latest full QA after the S1-16 `A1/C1` shared-composite cleanup: `CAPTURE_WAIT=4200 CAPTURE_SET=full` wrote screenshots to `/tmp/rogier-compare-phase1-a1-shared`. Home desktop/mobile, about, `/gc-2026/`, and `/hashgraph-vc/` reached post-preloader full-canvas states with no network failures or runtime exceptions. Dist markers stayed stable. Luma remains far lower in rebuild (`home desktop 0.011` vs original `0.104`; `/gc-2026/` `0.039` vs original `0.140`), so the next batch should not keep tweaking `A1` reveal/color defaults.
- Latest home background QA after the source floor/sky cleanup: `CAPTURE_WAIT=5200 CAPTURE_SET=home` wrote screenshots to `/tmp/rogier-sky-source-noise`. Both original mirror and rebuild reached full-canvas home desktop/mobile with no failed network requests or runtime exceptions. The source screenshot confirms the missing "fog bed" is a broad blue environment field; rebuild still only shows a smaller dark floor/reflection patch, so the next source target is `h1/u1/l1` environment material parity.
- Latest home background QA after the source `h1/u1/i1` environment/reflection cleanup: `CAPTURE_WAIT=5200 CAPTURE_SET=home` wrote screenshots to `/tmp/rogier-env-fullshader`. Both original mirror and rebuild reached full-canvas home desktop/mobile with no failed network requests or runtime exceptions. Rebuild now has full-screen environment coverage and the wedge-shaped bright artifact is gone, but the image is overbright and still shows a hard horizontal/reflection boundary compared with the source's darker blue fog field. This makes render-target color space, renderer output, and `Lu/kA/OA` input flow the next source-driven target.
- Latest QA after restoring source `workScene.renderTargetComposite -> mainScene.C1.tWork` wiring and source renderer flags: home capture wrote screenshots to `/tmp/rogier-source-work-composite-default`; full capture wrote screenshots to `/tmp/rogier-work-composite-full`. Home/about/`gc-2026`/`hashgraph-vc` all reached full-canvas states with no failed network requests or runtime exceptions. Home is materially closer and no longer overexposed, validating the source render-target wiring. The remaining visible gap is the hard horizontal floor/environment/reflection boundary and less source-like dark cube/fog depth.
- Latest floor/reflection audit QA: isolated source trials wrote screenshots to `/tmp/rogier-floor-blur-resolution`, `/tmp/rogier-source-reflector-update`, and `/tmp/rogier-source-work-depthless`. Build and `git diff --check` passed during the trials, and full capture for the depthless target had no runtime/network failures, but the visual output regressed: reflector/blur changes left the colored hard horizon intact, and `workRawTarget.depthBuffer=false` produced an overbright white floor in the current rebuild. The later source audit confirmed that `workRawTarget.depthBuffer=false` was not source-correct for `kA/Lu`, because source `Lu.initRenderer()` ends with `renderTargetA.depthBuffer = true`.
- Latest accepted floor ownership QA: after moving reflection rendering into `floorPlane.onBeforeRender`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, and `CAPTURE_WAIT=5200 CAPTURE_SET=full` passed. Screenshots were written to `/tmp/rogier-floor-onbeforerender`; home/about/`gc-2026`/`hashgraph-vc` all reached full-canvas states with no failed network requests or runtime exceptions. The visual output is stable rather than solved, so the next floor work should move the reflector child/camera math closer to source `i1` within this now-correct lifecycle.
- Latest accepted reflector-camera QA: after porting source `i1.update()` matrix/normal/view/target/up camera math within `floorPlane.onBeforeRender`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, home capture, and full capture passed. Screenshots were written to `/tmp/rogier-floor-source-camera-lifecycle` and `/tmp/rogier-floor-source-camera-full`; home/about/`gc-2026`/`hashgraph-vc` all reached full-canvas states with no failed network requests or runtime exceptions.
- Latest accepted reflector blur-resolution QA: after restoring source `i1.setSize()` blur resolution in the accepted floor lifecycle, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, home capture, and full capture passed. Screenshots were written to `/tmp/rogier-floor-source-blur-resolution-retest` and `/tmp/rogier-floor-source-blur-resolution-full`; home/about/`gc-2026`/`hashgraph-vc` all reached full-canvas states with no failed network requests or runtime exceptions.
- Latest work render-manager clear audit: a narrow no-clear trial for the work-scene raw/bloom/composite path wrote screenshots to `/tmp/rogier-work-manager-no-clear`. Build, `git diff --check`, and home capture passed, but the visual result was effectively unchanged, so the trial patch was reverted and only the source finding was documented.
- Latest main fluid alignment QA: after enabling the source `I1` main fluid path and adding the source-shaped `ag` pass, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, focused output probes, default brightness attribution, home capture/band analysis, and full capture passed. Probe outputs were written to `/tmp/rogier-main-fluid-probe-default-2`, `/tmp/rogier-main-fluid-probe-off-2`, `/tmp/rogier-main-fluid-attribution`, `/tmp/rogier-main-fluid-home`, and `/tmp/rogier-main-fluid-full`. The debug-off probe now works because `scripts/probe-output-color.mjs` correctly merges query parameters. This closes the explicit main-fluid settings divergence but does not solve the remaining hard horizon/fog-bed or final transfer/color gap.
- Latest fullscreen-triangle QA: after replacing render-manager screen-pass quads with the source fullscreen triangle, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, default brightness attribution, home capture/band analysis, and full capture passed. Outputs were written to `/tmp/rogier-fullscreen-triangle-probe`, `/tmp/rogier-fullscreen-triangle-attribution`, `/tmp/rogier-fullscreen-triangle-home`, and `/tmp/rogier-fullscreen-triangle-full`. The change is source-correct and stable, but the hard horizon/fog-bed and transfer/color gaps remain open.
- Latest floor-normal matrix QA: after moving floor normal tiling from rebuild-only `uNormalScale=(45,45)` into the source texture matrix path `repeat=(45,45)` with material `uNormalScale=(1,1)`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, home capture/band analysis, and full capture passed. Outputs were written to `/tmp/rd-floor-probe`, `/tmp/rd-floor-home`, and `/tmp/rd-floor-full`. The probe confirmed `floor.uNormalScale=[1,1]`; project page content remained present. This is a source-structure fix, not a Phase 1 visual closeout.
- Latest home spotlight target QA: after restoring source home spotlight target ownership to the default origin and adding spotlight position/target/parallax probe fields, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, home capture/band analysis, and full capture passed. Outputs were written to `/tmp/rd-home-spot-target-probe-2`, `/tmp/rd-home-spot-target-home`, and `/tmp/rd-home-spot-target-full`. The probe confirmed `position=[0,0,3.7]`, `target=[0,0,0]`, `parallax=true`, `hasMap=true`, and five in-map samples. The hard horizon/fog-bed gap remains open.
- Latest Three r164 QA: after pinning `three` and `@types/three` to `0.164.1` to match the source bundle's Three generation, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, home capture/band analysis, full capture, and a final output probe passed. Outputs were written to `/tmp/rd-three-r164-probe`, `/tmp/rd-three-r164-home`, `/tmp/rd-three-r164-full`, and `/tmp/rd-three-r164-final-probe`. The r164 runtime materially changes work/light target luma and removes a major non-source variable, but the hard horizon/fog-bed gap remains open.
- Latest VA vertex QA: after promoting the source `HA` early screen-UV expression and source world-position mouse displacement path under Three r164, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump/probe, home capture/band analysis, and full capture passed. Outputs were written to `/tmp/rd-va-r164-source-vertex-shader`, `/tmp/rd-va-r164-source-vertex-probe`, `/tmp/rd-va-r164-source-vertex-home`, and `/tmp/rd-va-r164-source-vertex-full`. Shader dump confirmed the source vertex paths are active; full capture reported no failed requests or runtime exceptions for home, about, `/gc-2026/`, and `/hashgraph-vc/`. Band analysis still shows rebuild mid-field luma above source (`desktop center 0.3105 vs 0.2276`, `mobile center 0.3752 vs 0.2168`), so Phase 1 remains open.
- Latest VA fragment-tail QA: after promoting the source `zA` fragment tail and ordinary fragment surface cleanup, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump/probe, home capture/band analysis, and full capture passed. Outputs were written to `/tmp/rd-va-fragment-tail-shader`, `/tmp/rd-va-fragment-tail-probe`, `/tmp/rd-va-fragment-tail-home`, and `/tmp/rd-va-fragment-tail-full`. Shader dump confirms `gl_FragColor.rgb`/`.a` mutation is active, `vec4(sourceColor)` is gone, ordinary `VA` has no auxiliary/scroll uniform residuals, and `uniformsOnlySource` is now empty. Full capture reported no failed requests or runtime exceptions for home, about, `/gc-2026/`, and `/hashgraph-vc/`. Band analysis still shows rebuild mid-field luma above source (`desktop center 0.3136 vs 0.2279`, `mobile center 0.3779 vs 0.2164`), so this is shader parity progress, not a Phase 1 closeout.
- Latest main `Lu/lA` composite QA: after adding the source default main composite pass after `A1/C1`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, home capture/band analysis, and full capture passed. Outputs were written to `/tmp/rd-main-composite-probe`, `/tmp/rd-main-composite-home`, and `/tmp/rd-main-composite-full`. Full capture reported no failed requests or runtime exceptions for home, about, `/gc-2026/`, and `/hashgraph-vc/`; project-page content and canvases remained present. Band analysis stayed essentially unchanged (`desktop center 0.3136 vs source 0.2280`, `mobile center 0.3799 vs source 0.2175`), which matches the source structure because `I1/Lu` has `boolBloom=false` and main fluid is nearly static at rest. This is structural source parity, not a brightness closeout.
- Latest media render-manager QA: after restoring the source-shaped `$1/j1/W1` media raw/composite target path and moving main composite execution outside the home-only branch, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, focused project media scroll probes, and full capture passed. Outputs were written to `/tmp/rd-media-composite-full-2`; project scroll probes confirmed `/gc-2026/` and `/hashgraph-vc/` retain 5 desktop media tracks and 5 mobile media fallbacks each, `uMediaReveal=1`, and non-zero WebGL media target content after scrolling to the media region. Full capture again reported no failed requests or runtime exceptions for home, about, `/gc-2026/`, and `/hashgraph-vc/`.
- Latest shader residual audit: after expanding the shader dump harness, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, and full capture passed. Outputs were written to `/tmp/rogier-phase1-shader-dump-s61b`, `/tmp/rogier-phase1-shaderdump-probe`, and `/tmp/rogier-phase1-shaderdump-full`. Source color setter semantics were confirmed as already source-shaped. The dump now shows `OA/kA` tonemapping/transfer and render-target color interpretation as the next source-backed audit target; no production brightness constants were changed.
- Latest `OA`/thumb tonemapping-surface QA: after restoring source tonemapping include pairs to `homeCompositeFragment` and `thumbCompositeFragment`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, and full capture passed. Outputs were written to `/tmp/rogier-phase1-oa-tonemap-shader`, `/tmp/rogier-phase1-oa-tonemap-probe`, and `/tmp/rogier-phase1-oa-tonemap-full`. The dump confirms the `fragmentIncludesOnlySource` residual is cleared for `OA-work-composite` and `x1-thumb-composite`; because both source materials are `toneMapped:false`, this is source-surface parity rather than a visual transfer fix.
- Latest pre-bloom cleanup QA: after removing the unused rebuild-only `preBloom*` branch between work composite and `A1/C1`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, and full capture passed. Outputs were written to `/tmp/rogier-phase1-remove-prebloom-probe` and `/tmp/rogier-phase1-remove-prebloom-full`. The active work bloom and disabled source-default main bloom remain intact; the output probe no longer reports non-source `preBloom*` targets.
- Latest `I1/C1` render-manager surface QA: after adding the source default-disabled main lensflare settings/target, routing `C1.tLensflare` to the full-resolution target, adding source-surface `tScene`/`tPortal` uniforms, using explicit main bloom factors, and restoring source `I1` half-POT main bloom sizing, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, and full capture passed. Outputs were written to `/tmp/rogier-phase1-i1-surface-probe` and `/tmp/rogier-phase1-i1-surface-full`; the probe confirmed `lensflareEnabled=false` and a full-resolution lensflare target.
- Latest `VA/A1` shader-surface QA: after restoring the source-shaped work-block `mouseSim` world-position expression, removing production `debug-va-*` and `debug-spotlight-map-transfer` shader fallback branches, deleting obsolete comparison scripts, and adding the missing `A1.tScene` shader declaration, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, and full capture passed. Outputs were written to `/tmp/rogier-phase1-va-surface-shader-3`, `/tmp/rogier-phase1-va-surface-probe`, and `/tmp/rogier-phase1-va-surface-full`; shader dump confirms `A1-pre-composite.fragmentUniformsOnlySource=[]` and the source `transformed /= 1. - mouseSim.r * .2` expression is active.
- Latest sky/environment shader-surface QA: after restoring source-declared `V1/H1/z1/B1` sky uniforms, the sky `tonemapping_fragment` tail, source-declared `h1/u1/l1` environment uniforms, per-frame environment `uTime`, and expanded sky/environment probe metadata, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, and full capture passed. Outputs were written to `/tmp/rd-sky-env-surface3-shader`, `/tmp/rd-sky-env-surface3-probe`, and `/tmp/rd-sky-env-surface3-full`; shader dump confirms `u1-environment.fragmentUniformsOnlySource=[]` and `fragmentUniformsOnlyRebuild=[]`. This is source-surface parity, not a Phase 1 visual closeout.
- Latest thumb-composite uniform QA: after renaming `x1/_1/v1` thumb composite uniforms from rebuild-only `uDarkness/uDarknessColor` to source `uDarkenIntensity/uDarkenColor`, updating setters/tweens/probes, and keeping compatibility probe aliases, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, and full capture passed. Outputs were written to `/tmp/rd-thumb-uniform-shader`, `/tmp/rd-thumb-uniform-probe`, and `/tmp/rd-thumb-uniform-full`; shader dump confirms `x1-thumb-composite.fragmentUniformsOnlySource=[]` and `fragmentUniformsOnlyRebuild=[]`. This improves thumb/spotlight attribution but is not a Phase 1 visual closeout.
- Latest media/main composite QA: after aligning `$1/j1/W1` project media composite, `UD/FD/LD` project media surface, `I1/Lu/aA` settings, source shader mapping, and the final `Lu/aA` uniform surface, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, project media probe, and full capture passed. Outputs were written to `/tmp/rd-media-surface-full`, `/tmp/rd-main-settings-full`, and `/tmp/rd-main-composite-surface-full`; project media probes confirmed `/gc-2026/` and `/hashgraph-vc/` keep five visible media tracks and non-zero media render targets. The next batch should stay on `Lu/kA/OA/I1` output-transfer or render-target content attribution rather than changing visual constants.
- Latest `OA/CA` production-surface QA: after moving `uBloomDistortion` back to the source local constant, removing rebuild-only debug uniforms from the default production work composite, and compiling the composite-stage diagnostics only behind explicit `debug-composite-*` query parameters, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, project media probe, composite-stage diagnostics, and full capture passed. Outputs were written to `/tmp/rd-oa-prod-surface-shader`, `/tmp/rd-oa-prod-surface-probe`, `/tmp/rd-oa-prod-surface-project-media`, `/tmp/rd-oa-prod-surface-composite-stages`, and `/tmp/rd-oa-prod-surface-full`; shader dump confirms `OA-work-composite.fragmentUniformsOnlySource=[]` and `fragmentUniformsOnlyRebuild=[]` for the default shader.
- Latest shared helper QA: after aligning source `Ur` saturation weights in `C1/A1`, `CA/OA`, and `v1/x1`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, project media probe, and full capture passed. Outputs were written to `/tmp/rd-saturation-helper-shader`, `/tmp/rd-saturation-helper-probe`, `/tmp/rd-saturation-helper-project-media`, and `/tmp/rd-saturation-helper-full`. This is a source helper correction, not a Phase 1 visual closeout.
- Latest bloom-target reuse QA: after matching source `Lu.update()` by rendering bloom composite output into `renderTargetsHorizontal[0]` and routing work/main `tBloom` through that reused target instead of standalone rebuild-only bloom targets, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, project media probe, and full capture passed. Outputs were written to `/tmp/rd-bloom-reuse-shader`, `/tmp/rd-bloom-reuse-probe`, `/tmp/rd-bloom-reuse-project-media`, and `/tmp/rd-bloom-reuse-full`. This removes a render-manager ownership drift but does not close Phase 1 visually.
- Latest floor/environment attribution QA: after adding debug-only `floor-off`, `floor-reflection-off`, and `environment-off` switches, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, brightness attribution, output probe, project media probe, and full capture passed. Outputs were written to `/tmp/rd-floor-env-attribution`, `/tmp/rd-floor-env-probe`, `/tmp/rd-floor-env-project-media`, and `/tmp/rd-floor-env-full`. Project pages retained visible media. The matrix narrows the remaining hard horizon/fog-bed issue to source draw-state/render-order interaction between environment, floor, and reflector rather than isolated floor brightness.
- Latest floor hierarchy QA: after restoring source `a1 group -> floor mesh -> i1 reflector` ownership, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, brightness attribution, project media probe, and full capture passed. Outputs were written to `/tmp/rd-floor-group-probe`, `/tmp/rd-floor-group-attribution`, `/tmp/rd-floor-group-project-media`, and `/tmp/rd-floor-group-full`. The probe confirmed `floor.groupPosition=[0,-1.65,0]` and `floor.planePosition=[0,0,0]`; project pages retained visible media.
- Latest floor material QA: after aligning source `o1` floor material surface to `RawShaderMaterial + GLSL3`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, brightness attribution, project media probe, and full capture passed. Outputs were written to `/tmp/rd-floor-rawshader-final-probe`, `/tmp/rd-floor-rawshader-final-attribution`, `/tmp/rd-floor-rawshader-final-project-media`, and `/tmp/rd-floor-rawshader-final-full`. The rejected `ShaderMaterial + GLSL3` path produced declaration collisions, so `RawShaderMaterial` is the source-shaped stable bridge. Project pages retained visible media.
- Latest reflector blur QA: after matching source `i1.update()` blur directions and removing the non-source reflector-camera `near` assignment, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, brightness attribution, project media probe, and full capture passed. Outputs were written to `/tmp/rd-reflector-blur-vpass-probe`, `/tmp/rd-reflector-blur-vpass-attribution`, `/tmp/rd-reflector-blur-vpass-project-media`, and `/tmp/rd-reflector-blur-vpass-full`. Project pages retained visible media.
- Latest `zA/VA` fragment-tail QA: after aligning the production work fragment tail with source `screenUv`, removing the dead zero-weight displacement color path, and assigning source alpha directly with `gl_FragColor.a = alpha`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, fresh shader dump, output probe, thumb spotlight probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-phase1-za-tail-shaders-fresh`, `/tmp/rd-phase1-za-tail-output`, `/tmp/rd-phase1-za-tail-thumb`, `/tmp/rd-phase1-za-tail-media`, and `/tmp/rd-phase1-za-tail-capture`. Desktop center-band luma is close to source (`-0.0013`), but the horizontal band delta remains above source (`+0.0041` desktop, `+0.0111` mobile), so this is shader-surface progress rather than Phase 1 closeout.
- Latest source reflector blur-loop QA: after replacing the fixed `(15,0)` then `(0,15)` floor-reflection blur sequence with source `i1`'s two-pass loop, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-reflector-source-blur-loop-probe`, `/tmp/rd-reflector-source-blur-loop-media`, and `/tmp/rd-reflector-source-blur-loop-capture`. Desktop center-band luma is effectively matched (`+0.0001`) and desktop max horizontal delta is now slightly below source (`-0.0017`), while mobile still has residual band differences (`-0.0132` center, `+0.0048` max delta).
- Latest `T1/w1/E1/x1` thumb shader-surface QA: after converting thumb plane and thumb composite shaders to source-style GLSL3 and bridging them with `RawShaderMaterial`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, fresh shader dump, thumb spotlight probe, output probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-thumb-rawglsl3-fresh-shader`, `/tmp/rd-thumb-rawglsl3-fresh-thumb`, `/tmp/rd-thumb-rawglsl3-output`, `/tmp/rd-thumb-rawglsl3-media`, and `/tmp/rd-thumb-rawglsl3-capture`. Thumb targets remained non-empty and project pages retained visible media; Phase 1 still has mobile/fog-bed and projection-feel residuals.
- Latest `Ka/Fn/Yi` mouse-simulation QA: after matching source four-decimal `Fn()` rounding for `Ka.update().uSpeed` and source `Yi()` rounded damping for `GA.mouseSpeed`, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, output probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-source-mouse-round-shader`, `/tmp/rd-source-mouse-round-output`, `/tmp/rd-source-mouse-round-media`, and `/tmp/rd-source-mouse-round-full`. During this audit, `u1.uShader1Mix2` was confirmed source-declared and not removed. Project pages retained five visible media tracks; Phase 1 still has mobile/fog-bed and spotlight/projection residuals.
- Latest `VA/zA` r164 physical residual QA: after narrowly stripping the source-absent Three r164 `USE_DISPERSION` and `USE_ANISOTROPY` uniform branches from the production work fragment, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, shader dump, thumb spotlight probe, output probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-r164-physical-shader`, `/tmp/rd-r164-physical-thumb`, `/tmp/rd-r164-physical-output`, `/tmp/rd-r164-physical-media`, and `/tmp/rd-r164-physical-capture`. Shader dump now reports `fragmentAnalysis.uniformsOnlyRebuild=[]` for the production work fragment. A broader `USE_SHEEN` strip was deliberately not accepted because the safe source boundary is not proven yet. Project pages retained five visible media tracks; Phase 1 still has mobile/fog-bed, spotlight/projection, and safe generated material-body residuals.
- Latest `i1` reflection blur no-clear QA: after removing rebuild-only `clear()` calls before and inside the floor-reflection blur pass, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, output probe, thumb spotlight probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-reflector-no-blur-clear-output`, `/tmp/rd-reflector-no-blur-clear-thumb`, `/tmp/rd-reflector-no-blur-clear-media`, and `/tmp/rd-reflector-no-blur-clear-capture`. Desktop band residual improved slightly (`+0.0006` center, `-0.0031` max-delta), while mobile max-delta remains worse (`+0.0235`), so this is source render-pass parity, not a Phase 1 closeout.
- Latest `A1/C1` pre-composite flow QA: after restoring source `displacementUv` and `vignetteF` computations, moving the `tNoise` sample back before contrast/saturation/media, and fixing the renderer audit checks for this flow, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, renderer audit, output probe, thumb spotlight probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-a1-flow-audit-2`, `/tmp/rd-a1-flow-output`, `/tmp/rd-a1-flow-thumb`, `/tmp/rd-a1-flow-media`, and `/tmp/rd-a1-flow-capture`. The audit now reports `A1` source/rebuild order matched and no inert-computation residuals. Project pages retained five visible media tracks; Phase 1 still has mobile/fog-bed and projection residuals.
- Latest sky/environment shader-surface QA: after matching source `u1/l1` commented include anchors and preserving source `z1`'s missing-constant `uShaderMix` binding as undefined/null, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, fresh shader dump, output probe, thumb spotlight probe, project media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-s130b-shader`, `/tmp/rd-s130b-output`, `/tmp/rd-s130b-thumb`, `/tmp/rd-s130b-media`, and `/tmp/rd-s130b-capture`. Shader dump confirms `u1-environment` source/rebuild commented include lists match; project pages retained five visible media tracks. Desktop center-band delta is `+0.0003`, mobile center-band delta is `-0.0160`, so Phase 1 remains open for mobile/fog-bed, projection feel, and transfer attribution.
- Latest `i1` reflector target-size QA: after matching source `i1.setSize()` by passing unrounded `width * .75` and `height * .75` values into the floor-reflection raw/read/write targets while keeping blur `uResolution` at full CSS size, `ASTRO_TELEMETRY_DISABLED=1 npm run build`, `git diff --check`, renderer audit, shader dump, desktop output probe, mobile output probe, thumb spotlight probe, project-media probe, full capture, and band analysis passed. Outputs were written to `/tmp/rd-reflector-size-audit`, `/tmp/rd-reflector-size-shader`, `/tmp/rd-reflector-size-output`, `/tmp/rd-reflector-size-mobile-output`, `/tmp/rd-reflector-size-thumb`, `/tmp/rd-reflector-size-media`, and `/tmp/rd-reflector-size-capture`; the mobile output probe now reports floor-reflection targets at `292.5x633`, which preserves source `390 * .75` sizing instead of the rebuild-only rounded `293x633`. Project pages retained visible media targets and no runtime failures. Desktop center-band delta is `+0.0036`, mobile center-band delta is `+0.0287`, so Phase 1 remains open for mobile/fog-bed, projection feel, and transfer attribution.

## Next Candidate Steps

1. Continue source audit around `a1/i1/h1/u1` draw-state, reflection target content, and generated material body ownership. The next work should be selected by mirrored-source evidence, 1:1 blocker severity, and controllable implementation risk, with target-content attribution preferred over numeric tuning.
   - Latest band probe narrows the hard horizon further: `floorReflectionRead` has the strongest adjacent-band jump (`0.3314` at `0.50`), `workRaw` already contains the discontinuity, and `skyComposite` is smooth. Prioritize reflector target content, oblique clipping, clear/background state, and floor sampling before revisiting `OA/A1`.
   - Latest reflector variants reject three simple fixes: disabling oblique clipping, disabling blur, and sampling raw reflection. The accepted no-clear blur pass, target/camera initialization, and sky `Lo` no-clear pass now match their source render-manager ownership, but mobile still has a horizontal residual. Next work should compare reflected scene content and camera/visibility state against source, not remove source `i1` or `V1/H1` mechanics.
2. Continue source audit around remaining `Lu/kA/OA/I1` output/transfer ownership only when a source-backed target-content divergence is proven. The obvious source tonemapping include residual, dead pre-bloom graph artifact, `I1/C1` lensflare/default-disabled drift, `A1.tScene`, `A1` flow order, `u1/l1`, `x1/v1`, `Lu/aA`, default `OA/CA` shader-surface residuals, and `Lu` bloom-output target ownership are now aligned.
3. Re-audit generated `GA/VA` and environment material body residuals where mirrored-source evidence shows a remaining source-structure, source-value, or source-behavior mismatch. The production `debug-va-*` fallback switches are now gone, `u1`'s uniform declaration surface is clean, and the obvious r164 `dispersion/anisotropy` work-fragment residual is stripped; remaining differences are mostly generated Three chunk bridge/body details and should not be treated as tuning license.
4. Keep project detail pages as regression checks because the darker project composite may share render-manager or media-background ownership with home.
5. Re-run `CAPTURE_SET=full` after the next WebGL batch and update `PHASE1_AUDIT.md` with new source-proven fixes, remaining source mismatches, or documented technical bridges.
6. Defer Phase 2 DOM/interaction work until Phase 1 has passed the source-parity gate, with visual QA used only as a regression check.

## Verification Baseline

Run after each completed batch of up to ten related source-alignment steps, unless a risky shader, navigation, or media change needs an earlier quick check:

- Batch size target: group related source-backed differences together, usually 3-10 per commit and never more than 10.
- Do not commit unsupported visual tuning just to fill a batch.
- Update this plan and `PHASE1_AUDIT.md` once per completed batch, after verification.

```sh
ASTRO_TELEMETRY_DISABLED=1 npm run build
git diff --check
```

For local smoke:

```sh
PORT=5173 SERVE_ROOT=dist FALLBACK_ROOT=public node scripts/serve.mjs
```

For source comparison QA:

```sh
PORT=5175 SERVE_ROOT=legacy-mirror/public FALLBACK_ROOT=public ENABLE_CONTENT_JSON_FALLBACK=1 node scripts/serve.mjs
CHROME_PATH=/usr/bin/google-chrome OUT_DIR=/tmp/rogier-compare CAPTURE_WAIT=4200 CAPTURE_SET=full node scripts/capture.mjs
```

Open:

```text
http://127.0.0.1:5173/?skip-preloader
http://127.0.0.1:5173/gc-2026/?skip-preloader
```

Browser smoke should confirm:

- home loads the latest JS bundle,
- project pages keep `.gl-canvas`,
- project pages keep desktop `[data-media][data-media-src]` tracks,
- no WebGL shader/runtime errors in available logs.
